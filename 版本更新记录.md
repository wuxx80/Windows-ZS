# ZS 装机助手 · 版本更新记录

> 项目：Windows-ZS
> 版本号：v0.0.268311（无显式版本升级，仅按开发增补轮 R1~R7 记录；用户未通知时版本号保持不变）
> 版本号规则：MAJOR.MINOR.PATCH；当前 PATCH 268311 为本项目首发的连续版本标识（首发日期 2026-08-31）。

---

## 最新开发轮索引（由新到旧）
- R7：R7 架构切换（2026-09-01）—— 主链路切换为 BCD + 全离线任务包；菜单精简；旧链路标记过渡期保留
- R6：首页四卡明亮渐变 + PE 无网离线注入（方案A / B / C）
- R5：用户注册 / 登录体系 + 后台菜单精简 + 客户端炫彩动效
- R4：U盘启动制作完善（本地写盘 + 生成 ISO + 在线 PE 拉取）
- R3：一键安装闭环 9 项修复
- R2：U盘制作与工具大全（四步向导 + 53 工具 / 10 分类 + B5 联动）
- R1：全功能流程闭环整理（客户端首页 + 无人值守续装全链路）
- 首发 v0.0.268311：初始正式版本（后端 35 表 / 后台 29 页 / 客户端框架）

---

## R7（2026-09-01 ~ 2026-09-02）：架构切换 · BCD 一次性启动 + 全离线任务包

> 本项目主链路切换。**废弃**「Windows 下单 → U 盘 / ISO / PXE → PE 端联网认领 waiting 任务」；**启用**「Windows 下单 100% 下载全部资源到 ZS_Task → BCD bootsequence 一次性启动 → PE 完全离线八阶段流水线 → 新系统 SetupComplete.cmd」。U盘 / ISO 降级为兜底逃生通道。本轮不提升正式版本号。

### 新增
- BCD 离线新架构设计文档 v1：docs/superpowers/specs/2026-09-01-zs-perfect-unattended-deployment-v1-design.md（顶层对比表 / 10 项任务目录 / task.ini 16 字段 / P1~P5 五步 / 固件双重方案 / 分区尾端验证 / 26 项故障逃生矩阵）
- 新主链路数据流与三层架构重定义：README / 项目理解报告 / 操作指南 全部按 R7 口径重写
- 管理后台菜单精简（19 项有效菜单）：从侧边栏和报表移除 PXE 配置 / 网络部署 / 客户端×3 / 任务×2 / 客户 / 工单
- reports.html 报表统计大改版：旧订单 / 工单 / 客户端版本 4 张卡片全部改为任务状态 / 客户端在线分布 / 镜像排行 7 卡片
- P0 基建缺口 6 项清单（RoleController 缺 / role 契约不一致 / WinPE_Agent 未入编 / 中间件未注册 / 测试架未提交删除 / Git 脏）写入六件套所有文档和设计文档顶部，修基建前不能开始 Phase 1~5 编码

### 修改
- 「一键装机」主链路说明：从「PE 联网认领 waiting + 心跳」改为「P1 下载 → P2 BCD 注入 → P3 10s 逃生窗 → P4 八阶段 → P5 SetupComplete」（代码仍保留旧链路，Phase 2 开始替换）
- 「U盘制作 / ISO 生成」定位：从并列主入口降级为「BitLocker / 多引导管理器场景下的兜底逃生通道」（功能不变，定位降维）
- 六件套文档去外部品牌名：所有文档统一使用内部任务目录名 ZS_Task（之前的参考名已移除）
- 六件套目录说明去虚构：项目结构.txt / README / 操作指南 删除不存在的 RoleController.php / StorageController.php / e2e_winpe_test.php / ZS_Installer.sln 等虚构项；.sln 统一为 ZS_Installer.slnx
- 项目状态真实化：所有六件套文档把「全功能完成 / 测试通过」声明改为逐项磁盘实查状态，明确标注 5 个测试脚手架实际不存在、WinPE_Agent 磁盘存在但未入编

### 记录 P0 技术债务（未修复，必须在 Phase 0 内完成）
| 债务 # | 内容 | 影响 |
|---------|------|------|
| P0-1 | server/app/controller/admin/RoleController.php 缺失；route/admin.php 无 Role 路由 | 用户管理角色 CRUD 不可用 |
| P0-2 | users.html 提交 role:string；后端 UserController.php 读 role_id:int | 角色写空或 0 |
| P0-3 | WinPE_Agent 未入 ZS_Installer.slnx、Git 未跟踪（WinPE_Agent/ 目录 untracked） | 新 clone 缺新主链路 PE 执行者 |
| P0-4 | config/middleware.php 仅注册 CorsMiddleware；AuthMiddleware / LogMiddleware 存在但未挂 | zs_logs 空；认证手动判断 |
| P0-5 | 5 个测试脚手架（_b5_logic_test / _b5_ui_harness / _iso_logic_test / _offline_logic_test / _offline_ui_harness）磁盘已删、Git delete 未提交 | 原 R2/R4/R6「测试通过」源码不可复现 |
| P0-6 | Git 工作树脏：28 modified / 8 deleted / 6 untracked（含 WinPE_Agent） | 新 clone 不能复现本机状态 |

### 验证
- README / 项目结构.txt / 项目理解报告.md / 操作指南.md / 版本更新记录.md / 开发计划表.md 六件套文档一致性人工交叉检查通过
- 后台侧边栏与 reports.html 浏览器刷新检查：PXE / 网络部署菜单不存在；报表 7 卡片改为任务 / 在线客户端 / 镜像排行
- 磁盘实查：项目结构.txt 目录清单、文件名、控制器数、模型数、页面数 与 LS / Glob 结果一一对应
- 编译状态：Windows_Client、WinPE_Client Release 编译 0 错 0 警；WinPE_Agent Release（单独）0 错 1 nullable 警告（P0-3 入编后修复）

### R7-C：Startnet.cmd ↔ WinPE_Agent 契约对齐（2026-09-02，子轮）

> 设计文档 §5.1 定义 Startnet.cmd 调用 `ZS_PE_Agent.exe --auto --task task.ini --manifest zs_manifest.key --log pe_log.txt`；但原 Program.cs 不支持这些参数，task.ini / zs_manifest.key 的解析与校验逻辑完全缺失。本子轮完成契约对齐，让 Startnet.cmd → ZS_Agent 主链路真正可跑通。

#### 新增（3 个 .cs 文件）
- `WinPE_Client/Models/TaskIni.cs`：设计 §2.1 定义的 16 字段模型（meta/target_disk/partition_scheme/system_image/drivers/software/optimize 共 7 个 section），含嵌套类 TaskMeta / TargetDisk / PartitionScheme / SystemImage / DriverOptions / SoftwareEntry / OptimizeOptions
- `WinPE_Client/Services/TaskIniParser.cs`：解析 task.ini → TaskIni 模型，支持 section / key=value / 注释 / 动态编号字段（如 [software] 中 sw1_*/sw2_*）聚合为 SoftwareEntry 列表；附带 `ResolveImagePath()` 和 `ResolveAgentPath()` 镜像/Agent 路径解析
- `WinPE_Client/Services/ManifestValidator.cs`：解析 zs_manifest.key（每行 `sha256=哈希=相对路径`），按目录逐文件计算 SHA-256 对比期望值，生成 VerifyReport（含 Total/Passed/Failed/Results/AllPass）；设计 §6.6 装机前必须 100% 通过

#### 修改（2 个文件）
- `WinPE_Agent/Program.cs`：
  - Main() 新增 `--auto / --task / --manifest / --log` 参数解析（与 Startnet.cmd 契约一致）
  - 新增 `RunAutoMode(taskPath, manifestPath, noReboot)` 入口：①校验 task.ini 存在 → ②解析 → ③manifest 校验（失败立即 Exit 3）→ ④解析镜像路径（失败 Exit 4）→ ⑤映射为 InstallOptions → ⑥调 ExecutePipeline 走 7 步管线
  - 新增 `MapTaskIniToOptions(TaskIni)` 把 task.ini 映射为现有 InstallOptions（clean_whole_disk → AutoPartition、drivers.inject → DriverInject、oobe=auto → Unattended 等）
  - 修复 `Truncate(string s, int n)` 的空引用警告（CS8602）
- `WinPE_Client/Services/TaskIniParser.cs:208`：修复 `System.Reflection.Assembly.Location` 单文件发布 IL3000 警告（改用 `AppContext.BaseDirectory`）
- `WinPE_Agent/WinPE_Agent.csproj`：在 ItemGroup 添加 3 个新文件的 Compile Include Link（保持单一数据源，避免重复维护）

#### 新增文档
- `docs/superpowers/specs/2026-09-01-客户端模块调整清单.md`：基于 R7 设计文档对齐的客户端模块调整清单（A. Windows_Client / B. WinPE_Client / C. 后端 API 缺失端点 / D. Shared 类库抽离建议），含 P0~P4 优先级排序和验证里程碑 M1~M3

#### 验证
- WinPE_Agent Release 编译 **0 错 0 警**（修复了 CS8602 空引用 + IL3000 单文件发布 共 2 个警告）
- 契约对齐人工核对：Startnet.cmd 调用形式与 Program.cs 参数解析完全一致
- task.ini 解析逻辑覆盖 7 个 section + 动态软件包字段聚合

#### 已知后续工作（C8 整理，详见客户端模块调整清单）
- ✅ B.5 PE 端装机管线 4 个 Service 已在 R7-D 完成：FirmwareDetector（§6.0 固件双判）/ PartitionVerifier（§6.1 分区尾端验证）/ SetupCompleteBuilder（§6.6 模板渲染）/ UnattendXmlBuilder（§6.6c）
- ✅ B.6 Program.cs RunAutoMode 集成 4 个 Service 已在 R7-D 完成（固件双判/分区尾端验证/SetupComplete 模板渲染/Unattend.xml 生成）
- ⏳ B.2/B.3 WinPE_Client.csproj 改 Exe + 移除 UseWPF + 移除 11 类 GUI 文件（延后，P1.8 可选项）
- ✅ A.3 Windows_Client 8 个新 Service 已在 R7-D 完成（ZsTaskBuilder/TaskIniWriter/ManifestWriter/PeAssetDownloader/SystemImagePreDownloader/DriverPackageDownloader/SoftwarePackageDownloader/BcdInjector）+ FirmwareDetector/TaskIni 通过 csproj Link 复用 + A.2 InstallWizardViewModel R7 集成已完成
- ⏳ C 后端 API 端点补齐：`/api/v1/peAssets/bootWim` / `/api/v1/peAssets/bootSdi` / `/api/v1/peAssets/meta`（P3 待实现）

---

### R7-D：C 类契约对齐补齐 + P2 Windows_Client 离线任务包（2026-09-02，子轮）

> R7-C 完成了 Startnet.cmd ↔ WinPE_Agent 参数契约对齐（task.ini/manifest 解析），但 PE 端装机管线 4 个核心 Service 和 Windows 端离线任务包构建器全部缺失。本子轮完成 P1（PE 端 4 Service + Program.cs 集成）和 P2（Windows_Client 8 个新 Service + InstallWizardViewModel R7 集成），让 Windows 下单 → ZS_Task 构建 → BCD 注入 → 自动重启进 PE → 八阶段装机全链路代码层闭环。

#### 新增（12 个 .cs 文件）

**PE 端 4 个 Service（WinPE_Client/Services/，WinPE_Agent 通过 csproj Link 复用）**
- `FirmwareDetector.cs`：设计 §6.0 固件双判——注册表主判定（HKLM\FirmwareType，0 IO）+ diskpart 回退（读 Partition Style）+ 6 级冲突处理（双判一致/单判 Unknown/CSM 冲突以 diskpart 为准/全 Unknown Exit 2）+ override 强制模式（force_gpt/force_mbr）
- `PartitionVerifier.cs`：设计 §6.1 分区尾端验证——GPT 分支验证 ESP 分区存在 + 卷标含 ESP + 文件系统为 FAT/FAT32；MBR 分支验证 Active 标志；失败时 Exit 3
- `SetupCompleteBuilder.cs`：设计 §6.6 模板渲染——按 task.ini Software 列表生成静默安装命令（msiexec/exe + args + expect_exit）+ 系统优化命令（休眠/待机/页面文件/遥测/Cortana）+ 自清理开关 + 自毁逻辑
- `UnattendXmlBuilder.cs`：设计 §6.6c OOBE 应答——auto 模式生成 Unattend.xml（SkipMachineOOBE + HideEULAPage + 本地管理员 + TimeZone + zh-CN + Win11 BypassNRO）；manual 模式返回 null 跳过

**Windows_Client 端 8 个新 Service（Windows_Client/Services/）**
- `TaskIniWriter.cs`：task.ini 7 section/16 字段生成器（UTF-8 无 BOM，bool 用 yes/no，软件用 sw{seq}_{field} 动态编号），与 PE 端 TaskIniParser 解析契约对齐
- `ManifestWriter.cs`：zs_manifest.key SHA-256 清单生成器（格式 `sha256=大写HEX=相对路径`，自动扫描 ZS_Task 目录所有文件，排除 manifest 自身/pe_log.txt/.part），与 PE 端 ManifestValidator 解析契约对齐
- `PeAssetDownloader.cs`：boot.wim + boot.sdi + ZS_PE_Agent.exe HTTP 下载（复用 ApiService.DownloadFileAsync，支持进度+取消）
- `SystemImagePreDownloader.cs`：系统镜像预下载到 images/ 子目录，返回相对路径供 task.ini [system_image].file 使用
- `DriverPackageDownloader.cs`：驱动包下载到 drivers/，zip 自动解压到 drivers/{name}/ 子目录
- `SoftwarePackageDownloader.cs`：软件包批量下载到 software/{key}/，支持进度上报（current/total/key）
- `ZsTaskBuilder.cs`：P1 全流程编排器——建目录+README → 下载 10 项资源（PE 资产/镜像/驱动/软件）→ 生成 task.ini（TaskIniWriter）→ 生成 zs_manifest.key（ManifestWriter）→ V1 验证 7 项 → 失败回退删 ZS_Task 目录（§3.3 F1 100% 零残留）
- `BcdInjector.cs`：BCD bootsequence 一次性启动项注入器——备份 BCD → /copy {current} 继承平台参数 → 配置 ramdisk 指向 boot.wim → 配置 {ramdiskoptions} 指向 boot.sdi → /bootsequence 一次性启动 → Rollback 回退（删启动项+恢复备份）

#### 修改（6 个文件）
- `WinPE_Agent/Program.cs`：
  - RunAutoMode 新增固件判定（FirmwareDetector.Detect，Unknown 时 Exit 2）
  - ExecutePipeline 新增分区脚本生成（BuildPartitionScript：GPT=ESP+MSR+Recovery+Primary / MBR=Primary+Active）+ 尾端验证（PartitionVerifier.Verify，失败 Exit 3）+ 引导修复双路径（UEFI=/f UEFI / BIOS=/f BIOS）+ SetupCompleteBuilder.WriteToSystem + UnattendXmlBuilder.WriteToSystem
  - 新增 BuildPartitionScript 辅助方法（按 task.ini PartitionScheme 字段生成 diskpart 脚本）+ CopyDirectory 递归复制（§6.6 软件目录拷贝）
- `WinPE_Client/Services/DiskPartService.cs`：新增 ExecuteRawScriptAsync 方法（执行自定义 diskpart 脚本字符串，供动态 GPT/MBR 分区脚本使用）
- `WinPE_Agent/WinPE_Agent.csproj`：添加 FirmwareDetector/PartitionVerifier/SetupCompleteBuilder/UnattendXmlBuilder 4 个 Compile Link
- `Windows_Client/Windows_Client.csproj`：添加 TaskIni.cs + FirmwareDetector.cs 2 个 Compile Link（复用 WinPE_Client 源码，单一数据源）
- `Windows_Client/Services/ApiService.cs`：新增 GetBaseUrl() 方法（供 PeAssetDownloader 等拼接下载 URL）
- `Windows_Client/ViewModels/InstallWizardViewModel.cs`：
  - 新增 ExecuteR7OfflineBuild 方法：选盘（非 C 盘 NTFS ≥20GB）→ ZsTaskBuilder.BuildAsync 构建 ZS_Task → BcdInjector.Inject 注入 BCD → shutdown /r /t 30 自动重启
  - StartExecution 中优先调用 R7 流程，失败回退旧 PreDownloadAndInjectOfflineTask（方案B zs_task.json + 手动进 PE）
  - 新增 SelectTaskDrive 静态方法（枚举固定盘按 NTFS + 可用空间过滤）

#### 验证
- WinPE_Agent Debug 编译 **0 错 0 警**
- Windows_Client Debug 编译 **0 错 0 警**
- 契约对齐：task.ini 格式与 TaskIniParser 解析契约一致（bool=yes/no，software=sw{seq}_{field}）；manifest 格式与 ManifestValidator 解析契约一致（sha256=HASH=path）
- 全链路代码层闭环：Windows 下单 → ZsTaskBuilder 生成 ZS_Task → BcdInjector 注入 BCD → 30s 自动重启进 PE → ZS_Agent 读 task.ini/manifest → 八阶段装机

#### 已知后续工作
- ⏳ P3：后端 API 端点补齐（/api/v1/peAssets/bootWim / bootSdi / meta）—— 依赖 P2 客户端 API 契约确定后实现
- ⏳ P4：Shared 类库抽离（ZS_Shared/ .NET Standard）—— TaskIni/TaskIniParser/ManifestValidator/ManifestWriter/FirmwareDetector 等可抽离，待 P1/P2 稳定后重构
- ⏳ P1.8（可选）：WinPE_Client.csproj 改 Exe + 移除 UseWPF + 清理冗余 GUI
- ⏳ 旧模块移除：OfflineTaskService/OfflineTask 作为 R7 失败回退路径保留，待 R7 充分测试后移除
- ⏳ 软件列表/驱动包参数：ZsTaskBuilder.BuildRequest 的 SoftwareList/DriverId 字段待 UI 层补充（当前 InstallWizardViewModel 中 TODO 留空）

---

## R6（2026-08-31）：首页四卡明亮渐变 + PE 无网离线无人值守（方案A / B / C）

> 针对「PE 环境无网络时登录无效」问题，实现离线无人值守双保险注入（方案A：U盘 / ISO 写入装机镜像 + 离线任务；方案B：Windows 下单预下载镜像到数据盘 + 注入 zs_task.json），PE 无网时自动扫描磁盘离线完成装机。不提升版本号。

### 新增
- 首页四张圆形入口卡片改为明亮纵向渐变 + 半球高光 + 彩色投影自然凸起
- 方案A（U盘 / ISO 离线注入）：U盘制作步骤③新增「写入装机镜像 + 离线无人值守任务」；拷贝装机镜像到根目录 ZS_Images\，生成 zs_task.json
- 方案B（Windows 预下载注入）：PreDownloadAndInjectOfflineTask 预下载镜像到 D:\ZS_Cache\images；取回 unattend/firstLogon 脚本；注入 D:\ZS_Cache\zs_task.json
- 方案C（PE 离线模式）：WinPE 端无网时 ScanOfflineTasks 扫描全部磁盘（固定 + 可移动）查找 zs_task.json；命中后首页出现「待执行任务」离线卡片
- 离线任务契约双端一致：Windows_Client/Models/OfflineTask.cs + WinPE_Client/Models/OfflineTaskModels.cs；OfflineTaskService 双端实现
- 测试脚手架：_offline_logic_test（6 项）+ _offline_ui_harness（11 项 UI 像素自动化）

### 验证
- 双端 Release 编译 0 错 0 警
- _offline_logic_test 6 项 PASS；_offline_ui_harness 11 项 PASS
- unattend/firstLogon 接口返回对齐 UnattendPayload/FirstLogonPayload

### 备注
> R6 的方案A/B/C 在 R7 里作为历史兼容实现，但不再是 R7 主链路。R7 主链路使用 ZS_Task 目录 + BCD bootsequence，Phase 3 会评估 R6 代码是否保留。

---

## R5（2026-08-31）：用户注册 / 登录体系 + 后台菜单精简 + 客户端炫彩动效

### 新增
- 用户注册 / 登录体系：后端 POST /api/v1/auth/register（公开，注册即自动登录，user 角色）；install.sql 新增普通用户角色 + 测试账号 wuxx80 / a111111
- Windows 客户端 LoginWindow + SessionStore；核心功能（一键装机 / U盘 / 绿软）需登录
- 公开 GET /api/v1/site/info + settings.html「站点信息」组（Logo / 主标题 / 副标题 / 标语 / 官网 / 版本号 / 版权 / 联系 / 关于）
- Windows 客户端 Settings / About / Contact / Version / Software 窗口；WinPE 端 About / Contact / Settings / Version 窗口
- 双端炫彩动效：流动渐变背景 + 光斑 + 星尘粒子 + 入口呼吸光晕 + 入场动画

### 修改
- 客户端登录：Windows 端由自动 admin 登录改为用户注册 / 登录；登出清除会话
- 后台菜单精简：移除客户端×3 / 任务×2 / 业务（客户 / 工单）菜单入口与页面；底层任务流程保留
- 仪表盘统计口径改为「待执行任务 + 在线客户端」

### 修复
- Image 模型 file_size 返回整数；新增 size_display；image version string→int
- settings.html 新增站点信息组字段对接

---

## R4（2026-08-31）：U盘启动制作完善（本地写盘 + 生成 ISO + 在线 PE 拉取）

### 新增
- U 盘制作输出方式双模式：写 U 盘 / 生成 ISO 镜像
- 纯 C# 可引导 ISO 生成器 IsoBuilder.cs：ISO9660 + Joliet + El Torito 单引导记录 + 双段引导目录（Legacy + EFI）；移除 DiscUtils 第三方依赖
- EfiSysGenerator.cs：PE 缺 efisys.bin 时用 bootx64.efi 自动生成 FAT12 1.44MB 软盘镜像兜底
- 在线 URL PE 来源：UDiskService.DownloadPeUrlAsync 从任意 HTTP(S) URL 直接拉取，缓存 C:\ZS_Cache\pe\
- 测试脚手架：_iso_logic_test

### 修复
- MountIsoAsync：改用 -EncodedCommand + DriveType=5 前后对比精确取新盘符
- FAT12 efisys.bin 簇链 SetFat 奇偶簇位偏移
- 引导目录：单引导记录 + 双段目录（Legacy 条目 + EFI 段头 + EFI 条目）
- PVD PathTableSize：改为路径表实际字节数（非扇区对齐值）
- CopyDirectoryAsync 裸盘符规范化补「:\」

---

## R3（2026-08-31）：一键安装闭环 9 项修复

### 修复
r1 镜像下载/校验/备份真实化；r2 client_id 断链补 EnsureRegistered；r3 重试状态 failed/cancelled→waiting；r4 进度上报乐观锁 + client_id 身份校验；r5 立即重启真实 shutdown /r；r6 CheckAuth 注册类型 winpe→windows；r7 删 StartInstall 死代码；r8 磁盘序号跨环境模糊匹配（disk_index/disk_size/disk_model 特征持久化）；r9 失败重试复用原任务 RetryTaskAsync 不新建。

---

## R2（2026-08-31）：U盘制作与工具大全

新增：U盘四步向导 + 写盘 4 项校验 + 盘号二次确认 + 红色警示；PE 文件下载 C:\ZS_Cache\pe\（断点续传 + 哈希）；工具大全 53 工具 / 10 分类（Data/Tools/tools.json）+ 本地 Process.Start 运行 + 管理员自动提权；勾选「写入内置工具」合并写入 U 盘 ZS_Tools 目录；测试脚手架 _b5_logic_test + _b5_ui_harness。

---

## R1（2026-08-31）：全功能流程闭环整理

新增：WinPE 首页重做（服务器状态徽标 / 待执行任务卡片 / 四卡入口）；PE 启动自动闭环：连接服务器 → 客户端自注册 → 检测 waiting → 启动心跳；PE 续装闭环：立即继续预填 → 跳过配置直接执行 → 写入 unattend.xml + 生成 SetupComplete.cmd；Windows 首页最近任务卡片 + 30 秒心跳；仪表盘在线客户端数修复（last_heartbeat 90s 内 + approved）；任务停滞检测（running >20min / waiting >24h is_stalled）。

---

## v0.0.268311（首发版本 · 2026-08-31）
完整项目初始化 + 数据库 35 表 + GitHub；后端 ThinkPHP 6.1.4 / 36 状态码 / 中间件 / 异常处理器；管理后台 29 页 + 仪表盘；API 30 接口浏览器自动化测试；修复 Hash / 字段名 / 路由冲突 / BOM 编码 4 项问题。

---

## 已知问题（跟随 R7 更新）
1. P0 基建 6 项债务未修复（见上），修完前不要在生产使用后台用户管理和操作日志
2. 新主链路 P1~P5 未实现，当前「一键装机」按钮仍走旧链路（R1~R6 遗留），勿在生产用于真实装机
3. WinPE_Agent 未入 sln、未入 Git、未实现 BCD 注入脚本、未实现固件 6 级冲突策略
4. Startnet.cmd（打包进 boot.wim）和 SetupComplete.cmd（写入新系统）两个脚本模板未在仓库落地
5. 旧链路 e2e_winpe_test.php 已从部署包移除，新 E2E 回归测试需在 Phase 4 后重写