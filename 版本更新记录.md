# ZS 装机助手 · 版本更新记录

> 项目：Windows-ZS
> 起始版本：v0.0.268311

---

## v0.0.268311（当前版本 · 初始版本）

> 发布日期：2026-08-31
> 当前状态：全功能版本（阶段一~六完成，可部署发布）
> 版本说明：本项目初始正式版本号，对应开发计划阶段一至阶段六全部完成后的首次发布
> 开发增补轮：R1 全功能流程闭环 → R2 U盘制作与工具大全 → R3 一键安装 9 项闭环修复 → R4 U盘启动制作完善（本地写盘 + 生成 ISO + 在线 PE 拉取） → R5 用户注册/登录体系 + 后台菜单精简 + 客户端炫彩动效

### 新增
- 一键装机六步向导（WinPE 端完整实现 + Windows 端同步）：环境检测 → 选择镜像 → 目标磁盘 → 安装选项 → 确认 → 执行，对齐《一键装机交互设计.md v3.0》
- WinPE 端新增：InstallWizardWindow.xaml/.cs 六步向导窗口、Models/WizardModels.cs（EnvCheckItem/EnvIssueItem/WizardOption/TemplateItem/ExecStepItem/ImageSourceItem/ConflictItem/PartitionEditItem）、ViewModels/InstallWizardViewModel.cs（完整向导逻辑：9 项环境检测逐项实时回填、镜像来源切换/搜索防抖/分页筛选、磁盘冲突检测/GPT-MBR 转换提示/分区编辑器、选项联动规则、确认汇总、执行进度三层联动、暂停/继续/取消、重启倒计时）
- Windows 端新增：InstallWizardWindow.xaml/.cs 六步向导窗口、ViewModels/InstallWizardViewModel.cs（安全版：仅创建装机任务并上报进度，实际装机步骤标注"将在 WinPE 中执行"，避免宿主机破坏性磁盘操作）、Services/DeviceService.cs + DiskPartService.cs + ImageDeployService.cs（从 WinPE 迁移）
- 双端扩展 ApiService：GetPaginatedAsync 分页助手、镜像列表过滤参数（keyword/format/os_type）、任务控制接口（pause/resume/cancel）、远程 URL 添加、无人值守模板/软件模板/驱动列表拉取
- 双端新增值转换器（Converters.cs）：StringToBrushConverter、IndexZero/One/TwoToBoolConverter（分区方案单选）、BoolToConnectTextConverter、BoolToStatusBgConverter
- 完整项目初始化（设计文档、六件套、数据库 35 张表、GitHub 仓库）
- 后端：ThinkPHP 6.1.4 + MySQL 5.7 + 32 个后台控制器 + 36 个模型 + 8 个服务层 + 3 个中间件
- 后台前端：Layui 2.9+ 风格，29 个管理页面 + 首页 + 登录页 + 控制台仪表盘
- 镜像管理：上传/分片上传/校验/版本历史/标签系统/回收站
- 任务管理：创建/调度/监控/日志/重试/进度实时同步
- 客户端管理：注册审核/分组/版本管理/心跳检测
- 客户端自注册接口（POST /api/v1/clients/register）：WinPE/Windows 客户端连接服务器时幂等注册，已存在则刷新心跳
- WinPE 客户端（.NET 8 WPF）：装机引擎/DiskPart/引导修复/驱动注入/进度同步
- Windows 客户端（.NET 8 WPF）：四大入口（装机/U盘/工具/绿软）+ 登录
- 客户端发布脚本 publish.ps1（自包含 + 框架依赖两种模式）
- 服务器端部署包制作脚本 deploy_server.ps1（生成 zs_server_deploy.zip）
- 客户端端到端回归测试脚本 server/e2e_winpe_test.php（登录→注册→建任务→进度→完成，WinPE/Windows 双场景 15 项 PASS）

### 修改
- WinPE 端一键装机由简化首版补全为六步向导：新增镜像来源切换（本地服务器/远程URL/本地缓存/自定义源）、分区方案（自动/自定义/保留）、安装选项九宫格 + 扩展配置面板、环境异常汇总（致命/警告区分 + 修复/忽略/强制继续）、执行页暂停/继续/取消
- Windows 端一键装机由旧单页安装视图重构为六步向导（与 WinPE 端一致），并做安全化处理：宿主机仅创建任务 + 上报进度，分区/部署/驱动/引导等破坏性操作标记"将在 WinPE 中执行"
- 双端主窗口"一键装机"入口改为打开六步向导子窗口（MainViewModel.OpenInstallWizardCommand），移除旧 Install 视图相关属性
- 任务模块与 zs_tasks 表结构对齐：新增 task_no 任务编号、unattend_template_id、target_disk_index、target_partition、partition_scheme 分区方案、options 参数 JSON 列（兼容旧 params/type 参数）
- 任务进度上报支持状态闭环：客户端可上报 running/completed/failed，自动计算任务耗时（duration）
- 任务模板接口返回分区方案枚举（partition_schemes: auto/custom/keep）
- 管理后台任务管理页面（tasks.html）同步任务字段调整（镜像/磁盘/分区/分区方案）
- WinPE 客户端集成：连接服务器 → 自注册（幂等，client_id 复用）→ 创建装机任务 → 按步骤上报进度（分区/部署/修复引导/驱动注入/完成）
- Windows 客户端集成：登录 → 自注册（类型 windows，幂等）→ 创建装机任务 → 上报 running 状态（保持待 PE 环境执行，不直接标记完成）
- 注册幂等策略增强：① 按 client_id 匹配刷新心跳；② 未传 client_id 但 MAC 匹配则复用（兼容 WinPE 重启丢失本地 client_id）；③ 否则新建
- 客户端注册响应含服务端 id + client_id + 状态，客户端保存复用
- 客户端列表页（clients.html）使用 last_ip / last_heartbeat 字段展示 IP 与在线时间

### 优化
- 性能：仪表盘 7 天装机趋势、镜像标签统计 N+1 查询消除（单次 GROUP BY / LEFT JOIN）
- 部署：生产环境安全配置指南、客户端部署、WinPE 集成、部署清单、服务器端部署包
- 路由：任务带 :id 的特定路由（progress/cancel/retry/pause/resume/logs/detail）前置声明，避免误路由到通用 create
- 清理：移除临时调试脚本（debug_clients/progress/route.php），保留 e2e 回归测试脚本

### 修复
- 异常处理器返回 HTML 而非 JSON（移除 $request->id() 调用）
- UTF-8 BOM 导致的 JSON 解析错误（统一无 BOM 编码）
- Auth/profile 返回 null 用户数据
- ImageService::createSnapshot / getInfo 数据库字段名错误
- Image 详情路由返回列表数据（路由优先级）
- 通知未读数接口路由冲突（改名为 unread_count）
- 工单创建时自动创建客户
- 镜像删除 recycle_bin 表字段名不匹配
- TaskController 使用不存在的 TaskLog 模型（改为 TaskRecord）
- route/admin.php 命名空间分隔符语法错误
- 已跟踪 obj 构建产物从 Git 索引移除，.gitignore 新增 bin/obj/publish 忽略
- 客户端列表 500 错误：排序字段 last_online_time → last_heartbeat
- .NET 版本号格式：0.0.268311 → 0.0.268.311（程序集版本段限制 0-65534）

### 技术栈
- 后端：PHP 8.0+ + ThinkPHP 6.1.4 + MySQL 5.7+ + Nginx
- 后台前端：原生 HTML/CSS/JS（Layui 2.9+ 风格）
- 客户端：C# WPF (.NET 8) + System.Management (WMI) + System.Text.Json
- 客户端架构：MVVM 模式

### 安全审查
- 身份验证（Token 缓存 + Bearer）、CORS、异常 JSON 脱敏、文件上传白名单 + SHA-256、日志密码脱敏、SQL 注入防护、.env 排除 —— 全部通过
- 生产环境建议：APP_DEBUG 关闭、JWT 密钥更换、登录限流

### 联调验证
- 44/44 个 API 端点全部通过测试
- 管理后台前端与 API 联调验证通过
- 客户端构建验证通过（WinPE 0 错误 / Windows 0 错误）
- 一键装机六步向导双端编译验证通过 + Windows 端向导 UI 自动化验证通过（六步导航/环境检测逐项回填/镜像选择渲染正常，无 XAML 解析错误）
- 客户端端到端回归测试 15 项全部通过（登录/注册幂等/建任务/进度闭环/日志/非法参数拦截/Windows 场景）
- U 盘制作与 ISO 生成验证：B5 逻辑测试 4 项 + STA WPF UI 测试 4 项通过；_iso_logic_test 逻辑自测（FAT12 efisys.bin + ISO 结构 + 在线 PE 端到端）通过；在线拉取 ~340MB PE 生成 ~337MB 可引导 ISO 成功
- 发布产物启动验证通过（WinPE 自包含 145.7MB / Windows 框架依赖 0.5MB）
- 服务器端部署包生成验证通过（zs_server_deploy.zip，0.2MB）

### 已知问题
- 一键装机六步向导真实装机步骤（分区/部署/驱动注入/引导修复）需在真实 PE 环境验证（当前仅 Windows 端向导 UI 自动化验证 + 双端编译验证）
- 三端端到端联调需在真实服务器部署后验证
- WebSocket 实时通信尚未实现
- 登录频率限制尚未实现

---

## 开发过程记录

### v0.0.268311-R1（全功能流程闭环整理轮 · 客户端首页与无人值守全链路闭环）

> 发布日期：2026-08-31
> 说明：本轮回合不提升正式版本号，作为 v0.0.268311 的开发增补记录，目标是对齐《详细设计文档.md》客户端首页与一键装机设计，打通「Windows 下单 → WinPE 续装 → 无人值守全自动完成」的完整闭环。

### 新增
- WinPE 客户端首页重做（对齐设计文档 §4.1）：顶部服务器状态徽标（在线/离线）、客户端审核状态提示条、待执行任务卡片（任务号 + 镜像名 + 「立即继续/重新配置」）、4 大入口卡片（一键装机/U盘制作/工具大全/绿色软件）
- WinPE 启动自动闭环：窗口加载完成自动「连接服务器 → 客户端自注册 → 检测本机 waiting 任务 → 启动 30 秒心跳」（MainViewModel.Initialize 接入 Loaded 事件）
- WinPE 续装闭环：检测到本机待执行任务 → 「立即继续」预填向导（镜像/磁盘/分区方案/全部安装选项来自任务订单）→ 跳过配置直接执行；执行期补充两步：写入无人值守应答 C:\Windows\Panther\unattend.xml + 生成首次登录脚本 C:\Windows\Setup\Scripts\SetupComplete.cmd（装软件/系统优化）
- Windows 客户端首页新增「最近任务」卡片（对齐设计文档 §4.2）：登录后/下单后自动刷新，显示最近任务号 + 状态（waiting 提示「请重启进入 WinPE 自动继续装机」）
- Windows 客户端新增 30 秒心跳（后台据此判定 Windows 端在线/离线）
- 后端智能补强：
  - 仪表盘在线客户端数修复（改为 last_heartbeat 90 秒内 + approved 判定，非 status 字段）
  - 仪表盘新增任务实时统计（waiting/running/failed 数量）
  - 任务列表停滞检测（running 超 20 分钟无进度更新 / waiting 超 24 小时，标注 is_stalled + 原因，仅供展示不落库）
  - 任务列表逐条附加客户端在线状态（client_online）

### 验证
- 双端编译：WinPE 0 错误 0 警告 / Windows 0 错误 0 警告
- 后端 e2e 闭环实测 29 项全部通过：登录 → 注册幂等 → 审批 → 建任务 → 进度闭环（分区/部署/修复引导/完成）→ 非法进度拦截 → Windows 下单置 waiting → PE 拉取 waiting → waiting 转 running 认领 → 无人值守 XML 生成（含 FirstLogonCommands）→ 任务关联无人值守模板 → options 契约 13 键完整 → 取消 waiting/running → 心跳 → 客户端列表在线判定

### v0.0.268311-R2（U盘制作与工具大全开发轮）

> 发布日期：2026-08-31
> 说明：完成 U 盘制作四步向导与工具大全双端实现（B5 联动：勾选「写入内置工具」自动拷贝工具到 U 盘，PE 内离线可用），不提升正式版本号。

#### 新增
- U 盘制作四步向导（Windows + WinPE 双端 UDiskWindow.xaml + UDiskViewModel + UDiskService）：① 选择U盘 → ② 选择PE版本 → ③ 选项 → ④ 确认执行；支持格式化还原、多 PE 启动盘、写入装机助手客户端、应用 PE 定制
- 写盘安全护栏：4 项写盘校验 + 盘号二次确认 + 红色警示；执行过程可暂停/取消；管理员权限检测（不足自动提权重启）；多实例互斥锁（Local\ZS_UDiskWrite）防并发写盘
- PE 文件下载：C:\ZS_Cache\pe\ 缓存目录，断点续传 + 哈希校验 + 进度显示；后端 PeVersionController 新增客户端列表接口（clientList）与 PE 文件下载接口（流式 + Range 断点续传）
- 工具大全（Windows + WinPE 双端 ToolsWindow.xaml + ToolsViewModel + ToolService）：53 个工具 / 10 分类（disk/system/network/hardware/backup/password/recovery/boot/pe/comprehensive），每工具含 emoji 图标/exe 路径/参数/管理员标记；本地清单（Data/Tools/tools.json）+ 服务器双源
- 工具执行：本地内置工具经 Process.Start 运行并检测提权；U 盘制作勾选「写入内置工具」时合并本地 Tools 目录与 ZS_Cache/tools 缓存写入 U 盘 ZS_Tools 目录，PE 内离线可用；未检测到工具文件时明确提示跳过且不中断制作
- 管理后台 PE 版本页（pe-versions.html）增强：文件大小显示、启用状态、客户端下载入口
- 测试脚手架：_b5_logic_test（4 项逻辑测试：IncludeTools 合并拷贝/空目录不中断制作等）+ _b5_ui_harness（STA WPF UI 自动化测试 4 项）

#### 验证
- 双端（Windows/WinPE）Release 编译 0 错误 0 警告
- 4 项逻辑测试 + 4 项 UI 自动化测试全部通过；双端 UDiskService 接线一致

### v0.0.268311-R3（一键安装闭环修复轮 · 9 项）

> 发布日期：2026-08-31
> 说明：审计「一键安装」全链路（Windows 下单 → WinPE 续装 → 无人值守全自动完成）发现的 9 项闭环问题进行修复，不提升正式版本号。

#### 修复
- r1 镜像下载/校验/备份真实化：WinPE 端由模拟改为真实实现——镜像经客户端接口 GET /api/v1/images/:id/clientDownload 断点续传下载、SHA256 与后端 file_hash 比对校验、原系统盘用户数据 robocopy 备份；修复 ImageService.verifyHash 列名错误（sha256 → file_hash）
- r2 client_id 断链：后端拒绝无 client 的 waiting 任务；Windows 端新增 EnsureRegistered 保障下单前必完成客户端注册
- r3 重试状态断链：任务重试状态 failed/cancelled → waiting（原为 pending，PE 端无法按 client_id 拉取续装）
- r4 认领无保护：进度上报增加任务认领乐观锁（条件更新防并发重复认领）+ client_id 身份校验（A 客户端不能操控 B 任务）
- r5 Windows「立即重启」真实重启：shutdown /r + 引导热键提示弹窗（原为仅提示不真正重启）
- r6 Windows CheckAuth 注册类型 winpe → windows
- r7 删除 WinPE MainViewModel 旧 StartInstall 死代码及冗余依赖（DiskPartService 引用、SelectedImage/SelectedDisk 等属性）
- r8 磁盘序号跨环境兜底：Windows 下单 options 持久化 disk_index/disk_size/disk_model，PE 端按磁盘物理特征模糊匹配（宿主机与 PE 环境磁盘序号可能不一致）
- r9 失败重试复用原任务：双端新增 RetryTaskAsync 复用原任务 id（retry API），不再新建任务，续装沿用原订单

#### 新增
- 后端镜像客户端下载接口 GET /api/v1/images/:id/clientDownload（流式 + Range 断点续传，复用客户端 Token 鉴权）

#### 验证
- e2e_winpe_test.php 扩展闭环回归用例至 36 项全部通过（waiting 无 client 拦截、越权上报拒绝、失败/取消重试复用原任务、options 磁盘特征持久化、认领并发、正确身份认领等）
- 双端 Release 编译 0 错误 0 警告

### v0.0.268311-R4（U盘启动制作完善轮 · 本地写盘 + 生成 ISO + 在线 PE 拉取）

> 发布日期：2026-08-31
> 说明：U 盘制作新增「输出方式」双模式（写 U 盘 / 生成 ISO 镜像），新增在线 URL 直接拉取 PE，并实现纯 C# 无第三方依赖的可引导 ISO 生成器（ISO9660 + Joliet + El Torito，UEFI/Legacy 双引导），不提升正式版本号。

#### 新增
- U 盘制作输出方式切换（Windows / WinPE 双端 UDiskWindow + UDiskViewModel）：①「写入 U 盘」沿用原四步向导；②「生成 ISO 镜像」模式——无需选中 U 盘、无需管理员权限、无写盘锁，仅解包所选 PE 并打包成可启动 ISO
- 自定义可引导 ISO 生成器 IsoBuilder.cs（Windows / WinPE 双端）：纯 C# 实现 ISO9660 + Joliet（UCS-2）+ El Torito 引导记录，单引导记录 + 双段引导目录（Legacy + EFI，对齐 oscdimg 规范），移除 DiscUtils 第三方依赖
- EfiSysGenerator.cs：当 PE 缺少原生 efisys.bin 时，用 bootx64.efi 自动生成 FAT12 格式 efisys.bin（1.44MB 软盘镜像）兜底，保证 UEFI 引导兼容
- 在线 PE 来源（URL 源）：UDiskService.DownloadPeUrlAsync 从任意 HTTP(S) URL 直接拉取 PE（不依赖服务器托管、无需预设哈希），缓存至 C:\ZS_Cache\pe\，支持断点续传 + 进度显示
- ISO 构建流程（UDiskService.BuildIsoAsync）：解包 PE（挂载 ISO/目录）→ 按需写入客户端 ZS_Client + 内置工具 ZS_Tools → 检测/兜底生成引导镜像 → IsoBuilder 打包
- IsoBuildPlan 模型（UDiskModels.cs）：PE 源路径 / 输出路径 / 卷标 / 是否写入客户端与工具
- 测试脚手架 _iso_logic_test/：FAT12 读取器 + ISO9660 读取器，覆盖 efisys.bin 生成、ISO 结构、在线 PE 端到端

#### 修复
- ISO 挂载失败：MountIsoAsync 改用 PowerShell -EncodedCommand（UTF-16LE Base64）规避引号转义，挂载后通过 Win32_LogicalDisk(DriveType=5) 前后对比精确取新盘符，避免误取系统盘/物理光驱
- FAT12 efisys.bin 簇链错误：修正 SetFat 奇偶簇位偏移计算（奇簇上 4 位在 base+1 高半字节、高字节在 base+2）
- 引导目录兼容性：由多引导记录改为单引导记录 + 双段目录（Validation + Legacy 条目 + EFI 段头 + EFI 条目），修复「引导目录数量=1」校验
- PVD PathTableSize 误用扇区对齐值：改为路径表实际字节数
- CopyDirectoryAsync 裸盘符（如 "E"）未规范化导致 "找不到路径 E"：统一补全为 "E:\"

#### 验证
- 双端（Windows/WinPE）Release 编译 0 错误 0 警告
- _iso_logic_test 逻辑自测：FAT12 efisys.bin（引导扇区 FAT12/512B/2880扇区/media=0xF0、EFI→cluster2、BOOT→cluster3、BOOTX64.EFI→cluster4、文件内容哈希一致、FAT 链 0xFFF 正确）+ ISO 结构（PVD CD001、Joliet SVD、BootRecord、引导目录校验和、双引导条目指向真实数据、L 路径表、目录记录、1MB+123 文件数据完整性、中文 README 内容校验）+ 在线 PE 端到端
- 在线 PE 实测：从 GitHub（WindowsPEBasic）拉取 ~340MB PE → 生成 ~337MB 可引导 ISO，PVD/BootRecord/引导目录校验和/路径表/Joliet/根目录解析全部通过

### v0.0.268311-R5（用户注册/登录体系 + 后台菜单精简 + 客户端炫彩动效轮）

> 发布日期：2026-08-31
> 说明：客户端登录由「设备自注册」改为「用户注册/登录」体系（测试账号 wuxx80/a111111）；移除后台无意义的客户端管理/分组/版本管理、任务管理、业务管理菜单（仅删前端页面，保留底层任务流程机制）；客户端首页品牌信息对接后台设置；Windows/WinPE 双端新增炫彩动态背景效果。不提升正式版本号。

#### 新增
- **用户注册/登录体系**：后端 AuthController 新增 register 用户注册接口（公开路由 POST /api/v1/auth/register），注册成功即自动登录，角色固定「普通用户」（code=user），不可注册为管理员；install.sql 新增普通用户角色（id=4）+ 测试账号 wuxx80 / a111111
- Windows 客户端新增 LoginWindow（登录/注册对话框）+ SessionStore 会话持久化（本地保存 token + 用户名，重启免重复登录），不再自动 admin 登录；核心功能（一键装机/U盘制作/绿色软件）需登录后使用
- 客户端首页品牌信息：左上角品牌（ZS Logo + 主标题 + 副标题 + 标语）、边框项（设置/登录·退出/版权/版本/联系/关于）；新增公开接口 GET /api/v1/site/info（api/SiteController，读取 settings 表 group=site 配置）
- 管理后台系统设置新增「站点信息」组：Logo 文字/主标题/副标题/标语/官网/版本号/版权/联系/关于 均可后台编辑
- Windows 客户端新增窗口：SettingsWindow/AboutWindow/ContactWindow/VersionWindow/SoftwareWindow；WinPE 端新增 AboutWindow/ContactWindow/SettingsWindow/VersionWindow
- **客户端炫彩动态效果**（Windows/WinPE 双端）：动态流动渐变背景 + 5 个彩色漂浮光斑（正弦往返漂移）+ 星尘粒子层（闪烁 + 漂移）+ 入口按钮呼吸光晕 + 悬停缩放 + 中央内容入场动画（淡入 + 上浮）

#### 修改
- 客户端登录流程：Windows 端由「自动 admin 登录」改为「用户注册/登录」（登出清空会话；SessionStore 免重复登录）；WinPE 端保留 admin/admin123 自动登录（无人值守 PE 环境无法手动输入账号）
- 客户端设备自注册（clients/register）保留为任务执行凭证：register 自动 approved（无需后台人工审批），移除 TaskController 审批门槛，清理「待审核」提示
- **后台菜单精简**：移除「客户端管理/客户端分组/版本管理」「任务管理（任务列表/任务模板）」「业务管理（客户/工单）」菜单入口与页面（clients/client-groups/client-versions/tasks/task-templates/customers/work-orders 页面删除，底层任务流程机制——进度上报/认领/重试/无人值守——保留）
- 仪表盘统计口径改为「待执行任务」

#### 修复
- 镜像列表 JSON 反序列化错误：Image 模型 file_size 返回整数 + 新增 size_display（Int64 反序列化错误）；镜像 version 字段类型 string → int（双端模型对齐）
- 首页品牌信息对接后台设置（settings.html 新增站点信息组）

#### 验证
- 用户注册/登录接口验证通过（register 自动登录签发 token；重复用户名拦截；密码长度/用户名规则校验）
- 测试账号 wuxx80 / a111111 登录验证通过
- 客户端动效验证：像素差分检测 26.83% 帧间变化，确认背景/光斑/粒子动画持续运行
- 双端编译 0 错误 0 警告

### v1.7.0（开发中间版本 · WinPE/Windows 客户端完成）

> 发布日期：2026-08-30

- WinPE 客户端核心功能：一键装机引擎、DiskPart 分区、引导修复、驱动注入、进度同步
- Windows 客户端主界面框架（四大入口）+ 登录/登出 + 服务器连接管理
- 解决方案文件 ZS_Installer.sln
- DeviceController 磁盘信息接口、TaskController 进度更新接口
- 客户端构建验证：WinPE 0 错误 2 警告 / Windows 0 错误 0 警告

### v2.1.0（开发初始版本 · 后台与后端完成）

> 发布日期：2026-08-30

- 项目初始化 + 设计文档 + 数据库 35 张表 + GitHub 仓库
- 后端骨架：ThinkPHP 6.1.4、36 个状态码、认证/CORS/日志中间件、异常处理器
- 管理后台：登录页 + 主布局 + 29 个功能页面 + 仪表盘
- API：30 个接口浏览器自动化测试验证
- 修复：Hash 不存在、字段名错误、路由冲突、BOM 编码等

---

## 版本号规则

本项目使用 MAJOR.MINOR.PATCH 版本号规则：
- MAJOR：重大架构变化或不兼容更新
- MINOR：新增功能
- PATCH：Bug 修复、小型修改和兼容性修复

初始版本 v0.0.268311 中，PATCH 位 268311 为本项目首发的版本标识（对应首发日期 2026-08-31）。