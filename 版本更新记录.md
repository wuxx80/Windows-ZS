# ZS 装机助手 · 版本更新记录

> 项目：Windows-ZS
> 起始版本：v2.1.0

---

## v1.7.0（当前版本）

> 发布日期：2026-08-30
> 当前状态：WinPE 客户端 + Windows 客户端开发完成

### 新增
- WinPE 客户端项目（.NET 8 WPF）：完整项目结构、网络通信层、设备信息采集、镜像选择界面
- WinPE 客户端核心功能：一键装机引擎（WIM释放/ISO挂载）、DiskPart分区工具、引导修复（UEFI+BIOS）、驱动注入、进度反馈与状态同步
- Windows 客户端项目（.NET 8 WPF）：主界面框架（四大入口：一键装机/U盘制作/工具大全/绿色软件）、网络通信层
- Windows 客户端用户登录/登出、服务器连接管理
- 解决方案文件（ZS_Installer.sln）包含 WinPE_Client 和 Windows_Client 两个项目

### 构建验证
- WinPE 客户端：构建成功（0 错误，2 警告）
- Windows 客户端：构建成功（0 错误，0 警告）
- 两个客户端均使用 .NET 8.0-windows 框架，支持 WPF

### 技术栈
- 客户端：C# WPF (.NET 8) + System.Management (WMI) + System.Text.Json
- 客户端架构：MVVM 模式（Model-View-ViewModel）

---

## v2.1.0

> 发布日期：2026-08-30
> 当前状态：管理后台前端开发完成，29个页面均已浏览器验证

### 新增
- 完整项目初始化，创建项目目录结构
- 五件套文档创建（README.md / 项目理解报告.md / 项目结构.txt / 操作指南.md / 版本更新记录.md）
- 详细设计文档：系统架构、数据库设计、API 接口设计、项目目录结构
- Web 后台详细设计：21 章完整设计
- 一键装机交互设计：18 章完整设计
- 镜像管理模块深度设计：12 小节完整功能
- 后台全局完善：统一错误码（36 个）、WebSocket、消息通知、备份恢复、任务调度
- GitHub 仓库初始化并与远程同步

### 后端搭建
- 使用 Composer 创建 ThinkPHP 6.1.4 项目
- 配置 .env 环境适配宝塔面板（MySQL 5.7 + PHP 8.3）
- 安装 Composer 依赖（think-orm 等）
- 数据库导入：35 张表完整建表 SQL + 种子数据
- 创建日志系统（config/log.php）
- 创建缓存系统（config/cache.php）
- 基础控制器（BaseController + admin子类）
- 36 个业务状态码（config/status.php）
- RESTful API 路由定义（route/admin.php）
- 认证中间件（AuthMiddleware + Token 验证）
- CORS 中间件（跨域支持）
- 日志中间件（操作日志记录）
- 异常处理器（统一异常处理）
- 8 个 Service 骨架类
- 36 个 Model 骨架类
- 30 个 Controller 骨架类
- 创建管理员账号（admin / admin123）

### 管理后台前端
- 首页（index.html）介绍下载网站搭建
- 后台登录页（admin/login.html）开发
- 后台主布局（admin/index.html）—— 深色侧边栏 + 顶部栏 + iframe 内容区
- 控制台仪表盘（dashboard.html）—— 统计卡片 + 趋势图 + 最近任务
- 29 个功能管理页面完整开发
- 通用工具库（assets/js/admin-common.js）—— API 封装、Toast、分页、弹窗等
- 统一后台样式（assets/css/admin.css）—— 搜索栏、数据表格、表单、分页、按钮等

### API 已验证
- POST /api/v1/auth/login → 登录成功，返回 token
- GET /api/v1/auth/profile → 用户信息（需认证）
- POST /api/v1/auth/logout → 退出登录
- PUT /api/v1/auth/password → 修改密码
- GET /api/v1/dashboard → 仪表盘统计数据
- 全部 30 个 API 接口通过浏览器自动化测试验证

### 修复
- 修复 think\facade\Hash 在 ThinkPHP 6 中不存在 → 改用原生 password_hash/password_verify
- 修复 think\Request::id() 方法不存在 → 移除冗余字段调用
- 修复 IndexController 查询不存在的 type 列 → 修正查询条件
- 修复文件 BOM 编码问题 → 统一使用 UTF-8 无 BOM 编码
- 修复日志配置缺失 → 新增 config/log.php
- 修复 admin 密码哈希被截断 → 使用 PHP 脚本创建用户
- 修复多个控制器的字段名错误（sort→sort_order, user_id→recipient_id 等）
- 修复路由冲突（重复的 softwareCategories 路由定义）
- 修复 dashboard.html 编码问题（BOM 导致浏览器解析错误）

### 变更
- 恢复宝塔面板 Nginx vhost 配置文件至原始状态
- 删除宝塔面板 rewrite 目录下的 thinkphp.conf 文件
- 创建独立伪静态规则文件 server/thinkphp.nginx.rewrite.conf（供手动配置）
- 清理 public 目录中的测试/临时文件（15+ 个文件）
- 更新 .gitignore 配置（增加 runtime/、.env 等忽略规则）

### 技术栈
- 后端：PHP 8.0+ + ThinkPHP 6.1.4 + MySQL 5.7+ + Nginx
- 后台前端：原生 HTML/CSS/JS（Layui 2.9+ 风格）
- 客户端：C# WPF (.NET 8)

### 已知问题
- 客户端代码尚未开发（WinPE + Windows）
- 部分后端控制器尚未实现完整业务逻辑（骨架代码）
- 浏览器访问需手动配置伪静态规则
- 镜像上传/下载等大文件操作尚未实现
- WebSocket 实时通信尚未实现

---

## 版本号规则

本项目使用 MAJOR.MINOR.PATCH 版本号规则：
- MAJOR：重大架构变化或不兼容更新
- MINOR：新增功能
- PATCH：Bug 修复、小型修改和兼容性修复