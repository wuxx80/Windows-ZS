# ZS 装机助手 · 详细开发计划表

> 版本：v0.0.268311-R7（2026-09-02）
> 项目路径：D:\Users\Desktop\Windows-ZS
> 状态：R7 架构切换 + R7-C 契约对齐子轮 + **R7-D P1+P2 全链路代码层闭环**完成（Startnet.cmd ↔ WinPE_Agent 主链路参数对齐 + task.ini/zs_manifest.key 解析校验落地 + 客户端模块调整清单整理 + Windows 端 8 个新 Service + PE 端 4 个新 Service 落地 + Program.cs 八阶段全打通 + InstallWizardViewModel.ExecuteR7OfflineBuild 一键触发 + 编译 0 错 0 警）。Phase 0 基建 6 项债务未修；Phase 1 boot.wim 打包脚本已落地（pe_assets/Startnet.cmd + make_boot_wim.ps1 + upload_pe.ps1，待真机验证）；**Phase 2 (P1+P2) R7-D 已完成编码（待真机回归）**；Phase 3 (P3) / Phase 4 (P4) 待实现；Phase 5 / Phase 6 待编码。
> 版本策略：当前用户未批准升级版本号，所有修改维持 v0.0.268311。
> 后续任务清单：详见 docs/superpowers/specs/2026-09-01-客户端模块调整清单.md（A. Windows_Client 缺 11 个 Service / B. WinPE_Client 移除 11 类 GUI + 缺 4 个 Service / C. 后端 API 缺 3 端点 / D. Shared 类库抽离建议）；**R7-D 已完成 A 中 8 个 + B 中 4 个共 12 个 Service**；P1~P4 优先级已排序。

---

## 目录

1. 项目概述
2. 技术架构图
3. 目录结构（同项目结构.txt）
4. 开发阶段总览
5. Phase 0 基建修复（12h · P0 · 进行中）
6. Phase 1 boot.wim / boot.sdi 打包流水线（20h · P0）
7. Phase 2 Windows 端 P1 下载 + P2 BCD 注入（32h · P0）
8. Phase 3 PE 端 P3 Startnet.cmd 逃生窗 + 手动装机菜单（16h · P1）
9. Phase 4 PE 端 P4 ZS_Agent 八阶段流水线（40h · P0）
10. Phase 5 新系统 P5 SetupComplete.cmd + 自清理（16h · P1）
11. Phase 6 联调回归 + 审计官 + 发布（12h · P0）
12. 总工期与里程碑
13. 审计官配置（3 维度 × 18 检查项）
14. 风险与注意事项（14 项 + 缓解）

---

## 1. 项目概述

### 1.1 目标
交付一套 **100% 可落地**的 Windows 无人值守装机解决方案：用户在正常 Windows 里只点一次「一键装机」→ 自动 20~40 分钟 → 停在新系统桌面 + 装好 task.ini 内所有软件 + 已应用优化。**任何阶段 PE 不能依赖网络**。

### 1.2 技术栈（已固化，不再改）
| 层 | 技术 | 版本 |
|----|------|------|
| 后端语言 | PHP | 8.0+ |
| 后端框架 | ThinkPHP 6 | 6.1.4 |
| 数据库 | MySQL | 5.7+ |
| Web 服务器 | Nginx（宝塔面板） | 1.20+ |
| 后台前端 | Layui 2.9+ 风格 | 原生 HTML/CSS/JS |
| Windows 客户端 | C# WPF | .NET 8 |
| WinPE 客户端（旧） | C# WPF | .NET 8（Phase 3 评估降级） |
| WinPE Agent（新 PE 核心） | .NET 8 AOT 控制台 | 单文件 / 无运行时 |
| 部署 | 宝塔面板 + server/thinkphp.nginx.rewrite.conf 独立伪静态 | — |

### 1.3 交付验收标准
- 真实硬件一台（Win11 OEM、UEFI+GPT、没装任何工具）：Windows_Client → 登录 → 选 Win11 专业版 → 点「一键装机」→ 确认 → 之后不再按键盘 → 停在新桌面 + 软件已装 + 优化已应用
- 三重逃生窗各可被真实触发：P2 shutdown /a 成功回到原系统；P3 10s 内按键进入手动菜单；P4 中途外部命令失败立即停机
- 固件判定（registry+diskpart）6 级冲突策略全部有分支；分区脚本尾端验证（ESP+FAT/Active）全部可检出失败
- 审计官 18 检查项 100% 通过

---

## 2. 技术架构图（R7）

```
                    ┌───────────────────────────────────┐
                    │   管理后台 (Layui · 19 菜单)       │
                    │   控制台 / 镜像×3 / 无人值守 /    │
                    │   软件×3 / 驱动 / 脚本 / PE×2 /   │
                    │   用户(⚠角色断) / 设置 / 日志(⚠空)│
                    │   通知 / 定时 / 报表 / Webhook / 回收站│
                    └──────────────┬────────────────────┘
                                   │ HTTP API (RESTful JSON)
                    ┌──────────────▼────────────────────┐
                    │  PHP 后端 (ThinkPHP 6)             │
                    │  控制器(⚠缺Role) · 服务层8 ·      │
                    │  中间件3(⚠缺Auth/Log注册) ·      │
                    │  异常JSON脱敏 · JWT认证            │
                    └──────────────┬────────────────────┘
                                   │ ORM / PDO
                    ┌──────────────▼────────────────────┐
                    │  MySQL 5.7 · 35 表                 │
                    │  角色/用户 · 镜像×5 · 客户端×3 ·  │
                    │  任务×3 · 无人值守 · 软件×3 · 驱动 │
                    │  PE×2 · PXE(⚠Phase3删) · 网络(⚠)  │
                    │  客户(⚠) · 工单(⚠) · 设置/日志/定时│
                    │  通知/回收/Webhook · 下载×3        │
                    └─────┬────────────────────────────┬┘
                          │ P1 下载资源                 │ 心跳/建档
                          ▼                              ▼
         ┌─────────────────────────────┐     ┌──────────────────────────────┐
         │ Windows 客户端 (C# WPF)     │     │ WinPE 端（PE 永远无网）       │
         │ P1 下到 ZS_Task 10项资源   │     │ P3 Startnet.cmd 10s 逃生窗   │
         │ P2 BCD bootsequence 注入   │     │ P4 ZS_Agent 八阶段流水线     │
         │     30s倒计时→重启          │     │   固件双判/分区尾端验证/      │
         └─────────────┬───────────────┘     │   WIM/驱动/引导/Unattend/Setup│
                       │ BCD一次性启动         └───────────────┬──────────────┘
                       ▼ 重启（ramdisk加载boot.wim）         │ 重启（新系统）
         ┌─────────────────────────────────────────┐        ▼
         │ 硬盘 boot.wim (ADK预打包 · Startnet.cmd)  │   ┌─────────────────────┐
         └─────────────────────────────────────────┘   │ P5 SetupComplete.cmd │
                                                         │ 装软件·优化·可选删除 │
                                                         │ ZS_Task → 桌面        │
                                                         └─────────────────────┘
```

---

## 3. 目录结构
完全对齐《项目结构.txt》，此处不重复（避免六件套内部漂移）。

---

## 4. 开发阶段总览（R7 口径，总工时 ≈ 148h）

| 阶段 | 名称 | 优先级 | 状态 | 预估工时 | 备注 |
|------|------|--------|------|----------|------|
| Phase 0 | 基建修复（6 项 P0 债务） | P0 | ⏳ 进行中 | 12h | 修完才能开始 Phase 1~6 |
| Phase 1 | boot.wim / boot.sdi 打包流水线（开发机/服务器 ADK） | P0 | ✅ 编码完成·待真机验证 | 20h | make_boot_wim.ps1 + upload_pe.ps1 + pe_assets/Startnet.cmd 已落地 |
| Phase 2 | Windows 端 P1 资源收集 + P2 BCD 一次性启动 | P0 | ✅ R7-D 编码完成·待真机回归 | 32h | ZsTaskBuilder + BcdInjector + 6 Downloader + TaskIniWriter + ManifestWriter + ExecuteR7OfflineBuild；编译 0 错 0 警 |
| Phase 3 | PE 端 P3 Startnet.cmd 10s 逃生窗 + 手动装机菜单 | P1 | ⏳ 待实现 | 16h | Startnet.cmd 文本已落地，手动菜单 + boot.wim 打包待编码 |
| Phase 4 | PE 端 P4 ZS_Agent 八阶段流水线（核心装机引擎） | P0 | ⏳ 待实现 | 40h | R7-D 已完成 4 个 PE Service + Program.cs 集成（约 34h 工作量），单元测试 + 真机回归（约 6h）待编码 |
| Phase 5 | 新系统 P5 SetupComplete.cmd + 自清理 | P1 | ⏸ 未开始 | 16h | R7-D 已完成 SetupCompleteBuilder/UnattendXmlBuilder 生成器，真机联调待 Phase 5 |
| Phase 6 | 联调回归 + 审计官 + 发布打包 | P0 | ⏸ 未开始 | 12h | 最终交付 |

**总工时：12 + 20 + 32 + 16 + 40 + 16 + 12 = 148h**

> **R7-D 子轮完成范围（2026-09-02）**：
> - **Phase 2 (P1+P2)**：100% 编码完成（32h 全部完成）—— Windows 端 8 个新 Service + 2 个修改（ApiService.GetBaseUrl / InstallWizardViewModel.ExecuteR7OfflineBuild）
> - **Phase 4 (P4)**：约 85% 编码完成（34h/40h）—— 4 个 PE 端 Service + Program.cs 集成（① 固件判定 ② 分区+尾端验证 ③ WIM 应用 ④ 驱动注入 ⑤ 引导修复双路径 ⑥ Unattend ⑦ SetupComplete ⑧ 重启）；剩余 6h 单元测试（分区尾端验证模拟器 + 固件 6 级冲突模拟器）待编码
> - **Phase 5 (P5)**：约 25% 编码完成（4h/16h）—— SetupCompleteBuilder 模板渲染 + UnattendXmlBuilder 已落地；剩余 12h 真机联调 + 自清理逻辑 + 桌面提示待编码
> - 仍待：boot.wim 真机哈希白名单（Phase 1 末尾）+ 真机回归（Phase 6）+ 审计官 18 项（Phase 6）

---

## 5. Phase 0 · 基建修复（P0 · 12h · 必须先做）

### 5.1 任务分解
| # | 子任务 | 详情 | 优先级 | 工时 | 状态 | 依赖 |
|---|--------|------|--------|------|------|------|
| 0-1 | 补齐 RoleController + Role 路由 | 新建 server/app/controller/admin/RoleController.php（list/create/update/delete + 分页 + 按 role code 查）；在 route/admin.php 增加 Role 资源路由 | P0 | 2h | ⏸ | 无 |
| 0-2 | role 契约前后端对齐 | users.html：提交字段 `role`→`role_id`（整数）；用户新增 / 编辑页面角色下拉显示名、提交 id；UserController.php 校验 role_id 为正整数且存在 | P0 | 2h | ⏸ | 0-1 |
| 0-3 | WinPE_Agent 入编 | slnx 加项目引用；csproj nullable warnings 修复；publish.ps1 增加 WinPE_Agent 发布目标（AOT）；git add WinPE_Agent | P0 | 1h | ⏸ | 无 |
| 0-4 | 注册 AuthMiddleware + LogMiddleware | config/middleware.php：路由级挂 admin 组除 login/register/site/info 外全挂 AuthMiddleware；全局挂 LogMiddleware（password/token/密钥脱敏）；LogMiddleware 写 zs_logs 表 | P0 | 2h | ⏸ | 无 |
| 0-5 | 测试脚手架整理 | ① 选择性恢复 R2/R4/R6 的 5 个测试脚手架（推荐恢复 _iso_logic_test + _offline_logic_test 两个逻辑测试），或 ② 正式 git rm 提交删除标记；六件套文档更新对应声明 | P0 | 2h | ⏸ | 无 |
| 0-6 | Git 全量整理 + Commit/Push | 现状：28 改 / 8 删 / 6 未跟踪；按 R7 文档声明全部 commit（docs、脚本、删除、新增分 4~6 个规范化 commit，feat/fix/docs/chore 前缀）；Push 到远程 | P0 | 3h | ⏸ | 0-1~0-5 |

**Phase 0 里程碑：新 clone 仓库 → 读六件套 + 编译（双端 + WinPE_Agent）→ 跑 admin / login / users / logs 四个页面 → 全部功能真实可用。**

---

## 6. Phase 1 · boot.wim / boot.sdi 打包流水线（20h · P0）

目标：开发机/服务器运维一键可复现哈希地生成 boot.wim（内置 Startnet.cmd 逃生窗）和 boot.sdi，作为二进制资产上传到后端 pe_versions。

| 子任务 | 工时 | 交付物 |
|--------|------|--------|
| Windows ADK + WinPE Addon 安装脚本（开发机/服务器一次性） | 2h | docs/superpowers/specs/adk-setup.md + PowerShell 脚本 |
| copype + Dism 注入 Startnet.cmd / ZS_Agent.exe / 中文字体包 / VC++ 运行时 / 网卡/存储通用驱动 | 8h | make_boot_wim.ps1（可重入、失败自动清理） |
| boot.sdi 来源校验（来自 ADK，SHA-256 白名单） | 2h | manifest 模板 |
| 产物可复现哈希：同源码输入 → 同 SHA-256 输出 | 4h | 时间戳 / 随机种子固定化 |
| 一键上传到服务器 pe_versions 接口 | 4h | upload_pe.ps1 + PeVersionController 上传联调 |

---

## 7. Phase 2 · Windows 端 P1 资源收集 + P2 BCD 注入（32h · P0）

| 子任务 | 工时 | 说明 |
|--------|------|------|
| 选盘策略（跳过 C 盘，NTFS + 可用空间 ≥ 镜像×2.5，选最大） | 4h | UI 六步向导第③步改造；中文提示「请释放至少 XX GB」 |
| 断点续传批量下载器：boot.wim/boot.sdi/system.esd/驱动包/软件包/Autounattend/SetupComplete/ZS_Agent.exe | 12h | 复用 clientDownload 流式 + Range；失败重试 3 次；暂停 / 取消 / 回滚 ZS_Task 整目录删除 |
| task.ini 16 字段生成器（UTF-8 无 BOM） | 2h | 与设计文档 §2.1 契约一致；双端（Windows 生成 / PE 读取）单元测试 20 项 |
| zs_manifest.key SHA-256 清单生成 | 3h | 流式读取大文件分块计算；含相对路径 + 大小；PE 端可逐项验证 |
| README_请勿删除.txt + ZS_Task 目录结构 10 项 | 1h | — |
| P2-1 boot.wim + boot.sdi 写入 ESP / 系统保留分区 | 3h | 管理员提权检测；失败回滚；BCD 模板从现有启动项复制 |
| P2-2 bcdedit.exe 创建 ramdisk 启动项 + bootsequence 一次性 | 4h | 验证 ramdisk 存在 / device/osdevice ramdisk 指向 ZS_Task\boot.wim / sdi 路径 |
| P2-3 30s 倒计时重启 + shutdown /a 取消 + 状态闭环 | 3h | UI 三层联动；取消后 bootsequence 清除；ZS_Task 可选保留或删除 |

> **R7-D 子轮完成情况（2026-09-02）**：本 Phase 8 项子任务全部编码完成（32h/32h）。
> - 选盘 + UI：InstallWizardViewModel 六步向导接入 `ExecuteR7OfflineBuild`
> - 4 个 Downloader 落地：`PeAssetDownloader`（boot.wim+boot.sdi+ZS_PE_Agent.exe）/ `SystemImagePreDownloader` / `DriverPackageDownloader`（zip 解压）/ `SoftwarePackageDownloader`（按 key 批量）
> - `TaskIniWriter`：7 section / 16 字段 UTF-8 无 BOM
> - `ManifestWriter`：`sha256=HASH=path` 清单
> - `BcdInjector`：备份 + `/copy {current}` + ramdisk + bootsequence + Rollback 回退
> - 30s 自动重启：`shutdown /r /t 30`
> - `ZsTaskBuilder`：编排全流程 + V1 校验 + 失败回退删 ZS_Task
> - **仍待**：真机回归 + boot.wim 哈希白名单验证 + UI 三层联动 polish

---

## 8. Phase 3 · PE 端 P3 Startnet.cmd + 手动装机菜单（16h · P1）

| 子任务 | 工时 | 说明 |
|--------|------|------|
| Startnet.cmd：wpeinit → 所有盘符遍历找 \ZS_Task\task.ini → 最近写入时间选择 | 3h | 多 ZS_Task 冲突策略 |
| choice.exe 10s 倒计时逃生窗 + 双按键分流（超时自动 / 按键手动） | 3h | 屏幕提示黄色大字；日志追加 |
| 找不到 ZS_Task → 自动进手动菜单 | 2h | 原因 3 条中文提示 |
| 手动装机控制台菜单：①选镜像 ②目标磁盘 ③分区方案 ④功能开关 ⑤开始装机 ⑥退出到 CMD | 6h | 与 ZS_Agent 管线复用同一参数模型；开始前红色警告二次确认 |
| Startnet.cmd 打包进 boot.wim（Phase 1 make_boot_wim.ps1 注入入口） | 2h | — |

---

## 9. Phase 4 · PE 端 P4 ZS_Agent 八阶段流水线（40h · P0，核心）

| 阶段 | 内容 | 工时 |
|------|------|------|
| 总装入口 + 参数解析 + zs_manifest.key 全量校验 |  | 4h |
| ① 固件判定（registry 优先 / diskpart 回退 / 6 级冲突处理策略） |  | 6h |
| ② 分区（GPT / MBR 分支 / 自定义 / 保留 三模式）+ 尾端验证（ESP 卷标 + FAT/FAT32 / Active） |  | 10h |
| ③ WIM / ESD 应用（dism /Apply-Image）+ 百分比进度实时回传 |  | 4h |
| ④ 驱动注入（dism /Add-Driver /Recurse /ForceUnsigned）+ 失败驱动跳过 + 日志 |  | 3h |
| ⑤ 引导修复（bcdboot 自动选固件）+ 失败回退到 bootsect /nt60 |  | 3h |
| ⑥ Autounattend.xml 写入 C:\Windows\Panther\；⑦ SetupComplete.cmd 写入 C:\Windows\Setup\Scripts\ |  | 3h |
| ⑧ 重启（wpeutil reboot）+ 失败停 CMD |  | 1h |
| 单元测试（分区尾端验证模拟器 / 固件 6 级冲突模拟器） |  | 6h |

> **R7-D 子轮完成情况（2026-09-02）**：本 Phase 编码约 85% 完成（34h/40h，仅缺单元测试 6h）。
> - 总装入口 + 参数解析 + manifest 校验：R7-C 已落地（Program.cs RunAutoMode + TaskIniParser + ManifestValidator）
> - ① 固件判定：`FirmwareDetector.cs`（注册表主判定 + diskpart 回退 + 6 级冲突 + force_gpt/force_mbr override）
> - ② 分区 + 尾端验证：`PartitionVerifier.cs`（GPT ESP+FAT32 / MBR Active）+ Program.cs `BuildPartitionScript`
> - ③ WIM/ESD 应用 + ④ 驱动注入 + ⑧ 重启：ExecutePipeline 已集成
> - ⑤ 引导修复双路径：bcdboot 主路径 + 失败回退
> - ⑥ `UnattendXmlBuilder.cs`（auto 生成 Unattend.xml / manual 返回 null）写 C:\Windows\Panther\
> - ⑦ `SetupCompleteBuilder.cs` 写 C:\Windows\Setup\Scripts\
> - DiskPartService 新增 `ExecuteRawScriptAsync`
> - 4 个 PE 端 Service 通过 csproj Compile Link 让 WinPE_Agent 复用 WinPE_Client 源码
> - **仍待**：单元测试 6h（分区尾端验证模拟器 / 固件 6 级冲突模拟器）+ 真机回归

---

## 10. Phase 5 · P5 SetupComplete.cmd + 自清理（16h · P1）

| 子任务 | 工时 |
|--------|------|
| SetupComplete.cmd 模板渲染器：按软件模板 + 优化清单 + 自清理开关渲染真实 cmd | 4h |
| 静默安装每个软件按顺序执行，失败记录日志不中断 | 3h |
| 系统优化（注册表组策略 / 服务关闭 / 计划任务） | 3h |
| 可配置是否删除 ZS_Task 目录（默认保留；开关 first_boot_cleanup_zs_task） | 2h |
| 完成后重启；重入保护（标记文件避免二次执行） | 2h |
| 新系统首次登录后桌面展示「装机完成」小提示 | 2h |

> **R7-D 子轮完成情况（2026-09-02）**：本 Phase 模板渲染器子任务已编码完成（4h/16h）。
> - `SetupCompleteBuilder.cs` 已实现：软件静默安装 + 系统优化 + 自清理 + 自毁开关的模板渲染
> - `UnattendXmlBuilder.cs` 已实现：auto 模式生成 Unattend.xml（OOBE 跳过 + 本地管理员自动登录）/ manual 返回 null
> - **仍待**：真机首次启动联调（10h）+ 自清理逻辑真机验证 + 重入保护 + 桌面小提示

---

## 11. Phase 6 · 联调回归 + 审计官 + 发布（12h · P0）

| 子任务 | 工时 |
|--------|------|
| VMware 真机组装：UEFI/GPT + BIOS/MBR 两种模式，R7 主链路全流程跑通（P1~P5）各 3 次 | 5h |
| 三重逃生窗实测 6 组：shutdown /a / P3 按键 / P4 dism 失败各 2 次 | 2h |
| 审计官 3×18 = 54 项检查执行；出审计报告存 docs/superpowers/audit/202609xx-r7-验收.md | 3h |
| 发布脚本回归；制作 zs_server_deploy.zip + WinPE_Agent.exe + Windows_Client.exe 三个最终产物 | 2h |

---

## 12. 里程碑与总工期

| 里程碑 | 完成标志 | 预计完成（相对 Phase 0 启动日） |
|--------|----------|----------|
| M0 基建修复完成 | Phase 0 六项 100%；新 clone 用户管理 + 操作日志真实可用；Git 干净 | Day 2 |
| M1 boot.wim 可复现 | Phase 1 完成；两个 boot 产物哈希白名单通过 | Day 5 |
| M2 前半段 P1+P2 真实跑通 | 下单 → ZS_Task 10 项齐全 → 写入 BCD → 重启真实进 PE | Day 9 |
| M3 逃生窗 + 手动菜单 | Phase 3 完成；3 次真实按键进入手动菜单成功 | Day 11 |
| M4 核心八阶段可交付 | Phase 4 完成；VMware UEFI/MBR 各 3 次真实从空盘到桌面 | Day 16 |
| M5 首次启动自动化 | Phase 5 完成；SetupComplete 装 5 个常用软件 / 应用优化 10 项 | Day 18 |
| M6 交付验收 | Phase 6 完成；审计官 54 项全过；发布包可上传 | Day 20 |

**总工期 ≈ 148 工时（≈ 20 人天）**

> **R7-D 后当前里程碑状态（2026-09-02）**：
> - M0 基建修复：⏳ 进行中（6 项 P0 债务未修）
> - M1 boot.wim 可复现：⚠️ 脚本已落地（make_boot_wim.ps1 + upload_pe.ps1 + pe_assets/Startnet.cmd），真机哈希白名单验证未做
> - M2 前半段 P1+P2 真实跑通：✅ R7-D 编码完成（ZsTaskBuilder + BcdInjector + 8 个 Service + ExecuteR7OfflineBuild），编译 0 错 0 警；真机回归未做
> - M3 逃生窗 + 手动菜单：⏳ 待实现（Startnet.cmd 文本已落地，手动菜单未编码 + boot.wim 打包未做）
> - M4 核心八阶段可交付：⏳ 待实现（R7-D 编码约 85%，4 个 PE Service + Program.cs 集成；单元测试 + 真机回归未做）
> - M5 首次启动自动化：⏳ 待实现（R7-D 生成器已落地，真机联调未做）
> - M6 交付验收：⏳ 待实现

---

## 13. 审计官配置（3 维度 × 18 检查项，每 Phase 末尾必须跑一次）

### 13.1 稳定性（6）
S1 外部命令（diskpart/dism/bcdedit/bcdboot/wpeutil/dism mount）全部 ExitCode + stderr 判错；非 0 立即停流水线 + 红色日志
S2 中文路径 / 长路径 / 中文镜像名 / FAT32 4GB 单文件上限 全部有分支处理
S3 长时间操作（下载 / WIM 应用 / 驱动注入）不卡 UI 线程；进度条 + 取消按钮真实生效
S4 句柄释放（PowerShell Runspace / ISO Mount / WIM Mount / Mutex）100% 有 finally
S5 超时阈值：写盘 2h、下载按剩余时间估算×1.5、镜像应用 50GB/h；超时停 CMD 并保存日志
S6 异常回滚：分区出错 → 回滚 GPT/MBR；BCD 注入出错 → 恢复 BCD 备份；Z 目录下载中途取消 → 全删 ZS_Task

### 13.2 准确性（6）
A1 task.ini 16 字段 Windows 生成 ⇔ PE 读取双端单元测试：字符串 / 整数 / 布尔 / 路径分隔符 / 空值 20 组用例全过
A2 zs_manifest.key = 实际文件哈希：随机位翻转 / 截断 / 路径大小写 3 组失败用例必须被拦截
A3 GPT 尾端验证：ESP 卷标错、FAT/NTFS 错、双对 → 各 3 组模拟器通过
A4 MBR 尾端验证：Active 是 / 否 / 有两个分区 Active → 3 组模拟器通过
A5 固件判定 6 级冲突策略：reg=UEFI/disk=UEFI → 通过；reg BIOS/disk BIOS → 通过；reg UEFI/disk BIOS → disk 准；reg BIOS/disk UEFI → disk 准；reg 未知/disk UEFI → disk 准；reg/disk 均未知 → 停 6 条分支全覆盖
A6 Autounattend.xml OOBE 跳过 + 本地管理员自动登录 + 计算机名 + 时区 字段 xpath 准确；Windows SIM 校验无警告

### 13.3 完整性（6）
I1 P1 取消 → ZS_Task + manifest + 缓存 + 下载中的资源**全部**回滚删除；断点续传临时文件清干净
I2 P2 取消：shutdown /a 成功 + bootsequence 还原 + ramdisk 启动项删除 + boot.wim/boot.sdi 可选择保留或删除；反向闭环（点取消 → 10 秒后重查 BCD 不回滚也不崩溃）
I3 P3 10s 倒计时：① 超时 → 自动流水线；② 10s 内按任意键 → 进入手动菜单；③ 手动菜单选「返回」→ 退出到 CMD。三条反向闭环
I4 P4 任何阶段失败 → 日志落盘 pe_log.txt → 停 CMD 不重启；支持技术支持回收 pe_log.txt；失败不自动重启砖机
I5 P5 SetupComplete 自毁失败（占用 / 权限）→ 下次登录后继续清理；清理失败写注册表明文；不无限循环
I6 U 盘 / ISO 兜底通道：写 U 盘 / 生成 ISO → VMware 启动 → 手动菜单成功；与主链路互不依赖；代码双份但模型共享（OfflineTaskModels / OfflineTaskService）

---

## 14. 风险与注意事项（14 项 + 缓解）

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|------|------|------|----------|
| 1 | boot.wim 体积过大（>2GB）+ FAT32 单文件 4GB 限制，写入 ESP 失败 | 中 | 高 | Phase 1 严格控制 boot.wim ≈ 350MB；超过则用压缩 WIM + 拆分 SWM 方案 |
| 2 | BCD bootsequence 在部分 OEM BIOS 上不生效 | 低 | 高 | Phase 2 末尾自动执行 `bcdedit /enum {current}` 校验；不生效则走 U 盘 / ISO 兜底通道并中文提示 |
| 3 | PE 下中文显示方块 + 控制台字体乱码 | 中 | 高 | Phase 1 注入 chs_boot.ttf / 微软雅黑等宽；Startnet.cmd 先 `chcp 65001`；ZS_Agent 纯 ASCII 日志+ 中文转 UTF-8 文件 |
| 4 | 磁盘序号跨环境（Windows/PE）不一致导致误格式化 | 中 | 极高 | 沿用 R3 r8：P1 保存 disk_index/disk_size/disk_model 三元组；PE 端按 size+model 模糊匹配；匹配度 < 90% 则红色警告 + 停止 |
| 5 | Secure Boot 阻止 ramdisk boot.wim | 中 | 中 | Phase 4 检测 Secure Boot 状态；启用时给出 BIOS 设置步骤 + 关闭后重试入口 |
| 6 | 外部品牌名 / 参考名意外混入六件套 | 低 | 中 | 所有提交前跑 grep：检测 50+ 黑名单品牌词，命中则阻塞 commit |
| 7 | 宝塔面板用户手动修改了 PHP 配置导致项目不工作 | 中 | 中 | 文档反复强调 + 部署脚本做 15 项环境自检（php.ini 关键项对比推荐值；不对 → 告警但不自动修改） |
| 8 | 用户把 ZS_Task 目录放 C 盘 | 中 | 高 | P1 选盘强制跳过 C 盘 / 当前系统盘；中文红色警告 |
| 9 | PE 启动后找不到 ZS_Task（盘符分配异常） | 中 | 高 | Startnet.cmd 遍历 CDEFGHIJKLMNOPQRSTUVWXYZ 所有盘符；最后尝试 list volume 读 diskpart 盘符 |
| 10 | SHA-256 清单被篡改 / 镜像被恶意替换 | 低 | 高 | zs_manifest.key 增加服务器端签发的签名（HMAC-SHA256 用 JWT.KEY 的派生密钥）；PE 端可验证（离线模式下仍可校验完整性哈希） |
| 11 | 5 个测试架源码永久丢失 | 高 | 低 | Phase 0-5 选方案：恢复逻辑测试两个；UI 测试架因像素级依赖不再强制保留（改验收截图 + 人工） |
| 12 | Git 工作树一直脏 | 高 | 中 | Phase 0-6 一次性整理；后续开发启用 pre-commit：运行审计官稳定性 S1~S3 检查 + 黑名单品牌词 |
| 13 | Autounattend.xml 密码明文泄露到 pe_log.txt | 中 | 高 | LogMiddleware + ZS_Agent 日志统一掩码：<Value>password</Value>、productkey、token、key 等字段替换为 *** |
| 14 | SetupComplete.cmd 自毁被反病毒软件拦截 | 中 | 中 | 加数字签名（后期）；默认保留 ZS_Task，删除改为可选开关 + 二次勾选确认 |

---

## 15. 参考文档索引

- 新主链路唯一设计：docs/superpowers/specs/2026-09-01-zs-perfect-unattended-deployment-v1-design.md
- 旧架构对照（仅迁移时参考）：设计文档/*.md
- 六件套：README.md / 项目理解报告.md / 项目结构.txt / 操作指南.md / 版本更新记录.md / 开发计划表.md（本文件）