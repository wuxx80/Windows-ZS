# ZS 装机助手 · 项目理解报告

> 版本：v0.0.268311
> 日期：2026-08-30
> 本文档用于 AI 切换时快速恢复项目上下文

---

## 项目概述

全栈 Windows 装机解决方案，包含 Web 管理后台（PHP + MySQL + Layui）、WinPE 客户端（C# WPF）和 Windows 客户端（C# WPF）。支持一键装机、U 盘制作、系统修复、驱动注入、无人值守、网络部署等功能。

## 系统架构

### 三层架构
- **Web 管理后台**：PHP（ThinkPHP 6）+ MySQL + Layui 风格，部署于宝塔面板
- **API 服务层**：RESTful JSON API
- **客户端层**：WinPE 环境（C# WPF）和 Windows 环境（C# WPF）

### 数据流
`
客户端 → HTTP API → PHP 控制器 → 服务层 → MySQL/文件存储
`

## 当前完成状态

| 阶段 | 状态 | 详情 |
|------|------|------|
| 设计文档 | ✅ 已完成 | 架构/数据库/API/交互设计 |
| 后端骨架 | ✅ 已完成 | ThinkPHP 6 + 35张表 + 30个控制器 |
| 数据库 | ✅ 已完成 | 35张表完整建表 SQL + 种子数据 |
| API 认证 | ✅ 已验证 | JWT Token 认证，30个接口已测试 |
| 后台前端 | ✅ 已完成 | 29个管理页面 + 首页 + 登录页 |
| 核心业务逻辑 | ✅ 已完成 | 所有控制器完整业务逻辑实现 |
| 浏览器全量测试验证 | ✅ 已完成 | 29页面+8API |
| WinPE 客户端 | ✅ 已完成 | MVVM架构，一键装机六步向导（环境检测/镜像/磁盘/选项/确认/执行）+ 装机引擎/DiskPart/引导修复/驱动注入 + U盘制作（写U盘/生成ISO）+ 工具大全 |
| Windows 客户端 | ✅ 已完成 | MVVM架构，四大入口（装机/U盘/工具/绿软）+ 一键装机六步向导（安全版：仅建任务，装机在 WinPE 执行）+ U盘制作（写U盘/生成ISO）+ 工具大全 |
| API 全量联调 | ✅ 已完成 | 44/44 接口测试通过 |
| 性能优化 | ✅ 已完成 | 仪表盘/标签 N+1 查询消除 |
| 安全审查 | ✅ 已完成 | 认证/CORS/异常/上传/日志 |
| 部署文档 | ✅ 已完成 | 生产环境配置/客户端部署指南 |
| 打包发布 | ✅ 已完成 | publish.ps1 + 双端发布产物验证 |
| U盘制作与工具大全 | ✅ 已完成 | 四步向导 + 53工具/10分类 + B5 工具联动（双端编译 0 错误 + 逻辑/UI 测试通过） |
| 一键安装闭环修复 | ✅ 已完成 | 9 项闭环问题修复（真实下载/校验/备份、认领并发保护、身份校验、任务重试复用等，e2e 36 项 PASS） |
| U盘启动制作完善 | ✅ 已完成 | 输出方式「写U盘/生成ISO」+ 在线 URL 拉取 PE + 纯 C# 可引导 ISO 生成器（ISO9660+Joliet+El Torito 双引导）+ FAT12 efisys.bin 兜底（双端编译 0 错误 0 警告，_iso_logic_test 自测 + 在线 PE 端到端通过） |
| 用户注册/登录体系 | ✅ 已完成 | 客户端由「设备自注册」改为「用户注册/登录」（POST /api/v1/auth/register，测试账号 wuxx80/a111111）；设备自注册保留为任务执行凭证且自动 approved |
| 后台菜单精简 | ✅ 已完成 | 移除客户端管理/分组/版本管理、任务管理（任务列表/模板）、业务管理（客户/工单）菜单与页面，底层任务流程机制保留 |
| 客户端首页品牌信息 | ✅ 已完成 | 左上角品牌（Logo/主标题/副标题/标语）+ 边框（设置/登录·退出/版权/版本/联系/关于），对接公开接口 GET /api/v1/site/info 与后台「站点信息」设置 |
| 客户端炫彩动效 | ✅ 已完成 | Windows/WinPE 双端：动态流动渐变 + 彩色漂浮光斑 + 星尘粒子 + 入口按钮呼吸光晕 + 入场动画（像素差分 26.83% 验证） |

## 模块关系

### 核心模块（管理后台现行菜单）
1. **控制台仪表盘** — 统计卡片/装机趋势/镜像排行/快捷操作（含待执行任务实时统计）
2. **镜像管理** — 列表/上传/远程添加/格式转换/校验/版本历史/标签/下载/清理
3. **无人值守** — 模板可视化编辑/JSON 编辑/验证/Autounattend.xml 生成
4. **软件/驱动管理** — 软件分类/上传/装机模板/静默安装参数；驱动分类/上传/注入到镜像（DISM）
5. **网络部署** — PXE 配置/网络克隆/部署报告
6. **PE 定制** — 壁纸/启动画面/内置工具/构建导出 ISO
7. **系统管理** — 用户权限/系统设置（含站点信息：品牌 Logo/标题/标语/版权/联系/关于）/操作日志/通知/定时任务
8. **其他** — 报表统计/Webhook 日志/回收站

> 已从后台移除（仅删前端入口，底层 API/任务流程机制保留）：客户端管理/客户端分组/版本管理、任务管理（任务列表/任务模板）、业务管理（客户/工单）。

### 保留的后台/后端业务机制（供客户端使用，无后台页面）
- **客户端设备自注册 + 心跳**：POST /api/v1/clients/register（幂等，自动 approved）、/clients/heartbeat（30 秒）
- **任务流程**：创建/进度上报/认领并发保护/身份校验/取消/重试/暂停/继续/无人值守 XML/首次登录脚本（PE 端自动拉取 waiting 任务续装）
- **客户端接口**：镜像下载 clientDownload（断点续传）、PE 版本列表 clientList 与文件下载 fileDownload

### 模块依赖关系
- 镜像管理 → 装机任务（任务依赖镜像）
- 客户端设备（自注册记录）→ 装机任务（任务分配给客户端）
- 无人值守 → 装机任务（任务可选无人值守模板）
- 软件管理 + 驱动管理 → 装机任务（任务可选软件/驱动）
- 网络部署 → 镜像管理（PXE 启动依赖镜像）
- PE 定制 → 镜像管理（PE 构建依赖镜像）

## 关键技术方案

### 客户端认证（用户注册/登录体系）
- **用户注册/登录**：客户端登录为「用户名 + 密码」体系，公开接口 POST /api/v1/auth/register 注册（注册成功即自动登录，签发 token，角色固定「普通用户」code=user，不可注册为管理员）；POST /api/v1/auth/login 登录；测试账号 wuxx80 / a111111
- 会话持久化：Windows 端 SessionStore 本地保存 token + 用户名（重启免重复登录），退出时清除；核心功能（一键装机/U盘制作/绿色软件）需登录后使用
- WinPE 无人值守：PE 环境无法手动输入账号，延续 admin/admin123 自动登录连接服务器
- **设备自注册（任务执行凭证，非登录）**：clients/register 幂等自注册（client_id = {类型前缀}-{SHA256(MAC+CPU序列号)前12位}），register 自动 approved（无需后台人工审批），任务认领/进度上报以 client_id 校验身份
  - 幂等策略（按优先级）：① 传入 client_id 按 client_id 匹配刷新心跳；② 未传 client_id 但 MAC 匹配已有客户端则复用（兼容 WinPE 重启丢失本地 client_id）；③ 否则创建新客户端
  - 注册返回：服务端数据库 id + client_id + 状态，客户端保存以便后续复用
- JWT Token 认证，24 小时有效期，心跳自动续期
- 心跳 30 秒/次，3 次失败进入离线模式

### 客户端集成流程（WinPE / Windows 双端）
- WinPE 客户端：连接服务器 → 自注册（幂等）→ 拉取镜像/磁盘 → 创建装机任务 → 按步骤上报进度（分区→部署→修复引导→驱动注入→完成，状态 running/completed/failed 闭环）
- Windows 客户端：登录 → 自注册（类型 windows）→ 拉取镜像 → 创建装机任务 → 上报 running 状态（保持待 PE 环境继续执行装机，不直接标记完成）
- 路由约定：任务等带 :id 的特定路由（progress/cancel/retry/pause/resume/logs/detail）必须声明在通用集合路由（tasks）之前，避免误路由

### 镜像管理
- 支持格式：WIM/ISO/ESD/SWM/GHO
- 上传方式：本地上传（分片） + 远程 URL 添加（后台下载）
- 校验：SHA256 自动比对
- 版本历史：每次更新自动保存快照
- 标签系统：手动标签 + 自动标签规则
- 清理策略：过期自动清理，回收站保留 30 天

### 一键装机交互（六步向导，对齐《一键装机交互设计.md v3.0》）
- 六步流程：① 环境检测 → ② 选择镜像 → ③ 目标磁盘 → ④ 安装选项 → ⑤ 确认 → ⑥ 执行
- 9 项环境检测（网络/服务器/认证/PE识别/磁盘控制器/磁盘/启动模式/安全启动/兼容性），逐项实时回填，致命/警告区分 + 修复/忽略/强制继续
- 镜像来源切换（本地服务器/远程URL/本地缓存/自定义源）、搜索防抖 300ms、格式/系统类型筛选、分页
- 磁盘冲突检测（空间不足/GPT-MBR 不匹配/PE 运行盘）+ 分区方案（自动/自定义编辑器/保留现有）
- 安装选项九宫格（引导修复/无人值守/驱动注入/自动分区/备份数据/装软件/系统优化/激活/保留数据）+ 选项联动规则 + 扩展配置面板
- 确认汇总：冲突分级展示（通过/可自动修复/需确认/致命）
- 执行进度：主进度条（分段变色）+ 步骤清单 + 实时日志三层联动，支持暂停/继续/取消，完成页重启倒计时
- 双端差异：WinPE 端完整执行真实装机（分区/部署/驱动/引导）；Windows 端安全版仅创建任务并上报进度，实际装机在 WinPE 执行
- 断点续传+任务状态持久化

### 无人值守
- 可视化 Tab 配置（8 组配置项）
- 高级 JSON 编辑模式
- 自动生成 Autounattend.xml
- 首次登录脚本链：装软件→优化→清理→报告

### 装机任务（与 zs_tasks 表结构对齐）
- 任务字段：task_no 任务编号 / image_id 镜像 / client_id 客户端 / unattend_template_id 无人值守模板 / target_disk_index 目标磁盘 / target_partition 目标分区 / partition_scheme 分区方案（auto/custom/keep）/ options 参数 JSON
- 进度上报：客户端可上报进度值 + 状态（running/completed/failed），自动计算任务耗时并写入 duration
- 认领并发保护：进度上报采用乐观锁条件更新（where status=原状态 → 更新为 running），冲突返回 task_claim_conflict，防多客户端重复认领
- 身份校验：进度上报/取消/续装必须携带 client_id，与任务 client_id 不匹配返回 auth_client_mismatch（A 客户端不能操控 B 任务）
- 重试复用：POST /tasks/:id/retry 将 failed/cancelled 任务重置为 waiting（progress=0），复用原任务订单，PE 端按 client_id 拉取续装

### U 盘制作（四步向导 · 输出方式：写U盘 / 生成ISO）
- 四步流程：① 选择 U 盘 → ② 选择 PE 版本 → ③ 选项 → ④ 确认执行；Windows / WinPE 双端一致
- 输出方式切换（步骤①前）：①「写入 U 盘」沿用四步向导；②「生成 ISO 镜像」——无需选中 U 盘、无需管理员权限、无写盘锁，解包 PE → 按需写入客户端/工具 → 打包成可启动 ISO
- PE 来源三选：服务器 PE 版本（peVersions/clientList 在线拉取）/ 本地文件（.iso/.wim）/ 在线 URL（DownloadPeUrlAsync 从任意 HTTP(S) 直接拉取，缓存至 C:\ZS_Cache\pe\）
- 写盘安全护栏：4 项写盘校验 + 盘号二次确认 + 红色警示；执行过程可暂停/取消；管理员权限不足自动提权重启；多实例互斥锁（Local\ZS_UDiskWrite）
- 三态结果：ResultKind 区分 success / failed / canceled，UI 分别渲染成功页/失败页/取消页
- 卷标清洗：SanitizeLabel 移除非法字符并截断 11 字符（文件系统兼容）

### 可引导 ISO 生成（IsoBuilder + EfiSysGenerator，纯 C# 无第三方依赖）
- IsoBuilder.cs（Windows / WinPE 双端）：自研 ISO9660 + Joliet（UCS-2）+ El Torito 引导记录生成器，单引导记录 + 双段引导目录（Validation + Legacy 条目 + EFI 段头 + EFI 条目，对齐 oscdimg 规范），已移除 DiscUtils 依赖
- EfiSysGenerator.cs：当 PE 缺少原生 efisys.bin 时，用 bootx64.efi 生成 FAT12 格式 efisys.bin（1.44MB 软盘镜像：EFI 目录 cluster2 → BOOT cluster3 → BOOTX64.EFI cluster4，FAT 链 0xFFF）兜底，保证 UEFI 引导兼容
- UDiskService.BuildIsoAsync 构建流程：解包 PE（ISO 挂载/目录）→ 按需写入 ZS_Client + ZS_Tools → DetectAndPrepareBoot 检测/兜底生成引导镜像 → IsoBuilder 打包
- ISO 挂载可靠性：MountIsoAsync 用 PowerShell -EncodedCommand（UTF-16LE Base64）规避引号转义，挂载后通过 Win32_LogicalDisk(DriveType=5) 前后对比精确取新盘符
- 测试脚手架 _iso_logic_test/：FAT12 读取器（引导扇区/目录/FAT链/文件内容哈希）+ ISO9660 读取器（PVD/Joliet/BootRecord/引导目录校验和/路径表/目录记录/文件数据完整性），覆盖 efisys.bin 生成、ISO 结构、在线 PE 端到端

### 工具大全（53 工具 / 10 分类）
- 分类：磁盘管理 / 系统修复 / 网络工具 / 硬件检测 / 备份还原 / 密码重置 / 数据恢复 / 引导修复 / PE 工具 / 综合工具
- 清单：Data/Tools/tools.json（本地）+ 服务器双源；每工具含 emoji 图标 / exe 路径 / 参数 / 管理员标记
- 执行：本地内置工具经 Process.Start 运行并检测提权；在线工具从服务器下载后执行
- B5 工具联动：U 盘制作勾选「写入内置工具」时合并本地 Tools 目录 + ZS_Cache/tools 缓存 → 写入 U 盘 ZS_Tools 目录（PE 内离线可用）；未检测到工具文件时提示跳过且不中断制作

### 一键安装闭环修复（R3 轮 · 9 项）
- 镜像真实下载：客户端接口 GET /api/v1/images/:id/clientDownload 断点续传 + SHA256 与 file_hash 比对校验（ImageService.verifyHash 修复列名错误 sha256 → file_hash）
- 数据真实备份：分区格式化前 robocopy 备份原系统盘用户数据目录
- client_id 断链修复：后端拒绝无 client 的 waiting 任务；Windows 端 EnsureRegistered 保障下单前必完成注册
- Windows「立即重启」真实重启：shutdown /r + 引导热键提示弹窗
- 磁盘跨环境兜底：Windows 下单 options 持久化 disk_index/disk_size/disk_model，PE 端按物理特征模糊匹配

### 首页品牌信息（对接后台设置）
- 公开接口 GET /api/v1/site/info（api/SiteController）：读取 settings 表 group=site 配置，客户端首页无需登录即可拉取
- 首页左上角品牌：ZS Logo 文字 / 主标题 / 副标题 / 标语；边框项：设置 / 登录·退出 / 版权 / 版本 / 联系 / 关于
- 管理后台「系统设置 → 站点信息」组可编辑：Logo 文字/主标题/副标题/标语/官网/版本号/版权/联系/关于
- 客户端对应窗口：Windows 端 LoginWindow/SettingsWindow/AboutWindow/ContactWindow/VersionWindow/SoftwareWindow；WinPE 端 SettingsWindow/AboutWindow/ContactWindow/VersionWindow

### 客户端炫彩动效（Windows / WinPE 双端）
- 动态流动渐变背景：主渐变色双 Stop 对向呼吸流动（ColorAnimation + SineEase 循环）
- 5 个彩色漂浮光斑：RadialGradientBrush 椭圆沿 X/Y 不同周期正弦往返漂移，组合不规则轨迹
- 星尘粒子层：Canvas 动态生成细小光点，缓慢漂移 + 明暗闪烁
- 入口按钮呼吸光晕 + 悬停缩放（ScaleTransform 1.0→1.06）+ 中央内容入场动画（淡入 + 上浮）
- 验证：像素差分检测 26.83% 帧间变化，确认动画持续运行

## 数据库概览（35 张表）

核心表：zs_users, zs_roles, zs_images, zs_image_sources, zs_image_versions, zs_image_tags, zs_image_tag_relations, zs_download_links, zs_download_logs, zs_download_queue, zs_clients, zs_client_groups, zs_client_versions, zs_tasks, zs_task_records, zs_task_templates, zs_unattend_templates, zs_software, zs_software_categories, zs_software_templates, zs_drivers, zs_scripts, zs_pe_versions, zs_pe_customize, zs_pxe_configs, zs_network_deploy, zs_customers, zs_work_orders, zs_settings, zs_logs, zs_scheduled_tasks, zs_scheduled_task_logs, zs_notifications, zs_recycle_bin, zs_webhook_logs

## 审计官信息

本项目需配合"审计官"子 agent 进行代码质量穿透式审计。审计官职责：
1. 稳定性审计：检查空值/异常/线程/资源释放/超时处理
2. 准确性审计：验证业务逻辑完整性/状态闭环/边界条件
3. 完整性审计：检查功能是否完整/UI 状态是否闭环/API 是否覆盖所有场景

## 开发注意事项
- 所有界面以纯中文显示为主，可切换纯英文
- 手机端美观优先，元素紧凑排列
- 按钮上不使用 > 符号
- 字体不加粗
- 检测/修复进度条与清单项需逐条实时联动
- 状态变更必须闭环（登录/退出/开启/关闭等）
- 禁止硬编码密钥/Token/密码，使用环境变量
- 多 agent 协作时，审计官信息需同步更新至此文档
- PHP 配置文件（宝塔面板）不得修改，伪静态规则使用独立文件
- 所有 API 返回统一格式：code/message/data/timestamp
- 数据库操作必须使用参数化查询防注入

## 项目状态
- 后端核心业务逻辑：已完成
- 浏览器全量测试验证：已完成（29页面+8API）
- 客户端集成：WinPE 客户端（注册+建任务+进度上报）与 Windows 客户端（注册+建任务）已完成
- 一键装机六步向导：WinPE 端完整实现 + Windows 端安全版同步完成（双端编译 0 错误，Windows 端向导 UI 自动化验证通过）
- U 盘制作与工具大全：四步向导 + 53工具/10分类 + B5 工具联动双端完成（编译 0 错误 0 警告，逻辑测试 4 项 + STA WPF UI 测试 4 项通过）
- U 盘启动制作完善：输出方式「写U盘/生成ISO」双模式 + 在线 URL 拉取 PE + 纯 C# 可引导 ISO 生成器（ISO9660+Joliet+El Torito 双引导）+ FAT12 efisys.bin 兜底（双端编译 0 错误 0 警告，_iso_logic_test 逻辑自测通过，在线拉取 ~340MB PE 生成 ~337MB 可引导 ISO 成功）
- 一键安装闭环修复（9 项）：真实下载/校验/备份、client_id 保障、重试状态 waiting、认领并发保护 + 身份校验、真实重启、注册类型修正、死代码删除、磁盘特征持久化、失败重试复用原任务
- 用户注册/登录体系：Windows 端 LoginWindow 注册/登录 + SessionStore 会话持久化；WinPE 端 admin/admin123 自动登录；客户端设备自注册自动 approved（无审批门槛）
- 后台菜单精简：移除客户端管理/分组/版本管理、任务管理、业务管理页面；仪表盘改为待执行任务统计
- 首页品牌信息对接后台设置：公开接口 /api/v1/site/info + 后台「站点信息」组
- 客户端炫彩动效：双端动态渐变/光斑/粒子/呼吸光晕/入场动画（像素差分 26.83% 验证通过）
- 端到端回归测试（server/e2e_winpe_test.php）：登录→注册（幂等）→建任务→进度上报→完成→闭环校验 36 项 PASS
- 双端客户端编译：0 错误 0 警告
- 待验证：六步向导真实装机步骤（分区/部署/驱动/引导）需在真实 PE 环境验证；U 盘写盘需在真实 U 盘验证；生成 ISO 引导兼容性建议用主流虚拟化/真机固件冒烟验证
- 待完成：真实服务器三端联调、WebSocket 实时通信、登录频率限制
