# ZS 装机助手 · 项目理解报告

> 版本：v0.0.268311
> 日期：2026-08-30
> 本文档用于 AI 切换时快速恢复项目上下文

---

## 项目概述

全栈 Windows 装机解决方案，包含 Web 管理后台（PHP + MySQL + Layui）、WinPE 客户端（C# WPF）和 Windows 客户端（C# WPF）。支持一键装机、U 盘制作、系统修复、驱动注入、无人值守、网络部署等功能。

## 系统架构

### 三层架构
- **Web 管理后台**：PHP（ThinkPHP 6）+ MySQL + Layui 风格，部署于宝塔面板
- **API 服务层**：RESTful JSON API
- **客户端层**：WinPE 环境（C# WPF）和 Windows 环境（C# WPF）

### 数据流
`
客户端 → HTTP API → PHP 控制器 → 服务层 → MySQL/文件存储
`

## 当前完成状态

| 阶段 | 状态 | 详情 |
|------|------|------|
| 设计文档 | ✅ 已完成 | 架构/数据库/API/交互设计 |
| 后端骨架 | ✅ 已完成 | ThinkPHP 6 + 35张表 + 30个控制器 |
| 数据库 | ✅ 已完成 | 35张表完整建表 SQL + 种子数据 |
| API 认证 | ✅ 已验证 | JWT Token 认证，30个接口已测试 |
| 后台前端 | ✅ 已完成 | 29个管理页面 + 首页 + 登录页 |
| 核心业务逻辑 | ✅ 已完成 | 所有控制器完整业务逻辑实现 |
| 浏览器全量测试验证 | ✅ 已完成 | 29页面+8API |
| WinPE 客户端 | ✅ 已完成 | MVVM架构，包含装机引擎/DiskPart/引导修复/驱动注入 |
| Windows 客户端 | ✅ 已完成 | MVVM架构，四大入口（装机/U盘/工具/绿软） |
| API 全量联调 | ✅ 已完成 | 44/44 接口测试通过 |
| 性能优化 | ✅ 已完成 | 仪表盘/标签 N+1 查询消除 |
| 安全审查 | ✅ 已完成 | 认证/CORS/异常/上传/日志 |
| 部署文档 | ✅ 已完成 | 生产环境配置/客户端部署指南 |
| 打包发布 | ✅ 已完成 | publish.ps1 + 双端发布产物验证 |

## 模块关系

### 核心模块
1. **控制台仪表盘** — 统计卡片/装机趋势/镜像排行/快捷操作
2. **镜像管理** — 列表/上传/远程添加/格式转换/校验/版本历史/标签/下载/清理
3. **客户端管理** — 注册审核/分组/版本管理/远程命令/心跳检测
4. **装机任务** — 创建/调度/监控/模板/日志/重试/回滚
5. **无人值守** — 模板可视化编辑/JSON 编辑/验证/Autounattend.xml 生成
6. **软件管理** — 分类/上传/装机模板/静默安装参数
7. **驱动管理** — 分类/上传/注入到镜像（DISM）
8. **网络部署** — PXE 配置/网络克隆/部署报告
9. **PE 定制** — 壁纸/启动画面/内置工具/构建导出 ISO
10. **客户工单** — 客户信息/维修工单/统计
11. **系统管理** — 用户权限/存储/通知/插件/Webhook/API
12. **日志审计** — 操作日志/类型分布/清理
13. **报告统计** — 装机报表/客户端统计/镜像排行/工单统计

### 模块依赖关系
- 镜像管理 → 装机任务（任务依赖镜像）
- 客户端管理 → 装机任务（任务分配给客户端）
- 无人值守 → 装机任务（任务可选无人值守模板）
- 软件管理 + 驱动管理 → 装机任务（任务可选软件/驱动）
- 网络部署 → 镜像管理（PXE 启动依赖镜像）
- PE 定制 → 镜像管理（PE 构建依赖镜像）
- 客户工单 → 装机任务（工单关联装机记录）

## 关键技术方案

### 客户端认证
- client_id = {类型前缀}-{SHA256(MAC+CPU序列号)前12位}
- 客户端自注册接口（POST /api/v1/clients/register）：WinPE/Windows 客户端连接服务器时调用，幂等注册
  - 幂等策略（按优先级）：① 传入 client_id 按 client_id 匹配刷新心跳；② 未传 client_id 但 MAC 匹配已有客户端则复用（兼容 WinPE 重启丢失本地 client_id）；③ 否则创建新客户端
  - 注册返回：服务端数据库 id + client_id + 状态，客户端保存以便后续复用
- JWT Token 认证，24 小时有效期，心跳自动续期
- 心跳 30 秒/次，3 次失败进入离线模式

### 客户端集成流程（WinPE / Windows 双端）
- WinPE 客户端：连接服务器 → 自注册（幂等）→ 拉取镜像/磁盘 → 创建装机任务 → 按步骤上报进度（分区→部署→修复引导→驱动注入→完成，状态 running/completed/failed 闭环）
- Windows 客户端：登录 → 自注册（类型 windows）→ 拉取镜像 → 创建装机任务 → 上报 running 状态（保持待 PE 环境继续执行装机，不直接标记完成）
- 路由约定：任务等带 :id 的特定路由（progress/cancel/retry/pause/resume/logs/detail）必须声明在通用集合路由（tasks）之前，避免误路由

### 镜像管理
- 支持格式：WIM/ISO/ESD/SWM/GHO
- 上传方式：本地上传（分片） + 远程 URL 添加（后台下载）
- 校验：SHA256 自动比对
- 版本历史：每次更新自动保存快照
- 标签系统：手动标签 + 自动标签规则
- 清理策略：过期自动清理，回收站保留 30 天

### 一键装机交互
- 6 种页面状态：检测中→就绪→配置中→确认→执行中→完成
- 9 项环境检测，逐项实时回填
- 10 步执行序列，进度条+步骤清单+实时日志三层联动
- 断点续传+任务状态持久化
- 支持远程 URL 镜像源

### 无人值守
- 可视化 Tab 配置（8 组配置项）
- 高级 JSON 编辑模式
- 自动生成 Autounattend.xml
- 首次登录脚本链：装软件→优化→清理→报告

### 装机任务（与 zs_tasks 表结构对齐）
- 任务字段：task_no 任务编号 / image_id 镜像 / client_id 客户端 / unattend_template_id 无人值守模板 / target_disk_index 目标磁盘 / target_partition 目标分区 / partition_scheme 分区方案（auto/custom/keep）/ options 参数 JSON
- 进度上报：客户端可上报进度值 + 状态（running/completed/failed），自动计算任务耗时并写入 duration

## 数据库概览（35 张表）

核心表：zs_users, zs_roles, zs_images, zs_image_sources, zs_image_versions, zs_image_tags, zs_image_tag_relations, zs_download_links, zs_download_logs, zs_download_queue, zs_clients, zs_client_groups, zs_client_versions, zs_tasks, zs_task_records, zs_task_templates, zs_unattend_templates, zs_software, zs_software_categories, zs_software_templates, zs_drivers, zs_scripts, zs_pe_versions, zs_pe_customize, zs_pxe_configs, zs_network_deploy, zs_customers, zs_work_orders, zs_settings, zs_logs, zs_scheduled_tasks, zs_scheduled_task_logs, zs_notifications, zs_recycle_bin, zs_webhook_logs

## 审计官信息

本项目需配合"审计官"子 agent 进行代码质量穿透式审计。审计官职责：
1. 稳定性审计：检查空值/异常/线程/资源释放/超时处理
2. 准确性审计：验证业务逻辑完整性/状态闭环/边界条件
3. 完整性审计：检查功能是否完整/UI 状态是否闭环/API 是否覆盖所有场景

## 开发注意事项
- 所有界面以纯中文显示为主，可切换纯英文
- 手机端美观优先，元素紧凑排列
- 按钮上不使用 > 符号
- 字体不加粗
- 检测/修复进度条与清单项需逐条实时联动
- 状态变更必须闭环（登录/退出/开启/关闭等）
- 禁止硬编码密钥/Token/密码，使用环境变量
- 多 agent 协作时，审计官信息需同步更新至此文档
- PHP 配置文件（宝塔面板）不得修改，伪静态规则使用独立文件
- 所有 API 返回统一格式：code/message/data/timestamp
- 数据库操作必须使用参数化查询防注入

## 项目状态
- 后端核心业务逻辑：已完成
- 浏览器全量测试验证：已完成（29页面+8API）
- 客户端集成：WinPE 客户端（注册+建任务+进度上报）与 Windows 客户端（注册+建任务）已完成
- 端到端回归测试（server/e2e_winpe_test.php）：登录→注册（幂等）→建任务→进度上报→完成 全流程 15 项 PASS
- 双端客户端编译：0 错误 0 警告
- 待完成：真实服务器三端联调、WebSocket 实时通信、登录频率限制
