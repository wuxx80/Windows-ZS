# ZS 装机助手 · 项目理解报告

> 版本：v0.0.268311-R7（2026-09-02 · BCD 硬盘 PE 启动 + 全离线任务包架构切换）
> 子轮：R7-C（Startnet.cmd ↔ WinPE_Agent 契约对齐）→ R7-D（P1+P2 全链路代码层闭环）→ **R7-E（UI 同步 + 镜像管理修复 + 清理逻辑补全 + 菜单精简 · 三端编译 0 错 0 警）**
> 本文档用于 AI 切换时快速恢复上下文。**文档与代码不一致时，以实际磁盘代码 + Git 状态为准。**

---

## 一、项目概述

完整的 Windows 装机解决方案：Web 管理后台（PHP + MySQL + Layui 风格）+ WinPE 端（.NET 8 AOT 控制台）+ Windows 客户端（C# WPF）。

**主链路切换声明（R7 · 2026-09-01 确认）：废弃**「Windows 下单 → U 盘/ISO/PXE → PE 端联网认领 waiting 任务」的旧主链路；**新主链路为**「**Windows 下单时 100% 下载全部资源写入非系统分区 ZS_Task 目录 → BCD bootsequence 一次性启动项从硬盘加载 boot.wim → PE 端完全离线执行 §6 八阶段流水线 → 新系统首次启动 SetupComplete.cmd 装软件 + 优化**」。

U 盘 / ISO / PXE 仅作为无法注入 BCD 启动项（BitLocker / 系统加密 / 第三方多引导管理器）时的**兜底逃生通道**，不再是主链路。

## 二、系统架构

### 2.1 三层架构 + 新主链路 P1~P5

- **Web 管理后台**：PHP（ThinkPHP 6）+ MySQL + Layui 风格，部署于宝塔面板。**现行菜单不含**：PXE 配置、网络部署、客户端管理/分组/版本、任务管理（列表/模板）、业务管理（客户/工单）。底层控制器 / 路由 / 数据表仍保留作为迁移过渡期复用。
- **API 服务层**：RESTful JSON。新链路 P1 阶段（Windows 端）仅用来下载资源与创建任务记录；PE 阶段完全离线（不依赖任何 HTTP 接口）。
- **客户端层**：
  - Windows 端（C# WPF）：**P1**（登录 → 选镜像 / 目标盘 / 13 项开关 → 下载全部资源 → 写入 ZS_Task 目录 → 生成 task.ini + zs_manifest.key）+ **P2**（校验清单 → 写 boot.wim/boot.sdi 到 ESP → bcdedit 创建 ramdisk 启动项 → bootsequence 设置一次性启动 → 30s 倒计时重启）—— **R7-D 已完成代码层闭环**（`ZsTaskBuilder` + `BcdInjector` + 6 个 Downloader + `TaskIniWriter` + `ManifestWriter`）
  - PE 端：**Startnet.cmd**（遍历盘符找 ZS_Task 目录 / 10s 倒计时逃生窗）→ **ZS_Agent.exe**（.NET 8 AOT 控制台程序，纯本地外部命令流水线）执行 §6 八阶段流水线
  - 首次进新系统：**SetupComplete.cmd**（SYSTEM 权限装软件 → 优化 → 可配置是否自毁 ZS_Task）

### 2.2 数据流

新主链路（Offline-First）：
`Windows_Client P1/P2 → ZS_Task 目录（镜像/驱动/软件/脚本/应答/清单） + BCD bootsequence 一次性启动项 → 重启进硬盘 boot.wim（ramdisk） → Startnet.cmd 10s 逃生窗 → ZS_Agent 纯本地 DiskPart/DISM/BCDboot/DISM 驱动 流水线 → 重启 → 新系统 SetupComplete.cmd`

> **R7-D 全链路代码层闭环（2026-09-02）**：Windows_Client `InstallWizardViewModel.ExecuteR7OfflineBuild` 一键触发 → `ZsTaskBuilder`（建 ZS_Task 目录 + 10 项资源下载 + task.ini/zs_manifest.key 生成 + V1 校验 + 失败回退删 ZS_Task）→ `BcdInjector`（BCD 备份 + `bcdedit /copy {current}` + ramdisk + bootsequence 一次性启动 + Rollback 回退）→ 30s 自动重启进 PE。`StartExecution` 已改为优先走 R7 流程，失败回退到旧链路（保留兼容）。PE 端 `ZS_Agent.RunAutoMode` 读 task.ini/manifest 后执行 `ExecutePipeline` 八阶段（固件双判 / 分区尾端验证 / 引导修复双路径 / SetupComplete / Unattend 全部集成）。

旧链路（迁移过渡期保留，仅用于 P1 资源下载前的客户端注册与建档）：
`客户端 → HTTP API → PHP 控制器 → 服务层 → MySQL/文件存储`

## 三、P0 基建缺口（修基建前不能开始 Phase 1~5 编码，否则项目交接断链）

| # | 缺口 | 位置 | 影响 |
|---|------|------|------|
| P0-1 | RoleController.php 文件缺失 | server/app/controller/admin/RoleController.php 不存在；route/admin.php 无 Role 路由 | 后台用户管理中角色 CRUD / 分配完全失效 |
| P0-2 | 前后端 role 契约不一致 | 前端 users.html 提交 role:string vs 后端 UserController 读 role_id:int | 保存后角色写空或 0 |
| P0-3 | WinPE_Agent 未入 sln / 未入 Git | ZS_Installer.slnx 仅列 Win+WinPE 双端；Git 中 WinPE_Agent/ 为 untracked | 新 clone 缺新主链路 PE 核心执行者 |
| P0-4 | AuthMiddleware / LogMiddleware 未注册 | config/middleware.php 仅注册了 CorsMiddleware | zs_logs 永不写入；认证完全依赖各控制器手动判断 |
| P0-5 | 5 个测试脚手架已删、Git delete 未提交 | _b5_logic_test / _b5_ui_harness / _iso_logic_test / _offline_logic_test / _offline_ui_harness 五个目录 | 原 R2/R4/R6「测试通过」声明无法在新 clone 复现 |
| P0-6 | Git 工作树脏 | 28 modified / 8 deleted / 6 untracked（含 WinPE_Agent） | 新 clone 不能复现本机当前状态 |

## 四、当前真实完成状态（逐项 · 磁盘实查 · 2026-09-02）

| 项 | 状态 | 说明 |
|---|---|---|
| BCD 离线架构设计 v1 | ✅ 文档完成 | docs/superpowers/specs/2026-09-01-zs-perfect-unattended-deployment-v1-design.md |
| 后端骨架（35 表 + 31 控制器 + 36 模型 + 8 服务） | ✅ 代码存在 | 缺 RoleController（P0-1）；AuthMiddleware/LogMiddleware 未挂（P0-4） |
| 管理后台前端（22 页 + 登录 + 控制台） | ✅ 代码存在（菜单已精简为 19 项） | 现行菜单不含 PXE/网络部署/客户端×3/任务×2/客户/工单。用户角色页无效（P0-1）；操作日志空（P0-4） |
| WinPE_Client（WPF 六步 + U盘 + 工具） | ✅ Release 编译 0 错 0 警 | 旧主链路；Phase 3 评估是否降级为 PE 手动装机小工具 |
| Windows_Client（登录 + 六步 + U盘 + 工具 + 绿软） | ✅ Release 编译 0 错 0 警 | 「一键装机」按钮 **R7-D 已切到 `ExecuteR7OfflineBuild` 新流程**（ZsTaskBuilder + BcdInjector + 30s 自动重启），失败回退旧链路；8 个新 Service 已落地（TaskIniWriter/ManifestWriter/PeAssetDownloader/SystemImagePreDownloader/DriverPackageDownloader/SoftwarePackageDownloader/ZsTaskBuilder/BcdInjector）+ ApiService 新增 GetBaseUrl() |
| WinPE_Agent（§6 八阶段流水线 AOT） | ✅ R7-D 全链路代码层闭环 | 已入 sln + Git；Program.cs `RunAutoMode` 集成固件判定 + `ExecutePipeline` 集成分区脚本/尾端验证/引导修复双路径/SetupComplete/Unattend + `BuildPartitionScript`/`CopyDirectory` 辅助；4 个 PE 端 Service 通过 csproj Link 复用 WinPE_Client 实现（FirmwareDetector/PartitionVerifier/SetupCompleteBuilder/UnattendXmlBuilder）；DiskPartService 新增 `ExecuteRawScriptAsync`；编译 0 错 0 警。**仍待**：boot.wim 打包真机验证 / 单元测试（分区尾端+固件 6 级冲突模拟器）/ 审计官 18 项 |
| 客户端心跳/认领/进度旧链路 API | ⚠️ 代码保留 | P1 仅复用 clients/register、images/clientDownload、peVersions/clientList、tasks/create 少量接口；其余 Phase 3 评估删留 |
| 5 个测试脚手架 | ❌ 磁盘已删、Git 未提交删除 | Phase 0 末尾可选择性恢复 |
| **R7-C · Startnet.cmd ↔ WinPE_Agent 契约对齐** | ✅ 已完成 | TaskIni.cs + TaskIniParser.cs + ManifestValidator.cs 3 个新文件；Program.cs RunAutoMode 入口；详见版本更新记录.md R7-C 子轮 |
| **R7-D · P1+P2 全链路代码层闭环** | ✅ 已完成 | 12 新增 / 6 修改；WinPE_Agent + Windows_Client 编译 0 错 0 警；详见版本更新记录.md R7-D 子轮 |
| **客户端模块调整清单** | ✅ 已整理 | docs/superpowers/specs/2026-09-01-客户端模块调整清单.md：Windows_Client 缺 11 个 Service + WinPE_Client 需移除 11 类 GUI + PE 端缺 4 个 Service（FirmwareDetector/PartitionVerifier/SetupCompleteBuilder/UnattendXmlBuilder）—— **R7-D 已完成 PE 端 4 个 + Windows 端 8 个共 12 个 Service 落地** |
| **P1 · Windows ZS_Task 下载 + task.ini/zs_manifest.key 写入** | ✅ R7-D 代码完成 | `ZsTaskBuilder` 编排：建 ZS_Task 目录 + `PeAssetDownloader`（boot.wim+boot.sdi+ZS_PE_Agent.exe）+ `SystemImagePreDownloader`（系统镜像→images/）+ `DriverPackageDownloader`（zip 解压到 drivers/）+ `SoftwarePackageDownloader`（按 key 批量→software/{key}/）+ `TaskIniWriter`（7 section/16 字段 UTF-8 无 BOM）+ `ManifestWriter`（`sha256=HASH=path` 清单）+ V1 校验 + 失败回退删 ZS_Task |
| **P2 · BCD bootsequence 一次性启动项注入** | ✅ R7-D 代码完成 | `BcdInjector`：BCD 备份 + `bcdedit /copy {current}` + ramdisk + bootsequence 一次性启动 + Rollback 回退；`InstallWizardViewModel.ExecuteR7OfflineBuild` 一键触发 P1+P2 + 30s 自动重启；`StartExecution` 优先走 R7 流程，失败回退旧链路 |
| **P3 · Startnet.cmd（盘符遍历 / 10s 逃生窗 / 调用 ZS_Agent）** | ⚠️ pe_assets/Startnet.cmd 已落地 | 设计 §5.1 内容已实现，但需打包进 boot.wim Windows\System32\ |
| **P4 · ZS_Agent 八阶段流水线完整实现** | ✅ R7-D 代码完成 | `ExecutePipeline` 集成：① 固件判定（`FirmwareDetector`）② 分区+尾端验证（`PartitionVerifier`）③ WIM/ESD 应用 ④ 驱动注入 ⑤ 引导修复双路径 ⑥ `UnattendXmlBuilder` 写 Panther ⑦ `SetupCompleteBuilder` 写 Setup\Scripts ⑧ 重启；`BuildPartitionScript` + `CopyDirectory` 辅助。**仍待**：单元测试 + 真机回归 |
| **P5 · SetupComplete.cmd 模板渲染 + 写入新系统** | ✅ R7-D 代码完成（仅生成器） | `SetupCompleteBuilder` 已实现模板渲染（软件静默安装+系统优化+自清理+自毁）；`UnattendXmlBuilder` auto 模式生成 Unattend.xml / manual 返回 null；真机首次启动验证待 Phase 5 联调 |

## 五、模块关系

### 5.1 管理后台现行菜单（19 项）
1. 控制台仪表盘（任务实时统计 / 在线客户端）
2. 镜像管理：镜像列表 / 镜像源 / 镜像标签
3. 无人值守：无人值守模板
4. 软件驱动：软件 / 分类 / 模板 + 驱动 + 脚本
5. PE 定制：PE 版本 / PE 定制
6. 系统管理：用户管理（⚠️ 角色缺控制器）/ 系统设置（站点信息组）/ 操作日志（⚠️ 中间件未挂）/ 通知 / 定时任务
7. 其他：报表统计 / Webhook 日志 / 回收站

> 已从菜单移除（底层保留作过渡期复用，Phase 3 评估永久删除）：**PXE 配置、网络部署**、客户端管理/分组/版本、任务管理（列表/模板）、业务管理（客户/工单）。

### 5.2 保留的后台 / 后端机制（新链路仍用）
- 客户端设备自注册 + 心跳：POST /api/v1/clients/register（幂等自动 approved）、/clients/heartbeat（30s）—— Windows 端 P1 下单前建立身份
- 任务流程：创建 / 进度 / 认领乐观锁 / 取消 / 重试——Windows 端 P1 建档后写回进度
- 客户端下载接口：镜像 clientDownload、PE 版本 clientList + fileDownload——P1 阶段直接复用断点续传能力下载资源到 ZS_Task
- 无人值守模板：GET /unattendTemplates/:id/generateXml——P1 阶段渲染 Autounattend.xml 写入 ZS_Task

### 5.3 模块依赖
- 镜像管理 → ZS_Task\images\ 打包清单
- 无人值守模板 → Autounattend.xml + 新系统 SetupComplete.cmd（Phase 4）
- 软件 + 驱动 → ZS_Task\software\ 与 ZS_Task\drivers\ 打包清单
- 站点设置组 → 客户端首页品牌渲染

### 5.4 R7-D 子轮 · 全链路代码层模块清单（2026-09-02）

> Windows 下单 → ZsTaskBuilder 生成 ZS_Task → BcdInjector 注入 BCD → 30s 自动重启进 PE → ZS_Agent 读 task.ini/manifest → 八阶段装机

#### 5.4.1 Windows_Client 端（8 个新 Service + 2 个修改）
| 文件 | 职责 |
|------|------|
| `Windows_Client/Services/ZsTaskBuilder.cs` | **P1 全流程编排**：建 ZS_Task 目录 → 调度 10 项资源下载 → 生成 task.ini/zs_manifest.key → V1 校验 → 失败回退删 ZS_Task 整目录 |
| `Windows_Client/Services/TaskIniWriter.cs` | task.ini 生成器（7 section / 16 字段，UTF-8 无 BOM） |
| `Windows_Client/Services/ManifestWriter.cs` | zs_manifest.key SHA-256 清单生成器（格式 `sha256=HASH=path`） |
| `Windows_Client/Services/PeAssetDownloader.cs` | boot.wim + boot.sdi + ZS_PE_Agent.exe HTTP 下载 |
| `Windows_Client/Services/SystemImagePreDownloader.cs` | 系统镜像预下载到 `ZS_Task\images\` |
| `Windows_Client/Services/DriverPackageDownloader.cs` | 驱动包下载 + zip 解压到 `ZS_Task\drivers\` |
| `Windows_Client/Services/SoftwarePackageDownloader.cs` | 软件包批量下载到 `ZS_Task\software\{key}\` |
| `Windows_Client/Services/BcdInjector.cs` | **BCD 注入**：备份 + `/copy {current}` + ramdisk + bootsequence + Rollback 回退 |
| `Windows_Client/Services/ApiService.cs`（修改） | 新增 `GetBaseUrl()` 供各 Downloader 复用 |
| `Windows_Client/ViewModels/InstallWizardViewModel.cs`（修改） | 新增 `ExecuteR7OfflineBuild`：ZsTaskBuilder + BcdInjector + 自动重启；`StartExecution` 优先走 R7 流程失败回退旧链路 |
| `Windows_Client/Windows_Client.csproj`（修改） | Compile Link 添加 `TaskIni.cs` + `FirmwareDetector.cs` |

#### 5.4.2 PE 端（4 个新 Service + 2 个修改）
| 文件 | 职责 |
|------|------|
| `WinPE_Client/Services/FirmwareDetector.cs` | §6.0 固件双判：注册表主判定 + diskpart 回退 + 6 级冲突 + `force_gpt`/`force_mbr` override |
| `WinPE_Client/Services/PartitionVerifier.cs` | §6.1 分区尾端验证：GPT 验证 ESP + FAT32；MBR 验证 Active |
| `WinPE_Client/Services/SetupCompleteBuilder.cs` | §6.6 模板渲染：软件静默安装 + 系统优化 + 自清理 + 自毁 |
| `WinPE_Client/Services/UnattendXmlBuilder.cs` | §6.6c OOBE：auto 模式生成 Unattend.xml，manual 返回 null |
| `WinPE_Client/Services/DiskPartService.cs`（修改） | 新增 `ExecuteRawScriptAsync` |
| `WinPE_Agent/Program.cs`（修改） | `RunAutoMode` 固件判定 + `ExecutePipeline` 分区脚本/尾端验证/引导修复双路径/SetupComplete/Unattend 集成 + `BuildPartitionScript`/`CopyDirectory` |
| `WinPE_Agent/WinPE_Agent.csproj`（修改） | 4 个新 Service 通过 Compile Link 复用 WinPE_Client 实现 |

> **复用机制**：PE 端 4 个 Service 物理路径在 `WinPE_Client/Services/`，通过 csproj `<Compile Link="..." Include="..\WinPE_Client\Services\*.cs" />` 让 `WinPE_Agent`（AOT 控制台）直接复用同一份源码，避免代码双份漂移。

## 六、关键技术方案

### 6.1 客户端认证（用户注册 / 登录体系）
- **用户注册 / 登录**：客户端「用户名 + 密码」，公开接口 POST /api/v1/auth/register；注册即自动登录签发 token，角色固定 user。测试账号仅 install.sql 种子内置（本地联调）。
- 会话持久化：Windows 端 SessionStore 保存 token + 用户名；退出清除。核心功能（一键装机 / U盘 / 绿软）需登录。
- WinPE 无人值守：ZS_Agent.exe **不登录服务器**；完全离线读 ZS_Task 内 task.ini / Autounattend.xml / SetupComplete.cmd。
- **设备自注册（任务执行凭证，非登录）**：clients/register 幂等，client_id = {前缀}-{SHA256(MAC+CPU 序列号)前12位}。策略：① 传 client_id 按 client_id 匹配；② MAC 匹配复用；③ 否则新建。
- JWT 24 小时有效期，心跳续期；30s/次；3 次失败进入离线模式（Windows 端仍可继续本地 P1 下载）。

### 6.2 新主链路 P1~P5（BCD + ZS_Task 全离线）

- **P1（Windows · 资源收集）** ✅ R7-D 代码完成：登录 → 选镜像 → 选目标盘 → 13 项开关 → 开始装机。从服务器下载：镜像(WIM/ESD)+驱动包+软件包+Autounattend.xml模板+SetupComplete.cmd模板+PE boot.wim+boot.sdi+ZS_Agent.exe。全部写入**非系统分区** `\ZS_Task\`（子目录 images/drivers/software/scripts/boot/pe_agent），生成 `ZS_Task/task.ini`（16 字段）+ `ZS_Task/zs_manifest.key`（全资源 SHA-256 清单）。**实现**：`ZsTaskBuilder` 编排 + `PeAssetDownloader`/`SystemImagePreDownloader`/`DriverPackageDownloader`/`SoftwarePackageDownloader` 4 个 Downloader + `TaskIniWriter` + `ManifestWriter` + V1 校验 + 失败回退删 ZS_Task。
- **P2（Windows · 一次性启动）** ✅ R7-D 代码完成：校验 manifest → boot.wim + boot.sdi 写 ESP 或系统保留分区 → bcdedit 创建 ramdisk 启动项 → `bcdedit /set {bootmgr} bootsequence {ramdisk_guid}`（下次启动一次性进入 PE）→ 30s 倒计时重启（用户可 shutdown /a 取消）。**实现**：`BcdInjector`（BCD 备份 + `/copy {current}` + ramdisk + bootsequence + Rollback 回退）+ `InstallWizardViewModel.ExecuteR7OfflineBuild` 一键触发 + 30s 自动重启；`StartExecution` 优先走 R7 流程失败回退旧链路。
- **P3（PE · Startnet.cmd 逃生窗）** ⚠️ 仍待打包：ramdisk 加载 boot.wim → WinPE 启动 → Startnet.cmd 遍历所有固定盘找 `\ZS_Task\task.ini` → 找到后 **10s 倒计时**（按任意键进入手动装机，超时自动继续）→ 多个 ZS_Task 时按 manifest 完整性 + 最近写入时间选择；找不到则进手动装机控制台菜单。`pe_assets/Startnet.cmd` 文本已落地，仍需打包进 boot.wim Windows\System32\。
- **P4（PE · ZS_Agent 八阶段流水线）** ✅ R7-D 代码完成：读 task.ini → 验证 manifest → ① 固件判定（`FirmwareDetector`：registry 优先/diskpart 回退/6 级冲突 + force_gpt/force_mbr override）② 分区（`BuildPartitionScript` GPT/MBR + `PartitionVerifier` 尾端验证 ESP/FAT/Active）③ 应用 WIM (dism /apply-image) ④ 驱动注入 (dism /image:C: /Add-Driver ZS_Task\drivers /Recurse) ⑤ 引导修复双路径（bcdboot 主路径 + 失败回退）⑥ `UnattendXmlBuilder` 写 C:\Windows\Panther\ ⑦ `SetupCompleteBuilder` 写 C:\Windows\Setup\Scripts\ ⑧ 重启。**仍待**：单元测试（分区尾端验证模拟器 / 固件 6 级冲突模拟器）+ 真机回归。
- **P5（新系统 · 首次登录）** ✅ R7-D 代码完成（仅生成器，真机联调待 Phase 5）：OOBE 被 Autounattend.xml 跳过 → 本地管理员自动登录 → SetupComplete.cmd SYSTEM 权限安装 ZS_Task\software → 优化 → 可配置开关是否删除 ZS_Task → 重启 → 进入桌面。**实现**：`SetupCompleteBuilder` 模板渲染（软件静默安装+系统优化+自清理+自毁）+ `UnattendXmlBuilder`（auto 模式生成 Unattend.xml / manual 返回 null）。

### 6.3 固件判定双重方案 + 分区尾端验证
- **固件判定**：优先 `HKLM\SYSTEM\CurrentControlSet\Control` 注册表快速判断；冲突时 diskpart `Gpt` 列事实为准；两者都未知则**停止装机 + 红色警告**，不盲目分区。
- **分区脚本尾端验证**：GPT 分支完成后必须确认「ESP 卷标 = SYSTEM 或 ESP」+「ESP 文件系统 = FAT 或 FAT32」；MBR 分支必须确认「Active = 是」。不通过则回滚分区表变更并终止。

### 6.4 U 盘制作（四步向导 · 兜底逃生通道，非主链路）
- 仅在无法注入 BCD 启动项（BitLocker / 加密 / 多引导）时使用。
- 输出：「写入 U 盘」/「生成 ISO 镜像」双模式；PE 来源三选一（服务器 PE 版本 / 本地文件 / 在线 URL）；纯 C# IsoBuilder（ISO9660 + Joliet + El Torito 双引导）+ FAT12 efisys.bin 兜底；写盘 4 项校验 + 盘号二次确认 + 红色警示。

### 6.5 工具大全（53 工具 / 10 分类）
分类：磁盘管理 / 系统修复 / 网络工具 / 硬件检测 / 备份还原 / 密码重置 / 数据恢复 / 引导修复 / PE 工具 / 综合工具。清单 Data/Tools/tools.json；U 盘制作勾选「写入内置工具」后拷入 U 盘 ZS_Tools 目录（PE 内离线可用）。

### 6.6 品牌信息对接后台
公开 GET /api/v1/site/info 返回站点信息组（Logo 文字 / 主标题 / 副标题 / 标语 / 官网 / 版本号 / 版权 / 联系 / 关于）。客户端首页渲染。

## 七、数据库概览（35 张表）

zs_users / zs_roles / zs_images / zs_image_sources / zs_image_versions / zs_image_tags / zs_image_tag_relations / zs_download_links / zs_download_logs / zs_download_queue / zs_clients / zs_client_groups / zs_client_versions / zs_tasks / zs_task_records / zs_task_templates / zs_unattend_templates / zs_software / zs_software_categories / zs_software_templates / zs_drivers / zs_scripts / zs_pe_versions / zs_pe_customize / zs_pxe_configs / zs_network_deploy / zs_customers / zs_work_orders / zs_settings / zs_logs / zs_scheduled_tasks / zs_scheduled_task_logs / zs_notifications / zs_recycle_bin / zs_webhook_logs

> 注：zs_client_groups / zs_client_versions / zs_pxe_configs / zs_network_deploy / zs_customers / zs_work_orders 六张表当前无后台菜单；对应控制器 / 路由保留但不在新主链路使用。Phase 3 评估是否永久移除。

## 八、审计官信息（3 维度 × 18 检查项）

本项目必须配独立审计官子 agent，穿透式审计代码「稳定性 / 准确性 / 完整性」。

- **稳定性（6 项）**：① 所有外部命令（diskpart/dism/bcdedit/bcdboot/wpeutil）ExitCode + stderr 检查；② PE 下中文输出 / 长路径 / 非 ASCII 不乱码；③ 长时间操作有进度 + 取消按钮（不卡 UI 线程）；④ 资源释放（句柄 / PowerShell / ISO Mount）；⑤ 超时阈值（写盘 / 下载 / 镜像应用）；⑥ 异常回滚（分区出错不留下半截表）。
- **准确性（6 项）**：① task.ini 16 字段契约双端一致；② zs_manifest.key SHA-256 清单 = 实际文件哈希；③ GPT 分支 ESP 卷标 + FAT/FAT32 尾端验证；④ MBR 分支 Active 标志验证；⑤ registry + diskpart 双方案固件判定（6 级冲突处理策略）；⑥ Autounattend.xml OOBE 跳过 + 本地管理员自动登录字段正确。
- **完整性（6 项）**：① P1 取消 → ZS_Task 全量回滚删除；② P2 取消 → shutdown /a + 清 bootsequence + 还原 BCD；③ P3 10s 倒计时 M 键 / 任意键 → 手动装机控制台菜单；④ P4 任何阶段失败 → 日志落盘 + 停 CMD 不重启；⑤ P5 SetupComplete.cmd 自毁失败 → 下次登录继续清理；⑥ U 盘 / ISO 兜底通道功能与主链路独立（互为备份）。

审计报告统一存放：`docs/superpowers/audit/yyyyMMdd-主题.md`。

## 九、开发注意事项
- 所有界面纯中文为主，可切换纯英文。
- 元素紧凑；按钮不使用 > 符号；字体不加粗。
- 进度条与清单项逐条实时联动，每完成一个步骤立即回填。
- **状态变更必须闭环**：登录 / 退出、BCD 注入 / 取消、重启倒计时 / 中止、PE 逃生键 / 自动、SetupComplete 自毁 / 保留 等入口触发 ↔ 反向退出 ↔ 联动动作三者不能断。
- 禁止硬编码密钥 / Token / 密码；一律使用环境变量或 settings 表。
- 宝塔面板 PHP 配置不得修改；伪静态独立写文件由用户手动贴。
- 所有 API 返回统一 code/message/data/timestamp；数据库操作参数化。
- **不使用任何第三方 PE 发行版的品牌名 / 作者名 / 商标名**作为本项目的标识。仅使用通用微软官方命令：bcdedit / dism / diskpart / bcdboot / wpeutil。
- `别人易语言写的/` 目录仅作技术学习和对比参考，**其中任何文件名、目录名、外部品牌名不得写入六件套和设计文档**。

## 十、项目状态总览（新主链路角度）
| 状态 | 内容 |
|------|------|
| ✅ 设计 v1 | 架构 / 目录 / 命令 / 验证全部落盘 |
| ✅ 后端/后台/数据库骨架 | 磁盘存在（P0-1/4 待修复） |
| ✅ Win_Client + WinPE_Client（WPF 旧链路） | 编译 0 错 0 警 |
| ✅ R7-C 契约对齐 | Startnet.cmd ↔ WinPE_Agent 参数对齐 + task.ini/manifest 解析校验 |
| ✅ R7-D 全链路代码层闭环 | 12 新增 / 6 修改；WinPE_Agent + Windows_Client 编译 0 错 0 警 |
| ✅ WinPE_Agent | 入编 + Git；R7-D 集成 4 个 PE 端 Service + ExecutePipeline 八阶段全打通；**仍待**单元测试 + 真机回归 |
| ✅ P1 Windows 端 ZS_Task 下载 + task.ini/zs_manifest.key | R7-D 代码完成（ZsTaskBuilder + 8 个 Service） |
| ✅ P2 BCD bootsequence 注入 | R7-D 代码完成（BcdInjector + ExecuteR7OfflineBuild） |
| ⚠️ P3 Startnet.cmd | pe_assets/Startnet.cmd 已落地，仍需打包进 boot.wim |
| ✅ P4 八阶段流水线（分区尾端验证 + 固件冲突策略） | R7-D 代码完成（FirmwareDetector/PartitionVerifier/SetupCompleteBuilder/UnattendXmlBuilder + Program.cs 集成）；**仍待**单元测试 + 真机回归 |
| ✅ P5 SetupComplete.cmd 模板渲染 + 写入 | R7-D 生成器完成（SetupCompleteBuilder/UnattendXmlBuilder）；**仍待**真机首次启动联调 |