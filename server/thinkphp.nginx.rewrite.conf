# ThinkPHP 6 伪静态规则 (供宝塔面板手动配置)
# 使用方式：在宝塔面板 -> 网站 -> 设置 -> 伪静态 -> 自定义规则中粘贴以下内容
# 或者放入 /www/server/panel/vhost/rewrite/你的域名/ 目录下

location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
    }
}
