# ZS 装机助手 - 操作指南

> 版本：v0.0.268311-R7（2026-09-02）
> 适用：宝塔面板部署 + 本地联调 + 真实生产环境
> 声明：本文档只记录**已验证可用**的路径；P1~P5 未实现的 Phase 会明确标注「未实现」，不做理论说明。

---

## 一、环境要求

### 1.1 服务器端
- 操作系统：CentOS 7+ / Ubuntu 20+ / Windows Server 2019+
- Web 服务器：Nginx 1.20+（宝塔面板推荐）
- PHP：8.0+（宝塔面板 PHP-8.0 / 8.1 / 8.2 / 8.3 任一）
- MySQL：5.7+（MariaDB 兼容）
- 存储：至少 200 GB（存镜像文件；建议独立数据盘）
- 宝塔面板：已安装（必须）

### 1.2 开发机 / 客户端
- 客户端运行：Windows 10 / 11 x64（.NET 8 Desktop Runtime 才能跑 Windows_Client 框架依赖版）
- 客户端编译：.NET 8 SDK
- PE Boot.wim 打包：Windows ADK + WinPE Addon（仅运维 / 开发机需要，普通用户不需要装）
- 本地联调 PHP：PHP 8.0+（命令行 php -S 即可）

---

## 二、服务器端部署

### 2.1 通过宝塔面板（推荐）

#### 步骤 1 — 创建网站
1. 登录宝塔面板 → 网站 → 添加站点
2. 域名 / IP + 端口（如 8080）
3. PHP 版本选 8.0+
4. 勾选创建 MySQL 数据库（数据库名：zs_installer）
5. 记录：数据库名 / 用户名 / 密码

#### 步骤 2 — 上传源码
上传整个 server/ 目录到网站根目录，或使用脚本：
```powershell
# 本机制作部署包
.\deploy_server.ps1 -Output D:\out\zs_server_deploy.zip
```
将 zip 上传到网站根目录并解压，确保 `public/` 子目录在解压后存在。

#### 步骤 3 — 网站运行目录
⚠️ **需要你操作**：宝塔面板 → 网站 → 设置 → 网站目录 → 运行目录 → 选择 **/public** 并保存。ThinkPHP 入口位于 public/index.php，必须设置运行目录，否则所有 URL 会 404。

#### 步骤 4 — 伪静态（独立规则文件，严禁改宝塔 PHP 配置）
⚠️ **需要你操作**：宝塔面板 → 网站 → 设置 → 伪静态 → 选择「自定义规则」→ 粘贴以下内容（与项目 `server/thinkphp.nginx.rewrite.conf` 文件内容一致）并保存：
```nginx
location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
    }
}
```
> ⚠️ 严禁修改宝塔面板的 PHP 配置文件（如 php.ini / fastcgi.conf / user.ini）。

#### 步骤 5 — 导入数据库
```bash
# 方法 1：宝塔面板 → 数据库 → phpMyAdmin 导入 database/install.sql
# 方法 2：命令行
mysql -u 用户名 -p zs_installer < database/install.sql
```

#### 步骤 6 — 配置 .env
⚠️ **需要你操作**：将 server/.env.example 复制为 server/.env 并编辑：
```ini
APP_DEBUG = false
APP_ENV = production

[DATABASE]
TYPE     = mysql
HOSTNAME = 127.0.0.1
DATABASE = zs_installer
USERNAME = 你的数据库用户名
PASSWORD = 你的数据库密码
HOSTPORT = 3306
CHARSET  = utf8mb4

[JWT]
KEY    = <用 openssl rand -hex 32 生成随机字符串，绝对不要用默认值>
EXPIRE = 86400

[UPLOAD]
MAX_SIZE = 21474836480   ; 20 GB，按需调整
PATH     = /data/images  ; 镜像存储绝对路径，确保磁盘够大
```

#### 步骤 7 — 安装 Composer 依赖
```bash
cd /www/wwwroot/你的站点目录
composer install --no-dev --optimize-autoloader
```

#### 步骤 8 — 设置目录可写
```bash
chmod -R 755 runtime
chmod -R 755 public
chmod 600 .env
```

#### 步骤 9 — 验证后台
访问：`http://域名或IP:端口/admin/login.html`，默认 admin / admin123。
> ⚠️ 登录后第一件事：修改默认 admin 密码。
> ⚠️ 第二件事：执行 Phase 0 基建修复（见 §九），否则「用户管理 / 操作日志」完全不可用。

### 2.2 本地联调（php -S，不依赖宝塔）
```bash
cd server
php -S 127.0.0.1:8001 router.php
# 浏览器打开：
#   介绍页    http://127.0.0.1:8001/index.html
#   后台登录  http://127.0.0.1:8001/admin/login.html
```
客户端默认服务器地址 http://127.0.0.1:8001，改地址在「客户端 → 边框 → 设置」。

---

## 三、管理后台功能（19 项菜单 + 2 项保留文件）

### 3.1 现行菜单（19 项）
| 模块 | 页面 | 功能 | 状态 |
|------|------|------|------|
| 概览 | 控制台 dashboard.html | 统计卡片 / 装机趋势 / 镜像排行 / 最近任务 | ✅ |
| 镜像 | images.html / image-sources.html / image-tags.html | 上传 / 分片 / 校验 / 版本历史 / 标签 / 源 | ✅（基本可用） |
| 无人值守 | unattend.html | 可视化编辑 + JSON 编辑 + 模板校验 + 生成 Autounattend.xml | ✅ |
| 软件驱动 | software.html / software-categories.html / software-templates.html | 软件上传 / 分类 / 装机模板 | ✅ |
| 软件驱动 | drivers.html / scripts.html | 驱动上传 + 脚本管理 | ✅ |
| PE 定制 | pe-versions.html / pe-customize.html | PE 版本 / 壁纸 / 工具 / 启动画面 | ✅ |
| 系统管理 | users.html | 后台用户 CRUD | ⚠️（缺 RoleController，角色分配失效） |
| 系统管理 | settings.html | 系统参数 + 站点信息组（品牌 Logo/标题/标语/版权/联系/关于） | ✅ |
| 系统管理 | logs.html | 操作审计日志 | ⚠️（中间件未挂，空表） |
| 系统管理 | notifications.html / scheduled-tasks.html | 通知 / 定时任务 | ✅ |
| 其他 | reports.html | 装机 / 在线客户端 / 镜像排行（改版后 7 卡片） | ✅ |
| 其他 | webhook-logs.html / recycle-bin.html | Webhook 调用日志 / 回收站恢复 | ✅ |

### 3.2 保留文件（菜单已移除，Phase 3 评估是否删除）
- pxe-configs.html / network-deploys.html：PXE、网络部署页面（旧链路）
- clients.html / client-groups.html / client-versions.html / tasks.html / task-templates.html / customers.html / work-orders.html：**之前轮次已删除**，磁盘不存在，勿再引用。

---

## 四、客户端功能（新主链路分 Phase 说明）

### 4.1 通用：登录 / 品牌
- Windows 客户端：启动 → 边框「登录」→ 打开注册 / 登录对话框 → 用户名 + 密码登录（或注册新账号自动登录）。登录成功后核心功能（一键装机 / U盘 / 绿软）解锁。
- 会话持久化：本地 SessionStore 保存 token + 用户名，重启免重复登录；边框「退出」清除。
- 首页：左上角品牌（Logo 文字 + 主标题 + 副标题 + 标语）+ 边框（设置 / 登录·退出 / 版权 / 版本 / 联系 / 关于）均对接后台站点信息组（GET /api/v1/site/info）。
- 客户端测试账号（仅本地联调）：wuxx80 / a111111。

### 4.2 一键装机（R7 新主链路 · P1~P5）
⚠️ **当前 Phase：Phase 0 基建 + Phase 1 boot.wim 打包 + R7-C 契约对齐已完成；Phase 2 编码待开始**。设计定义如下：

- **P1 Windows · 资源收集（Phase 2 编码 · ❌ 未实现）**：登录 → 选镜像 → 选目标磁盘 → 13 项开关 → 开始装机。从服务器下载：镜像(WIM/ESD)+驱动包+软件包+Autounattend.xml模板+SetupComplete.cmd模板+PE boot.wim+boot.sdi+ZS_Agent.exe。全部写入**非系统分区** `\ZS_Task\` 根目录，生成 task.ini（16 字段）+ zs_manifest.key（全资源 SHA-256 清单）。
- **P2 Windows · 一次性启动（Phase 2 编码 · ❌ 未实现）**：校验 manifest → boot.wim+boot.sdi 写入 ESP → bcdedit 创建 ramdisk 启动项 → `bcdedit /set {bootmgr} bootsequence {ramdisk_guid}` 一次性启动 → 30s 倒计时重启（shutdown /a 可取消）。
- **P3 PE · 逃生窗（⚠️ pe_assets/Startnet.cmd 已落地）**：Startnet.cmd 遍历盘符找 `\ZS_Task\task.ini` → 10s 倒计时（按任意键手动装机，超时继续）。设计 §5.1 内容已实现，仍需打包进 boot.wim Windows\System32\。
- **P4 PE · 八阶段流水线（✅ R7-C 契约对齐已完成 / ⚠️ 缺 4 个 Service）**：ZS_Agent.exe（.NET 8 控制台，单文件自包含）接受 `--auto --task <task.ini> --manifest <zs_manifest.key> --log <pe_log.txt>` 参数，纯本地 DiskPart/DISM/BCDboot/DISM 驱动 流水线。
  - ✅ **已完成**：参数解析 + RunAutoMode 入口 + task.ini 解析（TaskIniParser）+ zs_manifest.key 校验（ManifestValidator）+ ExecutePipeline 7 步主体
  - ⚠️ **仍缺**：固件判定（registry 优先 + diskpart 回退 + 6 级冲突，§6.0）；分区脚本尾端验证（GPT：ESP 卷标 + FAT/FAT32；MBR：Active，§6.1）；SetupComplete.cmd 模板渲染（§6.6）；Unattend.xml 生成（§6.6c）
  - 详见客户端模块调整清单 B.5/B.6
- **P5 新系统 · SetupComplete.cmd（Phase 5 编码 · ❌ 未实现）**：OOBE 跳过 → 本地管理员自动登录 → SetupComplete.cmd SYSTEM 权限装软件 / 优化 / 可选删除 ZS_Task。

#### 4.2.1 R7-C 子轮 · Startnet.cmd ↔ WinPE_Agent 契约调用形式（已对齐）

设计文档 §5.1 定义 Startnet.cmd 末尾的调用形式为：
```batch
"%TASK_ROOT%\ZS_PE_Agent.exe" --auto --task "%TASK_ROOT%\task.ini" --manifest "%TASK_ROOT%\zs_manifest.key" --log "%TASK_ROOT%\pe_log.txt"
```

R7-C 子轮（2026-09-02）已在 WinPE_Agent/Program.cs 完整对齐此契约：
- `--auto` 触发 RunAutoMode 入口
- `--task <path>` 指向 task.ini 文件路径
- `--manifest <path>` 指向 zs_manifest.key 文件路径（可选，未指定则跳过校验）
- `--log <path>` 指定日志文件输出路径（替代默认 agent.log）

退出码约定：
- `0` = 部署成功，Startnet.cmd 让用户按任意键后 wpeutil reboot
- `2` = task.ini 不存在或解析失败，停 cmd 救援
- `3` = manifest 校验失败（文件缺失或哈希不匹配），停 cmd 救援
- `4` = 镜像文件不存在，停 cmd 救援

### 4.3 一键装机（旧链路 · 当前实际行为）
当前 Windows_Client 点击「一键装机」仍走旧链路（创建任务并上报 waiting，真实装机步骤标记「将在 WinPE 执行」）。此链路为 R1~R6 遗留代码，**Phase 2 会替换为 P1+P2 新逻辑**。不要在生产环境使用此旧链路。

### 4.4 U 盘制作（四步向导 · 兜底逃生通道 · ✅ 已实现）
主菜单 → U 盘制作。两种输出模式：
- **写入 U 盘**（需管理员权限 / 需选中 U 盘 / 写盘锁）
- **生成 ISO 镜像**（无需管理员权限、无需 U 盘）

PE 来源三选一：
1. 服务器 PE 版本（peVersions/clientList 接口 → 下载缓存到 C:\ZS_Cache\pe\，断点续传 + 哈希）
2. 本地文件（直接选 .iso / .wim）
3. 在线 URL（任意 HTTP(S) 直接拉取，缓存同上）

安全护栏（写 U 盘模式）：4 项写盘校验 + 盘号二次确认 + 红色警示；执行中可暂停 / 取消；管理员权限不足自动提权重启；多实例互斥锁（Local\ZS_UDiskWrite）。

### 4.5 工具大全（53 工具 / 10 分类 · ✅ 已实现）
分类：磁盘管理 / 系统修复 / 网络工具 / 硬件检测 / 备份还原 / 密码重置 / 数据恢复 / 引导修复 / PE 工具 / 综合工具。每工具 emoji 图标 / exe 路径 / 参数 / 管理员标记。本地 Process.Start 运行，管理员自动提权。U 盘制作勾选「写入内置工具」后合并到 U 盘 ZS_Tools 目录，PE 内离线可用。

### 4.6 绿色软件（✅ 已实现入口）
客户端首页第四张入口卡片 → 列表 / 详情 / 静默安装。具体软件列表和安装参数由后台「软件管理 / 软件模板」统一管理。

---

## 五、公开 API（RESTful JSON · code/message/data/timestamp）

### 5.1 公开接口（无需登录）
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/auth/login    | POST | 管理员 / 用户登录 |
| /api/v1/auth/register | POST | 客户端用户注册（注册即自动登录，普通用户角色） |
| /api/v1/site/info     | GET  | 站点品牌信息（客户端首页渲染） |

### 5.2 需 Bearer Token 的接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/auth/profile | GET | 获取当前用户 |
| /api/v1/auth/logout  | POST | 退出 |
| /api/v1/auth/password | PUT | 改密码 |
| /api/v1/dashboard | GET | 仪表盘统计（任务状态 / 在线客户端） |
| /api/v1/clients/register | POST | 客户端自注册（幂等，自动 approved） |
| /api/v1/clients/heartbeat | POST | 30 秒心跳，在线判定 |
| /api/v1/tasks?client_id=&status=waiting | GET | PE 拉取本机 waiting 任务（旧链路，Phase 3 评估删留） |
| /api/v1/tasks/:id/progress | POST | 进度上报 / 状态流转 |
| /api/v1/tasks/:id/unattend | GET | 旧链路：拉取 UnattendPayload |
| /api/v1/tasks/:id/firstLogon | GET | 旧链路：拉取 SetupComplete.cmd |
| /api/v1/unattendTemplates/:id/generateXml | POST | 按模板生成 Autounattend.xml（P1 下载时复用） |
| /api/v1/images/:id/clientDownload | GET | 镜像流式下载（断点续传 + SHA-256 校验，P1 下载复用） |
| /api/v1/peVersions/clientList | GET | PE 版本客户端列表（U 盘制作 P1 下载复用） |
| /api/v1/peVersions/:id/fileDownload | GET | PE 文件流式下载（断点续传 + Range，缓存 C:\ZS_Cache\pe\） |
| /api/v1/tasks/:id/retry | POST | 任务重试：failed/cancelled → waiting |

### 5.3 API 测试
本地联调跑在 http://127.0.0.1:8001。测试账号 admin/admin123，Token 放入 `Authorization: Bearer <token>`。

---

## 六、客户端构建与发布

### 6.1 构建
```powershell
# 所有项目（WinPE_Agent 暂未入 sln，先单独建）
dotnet restore ZS_Installer.slnx
dotnet build   ZS_Installer.slnx -c Release
```

### 6.2 发布（脚本一键）
```powershell
# 全部发布
.\publish.ps1

# 仅 Windows 端（框架依赖，体积小）
.\publish.ps1 -Windows

# 仅 WinPE 端（自包含，WinPE 内可直接跑）
.\publish.ps1 -WinPE
```

产物：
- publish\Windows_Client\  框架依赖（~0.5MB）：需客户端装 .NET 8 Desktop Runtime
- publish\WinPE_Client\    自包含（~146MB）：无需 .NET 运行时
- WinPE_Agent.exe（Phase 0 入编后加入发布脚本）：.NET 8 AOT 单文件（无需运行时、无 CMD 黑窗残留、无 WinForms 卡死风险）

---

## 七、故障排查

| 问题 | 可能原因 | 排查步骤 |
|------|----------|----------|
| 后台 404 | 运行目录没设为 /public，或伪静态没配 | §2.1 步骤 3/4 重做 |
| 登录 500 | JWT.KEY 空或数据库连不上 | 查 runtime/log/ 下日志；确认 .env 数据库值正确 |
| 管理后台样式加载失败 | Nginx 静态路径错 | 检查是否误删 public/assets/ 目录 |
| 上传镜像失败 | 上传目录无写权限 / upload.max_size 不够 | chmod -R 755 UPLOAD.PATH；php.ini post_max_size / upload_max_filesize（⚠️ 改 php.ini 需在宝塔里谨慎操作） |
| 用户管理页面点角色没反应 | RoleController.php 缺失 + route/admin.php 无 Role 路由 | 做 Phase 0-1 |
| 操作日志永远空 | LogMiddleware 未注册 | 做 Phase 0-4 |
| 客户端报「无法连接服务器」 | 默认服务器地址是 127.0.0.1:8001，部署到公网要改 | 客户端 → 边框 → 设置 → 改为你的服务器地址 |
| 编译客户端报错 | .NET 版本 < 8.0 | `dotnet --version` 检查；装 .NET 8 SDK |
| 保存用户后角色变成空 | P0-2 契约不一致（前端传 role:string 后端读 role_id:int） | 做 Phase 0-2 |

---

## 八、客户端端到端回归测试（旧链路 · 过渡期保留）
```bash
# 需本地 php -S 跑在 http://127.0.0.1:8001
# （部署包脚本已删除 e2e 脚本，新 E2E 将在 Phase 2 完成后重写）
```
> 注意：旧链路（R1~R3/R6）端到端用例均为「PE 联网认领」，与 R7 新主链路不一致。新的端到端将在 Phase 4 流水线完成后重写（包括 P2 BCD 注入模拟、P3 10s 倒计时分流、P4 八阶段虚拟磁盘回归）。

---

## 九、Phase 0 基建修复（登录后台后必须先做！共 6 项，约 12 工时）

| 步骤 | 内容 | 预计工时 |
|------|------|----------|
| P0-1 | 新建 server/app/controller/admin/RoleController.php（CRUD + 分页 + 权限检查）+ route/admin.php 补 Role 路由 | 2h |
| P0-2 | users.html 前端把提交字段 `role` 改为 `role_id`（整数），UserController.php 侧验证为正整数；新增用户/编辑用户页面角色为下拉选择 role_id | 2h |
| P0-3 | WinPE_Agent 项目加入 ZS_Installer.slnx；执行 `git add WinPE_Agent/` | 1h |
| P0-4 | config/middleware.php 注册 AuthMiddleware（路由级别：admin.php 除 auth/login/register/site/info 全部挂）+ LogMiddleware（全局，password 等字段脱敏） | 2h |
| P0-5 | Git 提交 5 个测试脚手架的 delete（如计划不恢复）；如恢复则从历史回档并提交源码 | 2h |
| P0-6 | 整体 Git commit：28 modified + 8 deleted + 6 untracked 全量整理 commit + push | 3h |

> 完成 Phase 0 后再进入 Phase 1~5。禁止跳过 Phase 0 直接开始新编码。