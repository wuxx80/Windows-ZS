# ZS 装机助手 - 操作指南

> 版本：v0.0.268311
> 日期：2026-08-31
> 部署环境：宝塔面板（Nginx + PHP + MySQL）

---

## 一、环境要求

### 服务器端
- 操作系统：CentOS 7+ / Ubuntu 20+ / Windows Server 2019+
- Web 服务器：Nginx 1.20+（宝塔面板推荐）
- PHP：8.0+
- MySQL：5.7+
- 存储空间：至少 200GB（用于存储镜像文件）
- 宝塔面板：已安装

### 客户端
- WinPE 客户端：Windows PE 环境（Win10PE/Win11PE）
- Windows 客户端：Windows 10/11 x64
- .NET 运行时：.NET 8.0

## 二、安装步骤

### 2.1 服务器端部署

#### 通过宝塔面板
1. 登录宝塔面板
2. 创建网站：域名/IP + 端口（如 8080）
3. 设置 PHP 版本为 8.0+
4. 创建 MySQL 数据库（数据库名：zs_installer）
5. 将项目代码上传到网站根目录
6. 导入数据库结构（SQL 文件）
7. 配置 .env 数据库连接信息
8. 设置网站运行目录为 public
9. 配置伪静态规则（使用独立配置文件）

#### 手动部署
```
# 1. 克隆代码
git clone https://github.com/wuxx80/Windows-ZS.git

# 2. 安装依赖
cd Windows-ZS/server
composer install

# 3. 配置环境
cp .env.example .env
# 编辑 .env 配置数据库连接

# 4. 导入数据库
mysql -u root -p zs_installer < database/install.sql
```

### 2.2 配置伪静态（宝塔面板）

需要你操作：
1. 进入宝塔面板 -> 网站 -> 设置 -> 伪静态
2. 选择自定义规则
3. 粘贴以下内容（或使用 server/thinkphp.nginx.rewrite.conf 文件内容）：

```nginx
location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=$1 last;
        break;
    }
}
```

### 2.3 生产环境配置

#### 2.3.1 安全配置

生产环境部署前，必须修改以下配置：

**修改 .env 生产配置**

需要你操作：编辑 `server/.env` 文件，将以下配置改为生产值：

```ini
APP_DEBUG = false          # 生产环境必须关闭调试模式
APP_ENV = production       # 切换为生产环境

[JWT]
KEY = <生成随机密钥>        # 改为随机字符串，不要使用默认值
EXPIRE = 86400             # Token 过期时间（秒）

[UPLOAD]
MAX_SIZE = 21474836480     # 20GB，可按需调整
PATH = /data/images        # 镜像存储路径，确保有足够磁盘空间
```

**生成随机 JWT 密钥**

需要你操作：在服务器上执行以下命令生成随机密钥：

```bash
# Linux
openssl rand -hex 32

# 将输出结果复制到 .env 的 JWT.KEY 字段
```

**设置文件权限**

```bash
# 设置 runtime 目录可写
chmod -R 755 server/runtime
chmod -R 755 server/public

# 确保 .env 文件不可公开访问
chmod 600 server/.env
```

## 三、启动与使用

### 3.1 后台管理
- 访问地址：http://<服务器IP>:端口/admin/login.html
- 默认管理员：admin / admin123
- 功能入口：左侧菜单栏（控制台/镜像管理/无人值守/软件驱动/PE定制/网络部署/系统管理/其他）

### 3.2 客户端登录（用户注册/登录）
- Windows 客户端：启动后在首页点「登录」→ 打开登录/注册对话框 → 输入用户名密码登录（或注册新账号自动登录）；登录成功后核心功能（一键装机/U盘制作/绿色软件）可用；本地保存会话，重启免重复登录
- 客户端测试账号：wuxx80 / a111111
- WinPE 客户端：无人值守 PE 环境自动以 admin/admin123 连接服务器，无需手动输入

### 3.3 后台功能列表

| 模块 | 页面 | 功能 |
|------|------|------|
| 概览 | 控制台 | 统计卡片、装机趋势图、最近任务列表 |
| 镜像管理 | 镜像列表 | 上传/编辑/删除/搜索镜像 |
| 镜像管理 | 镜像源 | 管理远程镜像源 |
| 镜像管理 | 镜像标签 | 标签 CRUD 管理 |
| 无人值守 | 无人值守模板 | Autounattend.xml 模板管理 |
| 软件驱动 | 软件管理 | 软件上传/编辑 |
| 软件驱动 | 软件分类 | 分类管理 |
| 软件驱动 | 软件模板 | 装机软件集合 |
| 软件驱动 | 驱动管理 | 驱动上传/编辑 |
| 软件驱动 | 脚本管理 | 自定义脚本管理 |
| PE 定制 | PE 版本 | PE 版本管理 |
| PE 定制 | PE 定制 | 壁纸/工具定制 |
| 网络部署 | PXE 配置 | PXE 网络启动配置 |
| 网络部署 | 网络部署 | 网络克隆任务管理 |
| 系统管理 | 用户管理 | 后台用户 CRUD |
| 系统管理 | 系统设置 | 系统参数配置 + 站点信息（品牌 Logo/标题/标语/版权/联系/关于） |
| 系统管理 | 操作日志 | 操作审计日志 |
| 系统管理 | 通知消息 | 通知管理 |
| 系统管理 | 定时任务 | 定时任务配置 |
| 其他 | 报表统计 | 装机/客户端/镜像统计 |
| 其他 | Webhook 日志 | Webhook 调用记录 |
| 其他 | 回收站 | 已删除数据恢复/永久删除 |

> 说明：客户端管理/客户端分组/版本管理、任务管理（任务列表/任务模板）、业务管理（客户/工单）已从后台移除（无实际意义），底层任务流程机制保留——客户端通过接口自注册（自动 approved）、创建任务、上报进度，无需后台人工审批。

## 四、API 接口

所有 API 遵循 RESTful 规范，统一响应格式：
```json
{
  "code": 0,
  "message": "操作成功",
  "data": {},
  "timestamp": 1693372800
}
```

### 已认证可用的 API
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/auth/login | POST | 登录 |
| /api/v1/auth/register | POST | 用户注册（客户端公开，注册即自动登录，普通用户角色） |
| /api/v1/site/info | GET | 站点品牌信息（客户端首页，公开无需登录） |
| /api/v1/auth/profile | GET | 获取用户信息 |
| /api/v1/auth/logout | POST | 退出登录 |
| /api/v1/auth/password | PUT | 修改密码 |
| /api/v1/dashboard | GET | 仪表盘数据（含任务实时统计/在线客户端判定） |
| /api/v1/clients/register | POST | 客户端自注册（WinPE/Windows 客户端连接服务器时调用，幂等） |
| /api/v1/clients/heartbeat | POST | 客户端心跳（30 秒/次，返回状态+待执行任务数；90 秒未到=离线） |
| /api/v1/tasks?client_id=&status=waiting | GET | PE 端拉取本机待执行任务（续装闭环） |
| /api/v1/tasks/:id/progress | POST | 进度上报/状态流转（waiting→running→completed/failed） |
| /api/v1/tasks/:id/unattend | GET | 获取无人值守应答 XML（PE 写入 C:\Windows\Panther\unattend.xml） |
| /api/v1/tasks/:id/firstLogon | GET | 获取首次登录脚本 SetupComplete.cmd（装软件/优化） |
| /api/v1/unattendTemplates/:id/generateXml | POST | 按模板配置生成真实 Autounattend.xml |
| /api/v1/softwareTemplates/:id | GET | 软件模板详情（首次登录脚本引用） |
| /api/v1/images/:id/clientDownload | GET | 客户端直接流式下载镜像（断点续传 + 鉴权；PE/Windows 装机场内拉取） |
| /api/v1/peVersions/clientList | GET | PE 版本客户端列表（U 盘制作选择 PE 源） |
| /api/v1/peVersions/:id/fileDownload | GET | PE 文件流式下载（断点续传 + Range，客户端缓存至 C:\ZS_Cache\pe\） |
| /api/v1/tasks/:id/retry | POST | 重试任务（failed/cancelled → waiting，复用原任务订单） |

详细 API 文档见 设计文档 > Web后台详细设计.md

### API测试
所有API使用Bearer Token认证，测试账号：admin/admin123

已验证的API端点：
- POST /api/v1/auth/login - 登录
- GET /api/v1/dashboard - 仪表盘
- GET /api/v1/images - 镜像列表
- GET /api/v1/software - 软件列表
- GET /api/v1/tasks - 任务列表
- GET /api/v1/clients - 客户端列表
- GET /api/v1/users - 用户列表
- GET /api/v1/imageTags - 镜像标签
- GET /api/v1/softwareCategories - 软件分类

## 五、常见问题

### 5.1 后台无法访问
- 检查是否已配置伪静态规则
- 检查 PHP 版本是否为 8.0+
- 检查网站运行目录是否设置为 public
- 检查数据库连接配置是否正确

### 5.2 登录失败
- 确认管理员账号：admin / admin123
- 检查数据库是否已导入种子数据
- 检查 Token 认证中间件是否正常

### 5.3 页面空白或加载失败
- 检查浏览器控制台是否有 404/500 错误
- 检查 API 接口是否返回正确数据
- 检查文件编码是否为 UTF-8 无 BOM

## 六、构建与打包

### 6.0 服务器端部署包制作

项目提供部署包制作脚本 `deploy_server.ps1`，可将 `server` 目录打包为可上传的 zip 部署包：

```powershell
# 在项目根目录执行
.\deploy_server.ps1                                    # 生成 zs_server_deploy.zip
.\deploy_server.ps1 -Output D:\out\zs_server.zip        # 指定输出路径
```

部署包包含：app/config/route/public、composer.json、composer.lock、think、.env.example、thinkphp.nginx.rewrite.conf，并自动排除 runtime/uploads/.env 等运行时可再生成或敏感文件，同时内置《部署说明.txt》。部署包不含 `database/install.sql`，需从源码获取。

### 6.0.1 客户端端到端回归测试

```powershell
# 需服务器运行于 http://127.0.0.1:8001
php server\e2e_winpe_test.php
```

覆盖流程（闭环 36 项）：登录 → 客户端自注册（幂等）→ 审批 → 创建装机任务（Windows 置 waiting，含 client_id 校验）→ PE 拉取 waiting → waiting 转 running 认领（乐观锁并发保护 + 身份校验）→ 进度上报（分区/部署/修复引导/完成）→ 任务详情/日志校验 → 非法参数拦截 → 取消 waiting/running → 心跳 → 客户端列表在线判定 → 无人值守 XML 生成（含 FirstLogonCommands）→ 任务关联无人值守模板 → options 契约 13 键完整 → 镜像下载/断点续传 → SHA256 校验 → 磁盘特征持久化（disk_index/disk_size/disk_model）→ 失败重试复用原任务（failed→waiting）→ 认领并发冲突拦截。

### 6.1 客户端构建

#### 环境要求
- .NET 8.0 SDK
- Windows 10/11 或 Windows PE 环境

#### 构建 WinPE 客户端
```powershell
cd D:\Users\Desktop\Windows-ZS\WinPE_Client
dotnet restore
dotnet build -c Release
```

#### 构建 Windows 客户端
```powershell
cd D:\Users\Desktop\Windows-ZS\Windows_Client
dotnet restore
dotnet build -c Release
```

#### 构建所有客户端
```powershell
cd D:\Users\Desktop\Windows-ZS
dotnet restore ZS_Installer.sln
dotnet build ZS_Installer.sln -c Release
```

### 6.2 客户端发布

#### 使用发布脚本（推荐）
项目提供了统一的发布脚本，可在项目根目录执行：

```powershell
# 发布所有客户端
.\publish.ps1

# 仅发布 WinPE 客户端
.\publish.ps1 -WinPE

# 仅发布 Windows 客户端
.\publish.ps1 -Windows

# 查看帮助
.\publish.ps1 -Help
```

#### 手动发布

```powershell
# WinPE 客户端（自包含部署，适用于 WinPE 环境）
# 自包含部署不需要目标机器安装 .NET 运行时，但体积较大（约 60-80MB）
dotnet publish WinPE_Client\WinPE_Client.csproj -c Release -r win-x64 --self-contained true -o publish\WinPE_Client

# Windows 客户端（框架依赖部署）
# 框架依赖部署需要目标机器安装 .NET 8.0 运行时，但体积较小（约 200KB）
dotnet publish Windows_Client\Windows_Client.csproj -c Release -r win-x64 --self-contained false -o publish\Windows_Client
```

#### 发布输出说明

| 发布产物 | 类型 | 大小 | 运行时依赖 |
|----------|------|------|-----------|
| publish/WinPE_Client/ | 自包含 | ~60-80MB | 无需 .NET 运行时 |
| publish/Windows_Client/ | 框架依赖 | ~200KB | 需要 .NET 8.0 运行时 |

### 6.3 WinPE 环境集成部署

需要你操作：将 WinPE 客户端集成到 WinPE 镜像中：

1. 使用发布脚本发布 WinPE 客户端（自包含模式）
2. 将 `publish/WinPE_Client/` 目录复制到 WinPE 镜像的 `Program Files\ZS_Installer\` 目录
3. 在 WinPE 启动脚本中（如 `startnet.cmd`）添加自动启动：

```batch
@echo off
cd /d "X:\Program Files\ZS_Installer"
start /wait WinPE_Client.exe --server http://<服务器IP>:端口
```

4. 重新打包 WinPE 镜像（WIM 文件）

### 6.4 Windows 客户端部署

需要你操作：在目标 Windows 系统上安装：

1. 确保目标系统已安装 .NET 8.0 运行时
   - 下载地址：https://dotnet.microsoft.com/download/dotnet/8.0
   - 选择 `.NET Desktop Runtime 8.0.x (x64)` 版本
2. 将发布后的 `Windows_Client.exe` 及依赖文件复制到目标系统
3. 直接运行 `Windows_Client.exe`

### 6.5 一键装机六步向导使用

需要你操作：在主界面点击「一键装机」打开六步向导子窗口：

1. **① 环境检测**：自动检测 9 项（网络/服务器/认证/PE 识别/磁盘控制器/磁盘/启动模式/安全启动/兼容性），逐项实时回填；失败项可点「修复」，致命/警告问题汇总后支持「全部修复 / 强制继续」
2. **② 选择镜像**：切换镜像来源（本地服务器/远程URL/本地缓存/自定义源），支持搜索、格式/系统类型筛选、分页；未缓存镜像标记「⬇ 需下载」
3. **③ 目标磁盘**：选择安装磁盘，冲突实时提示（空间不足/GPT-MBR 不匹配/PE 运行盘）；分区方案可选「自动分区 / 自定义分区 / 保留现有分区」
4. **④ 安装选项**：九宫格选项（引导修复/无人值守/驱动注入/自动分区/备份数据/装软件/系统优化/激活/保留数据），自动联动；下方扩展配置面板选择模板/驱动包/优化方案/备份位置
5. **⑤ 确认**：汇总镜像/磁盘/选项/预计耗时/完成后行为；冲突分级展示，确认后开始装机
6. **⑥ 执行**：进度条 + 步骤清单 + 实时日志三层联动，支持暂停/继续/取消；完成后进入重启倒计时

> 双端差异：WinPE 端执行真实装机（分区/部署/驱动/引导）；Windows 端仅创建装机任务并上报进度，实际装机在 WinPE 环境继续执行。

### 6.6 无人值守一键装机完整流程（Windows 下单 → WinPE 续装 → 全自动完成）

需要你操作：按以下顺序即可完成一次无人值守装机：

1. **后台准备（一次性）**：上传系统镜像 → 配置无人值守模板 → 配置软件模板（客户端设备自注册自动 approved，无需后台审批）
2. **Windows 端下单**：登录客户端 → 一键装机 → 六步向导配置完成后点「开始装机」→ 任务状态为 waiting → 完成页提示「重启进入 WinPE 自动继续」
3. **进入 WinPE**：重启后从 U 盘 / PE 恢复分区 / PXE 网络引导进入 WinPE → ZS 装机助手自动启动
4. **WinPE 自动续装**：PE 端自动连接服务器并检测到本机 waiting 任务 → 首页「待执行任务」卡片点「立即继续」→ 自动预填镜像/磁盘/选项 → 全自动执行：下载镜像 → 分区 → 部署 → 注入驱动 → 修复引导 → 写入无人值守应答 → 生成首次登录脚本 → 上报完成 → 自动重启
5. **新系统自动完成**：无人值守应答自动跳过 OOBE → 首次登录脚本自动静默安装软件 + 应用优化
6. **后台全程可视**：任务列表实时进度/步骤/日志，客户端列表显示在线/离线（30 秒心跳）

> 双端心跳：WinPE / Windows 客户端均每 30 秒上报心跳，后台据此判定在线状态；检测到 waiting 任务时 PE 端首页出现「待执行任务」卡片。

### 6.7 U 盘制作（四步向导 · 输出方式：写U盘 / 生成ISO）

需要你操作：在主界面点击「U 盘制作」打开制作窗口（Windows / WinPE 双端一致）：

**输出方式选择（步骤 0）**
- 「写入 U 盘」：沿用四步向导，将 PE 写入 U 盘（需管理员权限、需选中 U 盘）
- 「生成 ISO 镜像」：无需选中 U 盘、无需管理员权限，将所选 PE 打包成可启动 ISO（可勾选写入装机助手客户端与内置工具），选择输出路径后点「开始生成」

**① 选择 U 盘**（写U盘模式）：枚举可移动磁盘，仅显示满足条件（非系统盘/容量足够）的 U 盘；自动检测类型（USB/读卡器）

**② 选择 PE 版本**：三种来源任选：
- 服务器 PE 版本：从服务器拉取 PE 版本列表（客户端接口 peVersions/clientList），文件缓存至 `C:\ZS_Cache\pe\`（断点续传 + 哈希校验）
- 本地文件：直接选择本地 `.iso/.wim` 文件
- 在线 URL：输入 PE 镜像的 HTTP(S) 直接下载链接（不依赖服务器托管），自动下载并缓存

**③ 选项**：可勾选「写入装机助手客户端」「应用 PE 定制」「格式化还原」「写入内置工具」（勾选后自动合并本地 Tools 目录与 ZS_Cache/tools 缓存写入 U 盘/ISO 的 ZS_Tools 目录，PE 内离线可用；未检测到工具文件时提示跳过且不中断）

**④ 确认执行**：写U盘模式为盘号二次确认 + 红色警示 → 执行格式化 → 写引导 → 拷贝 PE/客户端/工具 → 完成后校验；生成ISO模式仅确认输出路径后开始打包

> 安全护栏（写U盘模式）：写盘 4 项校验 + 盘号二次确认 + 红色警示；执行过程可暂停/取消；管理员权限不足自动提权重启；多实例互斥锁（Local\ZS_UDiskWrite）防并发写盘。失败/取消/成功三态结果区分显示。
>
> 生成 ISO：使用内置纯 C# 生成器（ISO9660 + Joliet + El Torito），支持 UEFI + Legacy 双引导；PE 缺少 UEFI 引导镜像时自动用 bootx64.efi 生成 FAT12 efisys.bin 兜底。逻辑自测已通过，并用在线拉取的 ~340MB PE（GitHub WindowsPEBasic）实测生成 ~337MB 可引导 ISO 成功。

### 6.8 工具大全

需要你操作：在主界面点击「工具大全」打开工具列表窗口：

- **53 个工具 / 10 分类**：磁盘管理 / 系统修复 / 网络工具 / 硬件检测 / 备份还原 / 密码重置 / 数据恢复 / 引导修复 / PE 工具 / 综合工具
- 每工具含 emoji 图标 / exe 路径 / 参数 / 管理员标记；本地清单（Data/Tools/tools.json）+ 服务器双源
- 双击运行：本地内置工具经 Process.Start 运行，需管理员权限的工具自动检测并提权；在线工具从服务器下载后执行
- U 盘制作勾选「写入内置工具」后，工具已拷入 U 盘 ZS_Tools 目录，PE 内离线可直接使用

## 七、故障排查

### 7.1 日志位置
- 后台操作日志：系统管理 > 操作日志
- PHP 错误日志：runtime/log/ 目录
- PHP 错误日志：宝塔面板 > 日志

### 7.2 常用命令
```bash
# 查看 PHP 错误日志
tail -f server/runtime/log/*.log

# 清理缓存
cd server && php think clear
```

### 7.3 客户端常见问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 客户端无法连接服务器 | 服务器地址配置错误 | 检查 `--server` 参数是否正确 |
| 登录失败 | 账号密码错误 | 检查后台用户账号状态 |
| 镜像列表为空 | 服务器未上传镜像 | 在后台管理上传镜像 |
| 部署任务卡住 | 网络不稳定或磁盘空间不足 | 检查服务器日志和磁盘空间 |
| WinPE 客户端无法启动 | 缺少运行库或权限不足 | 确认使用自包含发布模式 |
| 部署进度不更新 | 客户端与服务器通信异常 | 检查网络连接和 Token 是否过期 |

## 八、部署清单

### 8.1 服务器部署清单

- [ ] 创建 MySQL 数据库并导入 install.sql
- [ ] 上传项目代码到服务器
- [ ] 运行 `composer install` 安装 PHP 依赖
- [ ] 配置 `.env` 文件（数据库连接、JWT密钥、存储路径）
- [ ] 设置网站运行目录为 `public`
- [ ] 配置 Nginx 伪静态规则
- [ ] 设置 `server/runtime` 目录可写
- [ ] 生产环境关闭 `APP_DEBUG`
- [ ] 生产环境更换 JWT 密钥
- [ ] 验证后台登录页可访问
- [ ] 验证 API 接口正常返回

### 8.2 客户端发布清单

- [ ] 确认 .NET 8.0 SDK 已安装
- [ ] 确认代码已同步最新版本
- [ ] 运行 `dotnet restore` 还原依赖
- [ ] 运行 `dotnet build -c Release` 构建
- [ ] 发布 WinPE 客户端（自包含）
- [ ] 发布 Windows 客户端（框架依赖）
- [ ] 验证发布产物可运行
- [ ] 测试客户端-服务器通信

### 8.3 生产环境安全检查清单

- [ ] APP_DEBUG = false
- [ ] JWT 密钥已从默认值改为随机值
- [ ] 默认管理员密码已修改
- [ ] .env 文件不在 web 可访问目录
- [ ] runtime 目录不可 web 直接访问
- [ ] 上传目录权限正确设置
- [ ] 数据库连接使用最小权限账户
- [ ] Nginx 正确配置，不暴露敏感路径