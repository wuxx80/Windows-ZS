# ZS 装机助手 · 操作指南

> 版本：v2.1.0
> 日期：2026-08-30
> 部署环境：宝塔面板（Nginx + PHP + MySQL）

---

## 一、环境要求

### 服务器端
- 操作系统：CentOS 7+ / Ubuntu 20+ / Windows Server 2019+
- Web 服务器：Nginx 1.20+（宝塔面板推荐）
- PHP：8.0+
- MySQL：5.7+
- 存储空间：至少 200GB（用于存储镜像文件）
- 宝塔面板：已安装

### 客户端
- WinPE 客户端：Windows PE 环境（Win10PE/Win11PE）
- Windows 客户端：Windows 10/11 x64
- .NET 运行时：.NET 6.0 或 .NET 8.0

## 二、安装步骤

### 2.1 服务器端部署

#### 通过宝塔面板
1. 登录宝塔面板
2. 创建网站：域名/IP + 端口（如 8080）
3. 设置 PHP 版本为 8.0+
4. 创建 MySQL 数据库
5. 将项目代码上传到网站根目录
6. 导入数据库结构（SQL 文件）
7. 配置 `.env` 数据库连接信息
8. 设置网站运行目录为 `public`
9. 配置伪静态规则（ThinkPHP/Laravel）

#### 手动部署
```bash
# 1. 克隆代码
git clone https://github.com/wuxx80/Windows-ZS.git

# 2. 安装依赖
cd Windows-ZS
composer install

# 3. 配置环境
cp .env.example .env
# 编辑 .env 配置数据库连接

# 4. 导入数据库
mysql -u root -p zs_installer < database/install.sql

# 5. 设置权限
chmod -R 755 storage/
chmod -R 755 public/uploads/
```

### 2.2 客户端部署

#### WinPE 客户端
1. 从后台下载 PE 定制 ISO
2. 使用 Rufus/Ventoy 写入 U 盘
3. U 盘启动进入 PE 环境
4. 客户端自动启动

#### Windows 客户端
1. 从后台下载客户端安装包
2. 运行安装程序
3. 输入服务器地址和认证信息
4. 启动客户端

## 三、配置说明

### 3.1 系统配置
- 站点名称：在后台 系统管理 > 系统配置 > 基本设置 中修改
- 存储路径：在 系统配置 > 存储设置 中配置各存储目录
- 通知方式：在 系统配置 > 通知设置 中配置通知渠道

### 3.2 镜像存储
- 镜像文件默认存储在 `/storage/images/` 目录
- 可通过系统配置修改存储路径
- 建议使用独立磁盘或挂载远程存储

### 3.3 客户端认证
- 客户端注册后需在后台审核通过
- 可在 客户端管理 > 审核 中批量操作
- 认证方式：MAC 地址 + 机器码绑定

## 四、启动与使用

### 4.1 后台管理
- 访问地址：`http://<服务器IP>:8080/admin`
- 默认管理员：admin（首次登录后请修改密码）
- 功能入口：左侧菜单栏

### 4.2 一键装机（客户端）
1. 客户端启动 → 自动环境检测
2. 选择镜像（服务器/远程URL/本地缓存）
3. 选择目标磁盘
4. 配置安装选项（引导修复/无人值守/驱动/软件等）
5. 确认 → 开始装机
6. 实时监控进度
7. 完成 → 重启系统

### 4.3 镜像管理（后台）
1. 进入 镜像管理
2. 点击 上传镜像 或 添加远程镜像
3. 填写镜像信息
4. 确认上传/添加

## 五、API 接口

所有 API 遵循 RESTful 规范，统一响应格式：
```json
{
  "code": 0,
  "message": "操作成功",
  "data": {},
  "request_id": "req_xxx",
  "timestamp": 1693372800
}
```

详细 API 文档见 设计文档 > Web后台详细设计.md

## 六、构建与打包

### 6.1 客户端构建
```bash
# WinPE 客户端
dotnet publish src/ZsInstaller.PE -c Release -o publish/pe

# Windows 客户端
dotnet publish src/ZsInstaller.Win -c Release -o publish/win
```

### 6.2 PE 构建（后台）
1. 进入 PE 定制模块
2. 选择基础 PE 版本
3. 配置壁纸/工具/驱动
4. 点击 构建 PE
5. 下载生成的 ISO 文件

## 七、常见问题

### 7.1 客户端无法连接服务器
- 检查服务器地址和端口是否正确
- 检查防火墙是否开放端口
- 检查客户端网络是否正常
- 检查服务器是否在线

### 7.2 镜像上传失败
- 检查文件格式是否支持（WIM/ISO/ESD/SWM/GHO）
- 检查文件大小是否超过限制（默认 20GB）
- 检查磁盘空间是否充足
- 检查 PHP 上传限制配置

### 7.3 装机任务失败
- 查看任务详情中的错误信息
- 检查客户端日志
- 确认镜像文件是否完整
- 检查磁盘空间是否充足
- 尝试重试任务

## 八、故障排查

### 8.1 日志位置
- 后台操作日志：系统管理 > 日志审计
- 任务日志：装机任务 > 详情 > 实时日志
- 客户端日志：C:\ZS_Installer\logs\
- PHP 错误日志：宝塔面板 > 日志

### 8.2 常用命令
```bash
# 重启 PHP 服务
systemctl restart php-fpm

# 查看实时日志
tail -f /www/wwwroot/zs-installer/runtime/log/*.log

# 检查磁盘空间
df -h

# 清理缓存
php think clear
```
