# ZS 装机助手 · 详细设计文档

> 版本：v2.1.0
> 日期：2026-08-30
> 后端：PHP + MySQL（宝塔面板）
> 客户端：WinPE (C# WPF) + Windows (C# WPF)
> 协议：JSON API / WebSocket

---

## 目录

1. [系统架构](#1-系统架构)
2. [数据库设计](#2-数据库设计)
3. [API 接口设计](#3-api-接口设计)
4. [项目目录结构](#4-项目目录结构)
5. [Web 后台菜单设计](#5-web-后台菜单设计)
6. [客户端 UI 设计](#6-客户端-ui-设计)
7. [开发计划](#7-开发计划)

---

## 1. 系统架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           部署环境                                    │
│                   宝塔面板 (Nginx/Apache)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────────────────────┐                           │
│  │          Web 管理后台 (PHP)            │                           │
│  │  控制台 · 镜像管理 · 客户端管理        │                           │
│  │  装机任务 · 无人值守 · 网络部署        │                           │
│  │  PE定制 · 客户工单 · 系统管理          │                           │
│  └──────────────────┬────────────────────┘                           │
│                     │ RESTful API                                    │
│                     ▼                                                │
│  ┌───────────────────────────────────────┐                           │
│  │          API 服务层 (PHP)              │                           │
│  │  认证 / 镜像 / 任务 / 客户端 / 配置     │                           │
│  └──────────────────┬────────────────────┘                           │
│                     │                                                │
│         ┌───────────┴───────────┐                                    │
│         ▼                       ▼                                    │
│  ┌──────────────┐     ┌──────────────────┐                           │
│  │   MySQL 数据库│     │   文件存储         │                           │
│  │   (宝塔MySQL) │     │   ISO/WIM/PE/驱动  │                           │
│  └──────────────┘     └──────────────────┘                           │
│                                                                      │
│  ┌───────────────────────────────────────────────────────┐           │
│  │               网络部署层 (PXE)                          │           │
│  │  DHCP · TFTP · HTTP · iPXE                            │           │
│  └───────────────────────────────────────────────────────┘           │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                          客户端层                                      │
│                                                                      │
│  ┌────────────────────────┐    ┌────────────────────────┐            │
│  │   WinPE 客户端          │    │   Windows 客户端        │            │
│  │   (C# WPF)              │    │   (C# WPF)             │            │
│  │                         │    │                        │            │
│  │  一键装机 · U盘制作     │    │  镜像下载/缓存          │            │
│  │  工具大全 · 绿色软件    │    │  任务管理 · 一键PE      │            │
│  └────────────────────────┘    └────────────────────────┘            │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈选型

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 后端语言 | PHP | 8.0+ | 宝塔面板原生支持 |
| 后端框架 | ThinkPHP 6 / Laravel | 8+ | 推荐 ThinkPHP，轻量成熟 |
| 数据库 | MySQL | 5.7+ | 宝塔面板内置 |
| 缓存 | Redis | 6+ | 可选，提升性能 |
| Web 服务器 | Nginx | 1.20+ | 宝塔面板默认 |
| 前端后台 | Bootstrap 5 + jQuery | - | 兼容性好，宝塔生态 |
| 后台 UI 框架 | Layui | - | 国产友好，成熟稳定 |
| API 协议 | RESTful JSON | - | 统一接口规范 |
| 文件存储 | 本地文件系统 | - | 宝塔文件管理 |
| PXE 服务 | dnsmasq + tftp-hpa | - | 可选，Linux 部署 |
| WinPE 客户端 | C# WPF (.NET 6/8) | - | 开发效率高 |
| Windows 客户端 | C# WPF (.NET 6/8) | - | 桌面应用框架 |

---

## 2. 数据库设计

### 2.1 数据库命名规范

- 表名：`zs_` 前缀，小写蛇形
- 字段：小写蛇形
- 主键：`id`（INT UNSIGNED AUTO_INCREMENT）
- 时间戳：`created_at`、`updated_at`

### 2.2 表结构总览

| 表名 | 说明 | 归属模块 |
|------|------|----------|
| `zs_users` | 管理后台用户 | 系统管理 |
| `zs_roles` | 角色权限 | 系统管理 |
| `zs_images` | 镜像管理 | 镜像管理 |
| `zs_image_sources` | 镜像源管理 | 镜像管理 |
| `zs_clients` | 客户端管理 | 客户端管理 |
| `zs_client_groups` | 客户端分组 | 客户端管理 |
| `zs_client_versions` | 客户端版本 | 客户端管理 |
| `zs_tasks` | 装机任务 | 装机任务 |
| `zs_task_records` | 任务记录/日志 | 装机任务 |
| `zs_task_templates` | 任务模板 | 装机任务 |
| `zs_unattend_templates` | 无人值守模板 | 无人值守 |
| `zs_software` | 软件管理 | 扩展功能 |
| `zs_software_categories` | 软件分类 | 扩展功能 |
| `zs_software_templates` | 装机软件模板 | 扩展功能 |
| `zs_drivers` | 驱动管理 | 扩展功能 |
| `zs_scripts` | 脚本管理 | 扩展功能 |
| `zs_pe_versions` | PE 版本管理 | PE 定制 |
| `zs_pe_customize` | PE 定制配置 | PE 定制 |
| `zs_pxe_configs` | PXE 配置 | 网络部署 |
| `zs_network_deploy` | 网络部署任务 | 网络部署 |
| `zs_customers` | 客户信息 | 客户管理 |
| `zs_work_orders` | 维修工单 | 客户管理 |
| `zs_settings` | 系统配置 | 系统管理 |
| `zs_logs` | 操作日志 | 系统管理 |

### 2.3 核心表结构

#### zs_users（管理后台用户）

```sql
CREATE TABLE `zs_users` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(255) NOT NULL COMMENT '密码hash',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `role_id` int unsigned DEFAULT NULL COMMENT '角色ID',
  `status` tinyint(1) DEFAULT '1' COMMENT '状态 1=启用 0=禁用',
  `last_login_ip` varchar(45) DEFAULT NULL COMMENT '最后登录IP',
  `last_login_at` datetime DEFAULT NULL COMMENT '最后登录时间',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理后台用户';
```

#### zs_images（镜像管理）

```sql
CREATE TABLE `zs_images` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(200) NOT NULL COMMENT '镜像名称',
  `file_name` varchar(255) NOT NULL COMMENT '文件名',
  `file_path` varchar(500) NOT NULL COMMENT '存储路径',
  `file_size` bigint unsigned DEFAULT '0' COMMENT '文件大小(字节)',
  `file_hash` varchar(128) DEFAULT NULL COMMENT '文件Hash (SHA256)',
  `format` enum('wim','iso','esd','swm','gho') NOT NULL COMMENT '镜像格式',
  `os_type` varchar(50) DEFAULT NULL COMMENT '系统类型: Windows 10/11/7',
  `os_edition` varchar(100) DEFAULT NULL COMMENT '版本: Pro/Enterprise/LTSC',
  `os_arch` enum('x64','x86','arm64') DEFAULT 'x64' COMMENT '架构',
  `os_version` varchar(50) DEFAULT NULL COMMENT '系统版本号',
  `language` varchar(20) DEFAULT 'zh-CN' COMMENT '语言',
  `description` text COMMENT '描述',
  `tags` varchar(500) DEFAULT NULL COMMENT '标签(逗号分隔)',
  `source_id` int unsigned DEFAULT NULL COMMENT '镜像源ID',
  `source_type` enum('upload','download','import','sync') DEFAULT 'upload' COMMENT '来源',
  `download_count` int unsigned DEFAULT '0' COMMENT '下载次数',
  `install_count` int unsigned DEFAULT '0' COMMENT '装机次数',
  `status` tinyint(1) DEFAULT '1' COMMENT '状态 1=可用 0=禁用',
  `version` int unsigned DEFAULT '1' COMMENT '版本号',
  `parent_id` int unsigned DEFAULT NULL COMMENT '父镜像ID',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_format` (`format`),
  KEY `idx_os_type` (`os_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='镜像管理';
```

#### zs_clients（客户端管理）

```sql
CREATE TABLE `zs_clients` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '客户端名称',
  `client_id` varchar(64) NOT NULL COMMENT '客户端唯一标识',
  `client_type` enum('winpe','windows') NOT NULL COMMENT '客户端类型',
  `version` varchar(20) DEFAULT NULL COMMENT '客户端版本',
  `mac_address` varchar(17) DEFAULT NULL COMMENT 'MAC地址',
  `ip_address` varchar(45) DEFAULT NULL COMMENT 'IP地址',
  `hostname` varchar(100) DEFAULT NULL COMMENT '主机名',
  `group_id` int unsigned DEFAULT NULL COMMENT '分组ID',
  `status` enum('online','offline','pending','disabled') DEFAULT 'pending' COMMENT '状态',
  `auth_status` enum('approved','pending','rejected') DEFAULT 'pending' COMMENT '审核状态',
  `last_online_at` datetime DEFAULT NULL COMMENT '最后在线时间',
  `cpu_info` varchar(200) DEFAULT NULL COMMENT 'CPU信息',
  `memory_total` bigint unsigned DEFAULT NULL COMMENT '内存总量(字节)',
  `os_version` varchar(100) DEFAULT NULL COMMENT '操作系统版本',
  `tags` varchar(500) DEFAULT NULL COMMENT '标签',
  `remark` varchar(500) DEFAULT NULL COMMENT '备注',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_client_id` (`client_id`),
  KEY `idx_status` (`status`),
  KEY `idx_group_id` (`group_id`),
  KEY `idx_auth_status` (`auth_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='客户端管理';
```

#### zs_tasks（装机任务）

```sql
CREATE TABLE `zs_tasks` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(200) NOT NULL COMMENT '任务名称',
  `task_type` enum('install','backup','restore','repair','custom') DEFAULT 'install' COMMENT '任务类型',
  `image_id` int unsigned DEFAULT NULL COMMENT '镜像ID',
  `client_id` int unsigned DEFAULT NULL COMMENT '客户端ID',
  `target_disk` varchar(50) DEFAULT NULL COMMENT '目标磁盘',
  `target_partition` varchar(50) DEFAULT NULL COMMENT '目标分区',
  `options` text COMMENT '任务选项(JSON)',
  `unattend_template_id` int unsigned DEFAULT NULL COMMENT '无人值守模板ID',
  `software_template_id` int unsigned DEFAULT NULL COMMENT '软件模板ID',
  `priority` enum('high','medium','low') DEFAULT 'medium' COMMENT '优先级',
  `schedule_type` enum('immediate','scheduled','trigger') DEFAULT 'immediate' COMMENT '调度类型',
  `schedule_at` datetime DEFAULT NULL COMMENT '定时执行时间',
  `status` enum('pending','running','completed','failed','cancelled','paused') DEFAULT 'pending' COMMENT '状态',
  `progress` tinyint unsigned DEFAULT '0' COMMENT '进度 0-100',
  `current_step` varchar(100) DEFAULT NULL COMMENT '当前步骤',
  `error_message` text COMMENT '错误信息',
  `retry_count` tinyint unsigned DEFAULT '0' COMMENT '重试次数',
  `max_retry` tinyint unsigned DEFAULT '3' COMMENT '最大重试次数',
  `created_by` int unsigned DEFAULT NULL COMMENT '创建者',
  `started_at` datetime DEFAULT NULL COMMENT '开始时间',
  `completed_at` datetime DEFAULT NULL COMMENT '完成时间',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_status` (`status`),
  KEY `idx_client_id` (`client_id`),
  KEY `idx_image_id` (`image_id`),
  KEY `idx_task_type` (`task_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='装机任务';
```

#### zs_unattend_templates（无人值守模板）

```sql
CREATE TABLE `zs_unattend_templates` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '模板名称',
  `description` text COMMENT '模板描述',
  `is_default` tinyint(1) DEFAULT '0' COMMENT '是否默认',
  `config_data` json NOT NULL COMMENT '配置数据(JSON)',
  `created_by` int unsigned DEFAULT NULL COMMENT '创建者',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='无人值守模板';

-- config_data JSON 结构示例:
-- {
--   "general": { "timezone": "China Standard Time", "language": "zh-CN" },
--   "user_account": { "username": "Admin", "auto_login": true },
--   "network": { "dhcp": true, "workgroup": "WORKGROUP" },
--   "disk": { "auto_partition": true, "partition_scheme": "gpt" },
--   "components": { "disable_defender": true, "disable_uac": true },
--   "customization": { "taskbar_align": "left" },
--   "first_logon": { "install_software": true },
--   "security": { "uac_level": "never_notify" }
-- }
```

#### zs_software（软件管理）

```sql
CREATE TABLE `zs_software` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '软件名称',
  `category_id` int unsigned DEFAULT NULL COMMENT '分类ID',
  `file_name` varchar(255) NOT NULL COMMENT '文件名',
  `file_path` varchar(500) NOT NULL COMMENT '存储路径',
  `file_size` bigint unsigned DEFAULT '0' COMMENT '文件大小',
  `file_hash` varchar(128) DEFAULT NULL COMMENT '文件Hash',
  `version` varchar(50) DEFAULT NULL COMMENT '软件版本',
  `publisher` varchar(100) DEFAULT NULL COMMENT '发布者',
  `description` text COMMENT '描述',
  `install_type` enum('silent','normal','custom') DEFAULT 'silent' COMMENT '安装类型',
  `install_params` varchar(500) DEFAULT NULL COMMENT '静默安装参数',
  `is_portable` tinyint(1) DEFAULT '0' COMMENT '是否绿色版',
  `download_count` int unsigned DEFAULT '0' COMMENT '下载次数',
  `status` tinyint(1) DEFAULT '1' COMMENT '状态',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_category_id` (`category_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='软件管理';
```

#### zs_work_orders（维修工单）

```sql
CREATE TABLE `zs_work_orders` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `order_no` varchar(32) NOT NULL COMMENT '工单号',
  `customer_id` int unsigned DEFAULT NULL COMMENT '客户ID',
  `device_info` text COMMENT '设备信息(JSON)',
  `device_brand` varchar(100) DEFAULT NULL COMMENT '设备品牌',
  `device_model` varchar(100) DEFAULT NULL COMMENT '设备型号',
  `device_sn` varchar(100) DEFAULT NULL COMMENT '设备序列号',
  `fault_description` text COMMENT '故障描述',
  `service_type` varchar(50) DEFAULT NULL COMMENT '服务类型',
  `handler_id` int unsigned DEFAULT NULL COMMENT '处理人',
  `status` enum('pending','processing','completed','cancelled') DEFAULT 'pending' COMMENT '状态',
  `fee` decimal(10,2) DEFAULT '0.00' COMMENT '费用',
  `payment_status` enum('unpaid','paid','partial') DEFAULT 'unpaid' COMMENT '支付状态',
  `warranty_expire` date DEFAULT NULL COMMENT '保修到期',
  `remark` text COMMENT '备注',
  `completed_at` datetime DEFAULT NULL COMMENT '完成时间',
  `created_by` int unsigned DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_customer_id` (`customer_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='维修工单';
```

#### zs_logs（操作日志）

```sql
CREATE TABLE `zs_logs` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `log_type` enum('operation','login','client','task','system','error') NOT NULL COMMENT '日志类型',
  `user_id` int unsigned DEFAULT NULL COMMENT '操作用户',
  `client_id` int unsigned DEFAULT NULL COMMENT '相关客户端',
  `action` varchar(100) NOT NULL COMMENT '操作行为',
  `target_type` varchar(50) DEFAULT NULL COMMENT '目标类型',
  `target_id` int unsigned DEFAULT NULL COMMENT '目标ID',
  `detail` text COMMENT '详情',
  `request_ip` varchar(45) DEFAULT NULL COMMENT '请求IP',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_log_type` (`log_type`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_client_id` (`client_id`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志';
```

---

## 3. API 接口设计

### 3.1 接口规范

- 基础路径：`/api/v1`
- 格式：`Content-Type: application/json`
- 认证：`Authorization: Bearer <token>`
- 响应格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "meta": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### 3.2 接口列表

#### 认证模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /auth/login | 管理员登录 |
| POST | /auth/logout | 退出登录 |
| POST | /auth/refresh | 刷新 Token |
| GET | /auth/profile | 获取当前用户信息 |
| POST | /auth/client/register | 客户端注册 |
| POST | /auth/client/login | 客户端登录 |
| POST | /auth/client/heartbeat | 客户端心跳 |

#### 镜像管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /images | 镜像列表 |
| GET | /images/{id} | 镜像详情 |
| POST | /images | 上传镜像 |
| PUT | /images/{id} | 编辑镜像 |
| DELETE | /images/{id} | 删除镜像 |
| GET | /images/{id}/download | 下载镜像 |
| POST | /images/verify | 校验镜像 |
| POST | /images/convert | 格式转换 |
| GET | /images/client/list | 客户端拉取镜像列表 |

#### 客户端管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /clients | 客户端列表 |
| GET | /clients/{id} | 客户端详情 |
| PUT | /clients/{id} | 编辑客户端 |
| POST | /clients/{id}/approve | 审核通过 |
| POST | /clients/{id}/reject | 审核拒绝 |
| GET | /clients/groups | 分组列表 |
| POST | /clients/groups | 创建分组 |
| GET | /clients/versions | 版本列表 |
| POST | /clients/versions | 上传版本 |
| GET | /clients/stats | 客户端统计 |

#### 装机任务

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /tasks | 任务列表 |
| GET | /tasks/{id} | 任务详情 |
| POST | /tasks | 创建任务 |
| PUT | /tasks/{id} | 更新任务 |
| POST | /tasks/{id}/start | 启动任务 |
| POST | /tasks/{id}/pause | 暂停任务 |
| POST | /tasks/{id}/cancel | 取消任务 |
| POST | /tasks/{id}/retry | 重试任务 |
| GET | /tasks/templates | 任务模板列表 |
| GET | /tasks/stats | 任务统计 |

#### 无人值守

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /unattend/templates | 模板列表 |
| POST | /unattend/templates | 创建模板 |
| PUT | /unattend/templates/{id} | 编辑模板 |
| DELETE | /unattend/templates/{id} | 删除模板 |
| POST | /unattend/templates/{id}/validate | 验证模板 |

#### 软件管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /software | 软件列表 |
| GET | /software/{id} | 软件详情 |
| POST | /software | 上传软件 |
| PUT | /software/{id} | 编辑软件 |
| GET | /software/categories | 分类列表 |
| POST | /software/categories | 创建分类 |
| GET | /software/templates | 软件模板列表 |

#### 驱动管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /drivers | 驱动列表 |
| POST | /drivers | 上传驱动包 |
| DELETE | /drivers/{id} | 删除驱动 |
| POST | /drivers/{id}/inject | 注入驱动到镜像 |

#### 网络部署

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /pxe/config | 获取 PXE 配置 |
| PUT | /pxe/config | 更新 PXE 配置 |
| POST | /pxe/restart | 重启 PXE 服务 |
| GET | /network-deploy | 网络部署任务列表 |
| POST | /network-deploy | 创建部署任务 |

#### PE 定制

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /pe/versions | PE 版本列表 |
| POST | /pe/versions | 上传 PE 版本 |
| PUT | /pe/customize | 更新 PE 定制配置 |
| POST | /pe/build | 构建 PE 镜像 |
| POST | /pe/export | 导出为 ISO |

#### 客户与工单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /customers | 客户列表 |
| POST | /customers | 创建客户 |
| PUT | /customers/{id} | 编辑客户 |
| GET | /work-orders | 工单列表 |
| POST | /work-orders | 创建工单 |
| GET | /work-orders/stats | 工单统计 |

#### 系统管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /settings | 系统配置 |
| PUT | /settings | 更新系统配置 |
| GET | /logs | 日志列表 |
| GET | /stats/dashboard | 仪表盘统计 |
| GET | /stats/install-report | 装机报表 |
| GET | /stats/image-ranking | 镜像使用排行 |

---

## 4. 项目目录结构

```
Windows-ZS/
├── server/                          # 服务端 (PHP + MySQL)
│   ├── public/                      # Web 入口
│   │   ├── index.php               # 入口文件
│   │   ├── admin/                   # 后台管理页面
│   │   │   ├── dashboard/           # 控制台
│   │   │   ├── images/             # 镜像管理
│   │   │   ├── clients/            # 客户端管理
│   │   │   ├── tasks/              # 装机任务
│   │   │   ├── unattend/           # 无人值守
│   │   │   ├── software/           # 软件管理
│   │   │   ├── drivers/            # 驱动管理
│   │   │   ├── pxe/                # 网络部署
│   │   │   ├── pe/                 # PE 定制
│   │   │   ├── customers/          # 客户管理
│   │   │   ├── orders/             # 工单管理
│   │   │   ├── settings/           # 系统设置
│   │   │   └── logs/               # 日志审计
│   │   └── api/                     # API 入口
│   │       └── v1/                 # API v1
│   ├── app/                         # 应用代码
│   │   ├── controller/             # 控制器
│   │   ├── model/                  # 模型
│   │   ├── service/                # 业务逻辑层
│   │   └── middleware/             # 中间件
│   ├── config/                      # 配置
│   ├── route/                       # 路由
│   ├── storage/                     # 存储
│   │   ├── images/                 # 镜像文件
│   │   ├── pe/                     # PE 镜像
│   │   ├── software/               # 软件包
│   │   ├── drivers/                # 驱动包
│   │   └── logs/                   # 日志文件
│   └── database/                    # 数据库
│
├── client/                          # 客户端
│   ├── winpe/                       # WinPE 客户端
│   │   ├── ZSInstallAssistant/     # PE 主程序
│   │   │   ├── MainWindow.xaml     # 主界面
│   │   │   ├── Pages/              # 子页面
│   │   │   │   ├── InstallPage.xaml       # 一键装机
│   │   │   │   ├── UDiskPage.xaml         # U盘制作
│   │   │   │   ├── ToolsPage.xaml         # 工具大全
│   │   │   │   └── SoftwarePage.xaml      # 绿色软件
│   │   │   ├── Controls/           # 自定义控件
│   │   │   ├── Services/           # 服务层
│   │   │   │   ├── ApiService.cs         # API 通信
│   │   │   │   ├── ImageService.cs       # 镜像操作
│   │   │   │   ├── DiskService.cs        # 磁盘操作
│   │   │   │   ├── BootService.cs        # 引导修复
│   │   │   │   └── DriverService.cs      # 驱动操作
│   │   │   ├── Models/             # 数据模型
│   │   │   └── Utils/              # 工具类
│   │   └── resources/              # PE 资源
│   │
│   └── windows/                     # Windows 客户端
│       ├── ZSClient/               # WPF 主程序
│       │   ├── MainWindow.xaml     # 主界面
│       │   ├── Pages/              # 子页面
│       │   ├── Services/           # 服务层
│       │   └── Models/             # 数据模型
│       └── ZSClient.Installer/     # 安装程序
│
├── pxe/                             # PXE 配置
│   ├── config/                      # PXE 配置文件
│   │   ├── dnsmasq.conf            # DHCP/TFTP 配置
│   │   └── ipxe/                   # iPXE 脚本
│   ├── boot/                        # 引导文件
│   │   ├── ipxe.efi                # UEFI 引导
│   │   ├── ipxe.pxe                # Legacy 引导
│   │   ├── wimboot                 # WIM 引导
│   │   └── menu.ipxe               # 启动菜单
│   └── images/                      # 网络引导镜像
│
├── docs/                            # 文档
│   ├── 详细设计文档.md              # 本文档
│   ├── API文档.md                   # API 接口文档
│   └── 部署指南.md                  # 部署说明
│
├── tools/                           # 辅助工具
│   ├── pe-builder/                  # PE 构建工具
│   └── image-converter/             # 镜像转换工具
│
├── .env.example                     # 环境配置示例
└── composer.json                    # PHP 依赖
```

---

## 5. Web 后台菜单设计

```
ZS 装机助手 管理后台
├── 📊 控制台
│   ├── 仪表盘
│   └── 快捷操作
├── 📦 镜像管理
│   ├── 镜像列表
│   ├── 上传镜像
│   ├── 镜像源管理
│   └── 格式转换
├── 💻 客户端管理
│   ├── 客户端列表
│   ├── 登录审核
│   ├── 客户端分组
│   ├── 版本管理
│   └── 远程管理
├── 📋 装机任务
│   ├── 任务列表
│   ├── 创建任务
│   ├── 任务模板
│   ├── 定时任务
│   └── 装机记录
├── 🔧 装机工具
│   ├── U盘启动盘制作
│   ├── 引导修复工具
│   ├── 密码清除工具
│   └── 硬件检测工具
├── 🤖 无人值守
│   ├── 模板管理
│   ├── 应答文件编辑
│   └── 配置验证
├── 💾 数据与磁盘
│   ├── 系统备份管理
│   ├── 数据恢复工具
│   └── 磁盘工具
├── 🛡️ 病毒查杀
│   ├── 病毒库管理
│   └── 查杀记录
├── 🌐 网络部署
│   ├── PXE 配置
│   ├── 网络克隆
│   ├── 多播部署
│   └── 部署报告
├── 🖥️ PE 定制
│   ├── PE 版本管理
│   ├── PE 定制配置
│   ├── 内置工具管理
│   └── PE 构建导出
├── 🧩 扩展功能
│   ├── 软件管理
│   │   ├── 软件列表
│   │   ├── 软件分类
│   │   ├── 上传软件
│   │   └── 装机模板
│   ├── 驱动管理
│   │   ├── 驱动列表
│   │   ├── 上传驱动
│   │   └── 驱动注入
│   ├── 脚本管理
│   └── 系统优化配置
├── 👥 客户与工单
│   ├── 客户信息管理
│   ├── 维修工单
│   ├── 维修记录
│   ├── 收费管理
│   └── 保修管理
├── 📊 报告统计
│   ├── 装机报表
│   ├── 客户端统计
│   ├── 镜像使用排行
│   └── 工单统计
├── 📋 日志审计
│   ├── 操作日志
│   ├── 客户端日志
│   ├── 任务日志
│   └── 安全日志
└── ⚙️ 系统管理
    ├── 系统配置
    ├── 用户权限
    ├── 存储管理
    ├── 通知公告
    ├── 插件扩展
    ├── Webhook 配置
    ├── API 管理
    └── 系统升级
```

---

## 6. 客户端 UI 设计

### 6.1 WinPE 客户端主界面

4 大入口卡片，点击进入子窗口：

- **📦 一键装机** — 镜像选择(大) + 磁盘下拉+分区 + 安装选项(紧凑药丸) + 开始装机
- **💿 U盘制作** — U盘选择 + PE版本 + 制作选项 + PE定制
- **🔧 工具大全** — 53 个工具，按分类网格排列
- **📦 绿色软件** — 8 大分类 + 装机模板

### 6.2 Windows 客户端

- 标签页：装机、任务、下载、设置
- 主要功能：镜像下载缓存、任务管理、一键重启进 PE

---

## 7. 开发计划

### 阶段划分

| 阶段 | 内容 | 预估时间 |
|------|------|----------|
| P1 基础框架 | PHP 后端框架 + 数据库 + 基础 API + 后台登录 | 5 天 |
| P2 镜像管理 | 镜像上传/列表/编辑 + 客户端拉取 | 4 天 |
| P3 客户端管理 | 客户端注册/登录/心跳 + 后台列表 | 3 天 |
| P4 装机任务 | 任务创建/分发/跟踪 + 模板 | 5 天 |
| P5 无人值守 | 应答模板管理 + 配置编辑 | 3 天 |
| P6 客户端核心 | WinPE 主界面 + 4 大入口子窗口 | 10 天 |
| P7 客户端工具 | 53 个工具逐个实现 | 10 天 |
| P8 软件管理 | 软件上传/分类/模板 + 静默安装 | 3 天 |
| P9 驱动管理 | 驱动包管理 + 注入 | 2 天 |
| P10 客户工单 | 客户管理 + 工单系统 | 4 天 |
| P11 网络部署 | PXE 配置 + 网络克隆 | 5 天 |
| P12 PE 定制 | PE 定制管理 + 构建导出 | 4 天 |
| P13 系统管理 | 用户权限 + 日志 + 统计 | 4 天 |
| P14 测试联调 | 功能测试 + Bug 修复 | 5 天 |
| P15 Windows 客户端 | Windows 桌面客户端 | 5 天 |
| P16 部署上线 | 宝塔部署 + 文档 | 2 天 |

**总计预估：74 个工作日**

### 开发优先级

```
第一优先级 (P1-P4)：核心框架 + 镜像 + 客户端 + 任务
第二优先级 (P5-P9)：无人值守 + 客户端核心 + 工具 + 软件 + 驱动
第三优先级 (P10-P13)：客户工单 + 网络部署 + PE 定制 + 系统管理
第四优先级 (P14-P16)：测试 + Windows 客户端 + 部署上线
```

---

> 文档版本：v1.0
> 最后更新：2026-08-30
> 编写：ZS Studio