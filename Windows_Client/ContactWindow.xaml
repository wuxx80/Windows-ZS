<Window x:Class="Windows_Client.ContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="联系我们" Height="360" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F7FA" FontFamily="Microsoft YaHei" WindowStyle="ToolWindow">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,18">
            <Border Width="46" Height="46" CornerRadius="10" Background="#2A3A5C">
                <TextBlock Text="{Binding Site.LogoText}" FontSize="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Site.Title}" FontSize="18" Foreground="#1E293B"/>
                <TextBlock Text="联系我们" FontSize="12" Foreground="#8896A8" Margin="0,3,0,0"/>
            </StackPanel>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="欢迎通过以下方式联系我们，我们将尽快回复您的咨询。" FontSize="12" Foreground="#64748B" Margin="0,0,0,14" TextWrapping="Wrap"/>
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Site.Contact}" FontSize="13" Foreground="#334155" TextWrapping="Wrap" LineHeight="26"/>
            </ScrollViewer>
        </Border>
        <Button Grid.Row="3" Content="关闭" Width="100" Height="34" Margin="0,18,0,0" HorizontalAlignment="Center"
                Click="OnCloseClick" Background="#1976D2" Foreground="White" BorderThickness="0" Cursor="Hand"/>
    </Grid>
</Window>
