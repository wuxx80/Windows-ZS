<Window x:Class="Windows_Client.VersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="版本信息" Height="300" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F7FA" FontFamily="Microsoft YaHei" WindowStyle="ToolWindow">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,18">
            <Border Width="46" Height="46" CornerRadius="10" Background="#2A3A5C">
                <TextBlock Text="{Binding Site.LogoText}" FontSize="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Site.Title}" FontSize="18" Foreground="#1E293B"/>
                <TextBlock Text="{Binding Site.Subtitle}" FontSize="11" Foreground="#8896A8" Margin="0,3,0,0"/>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="客户端版本" FontSize="13" Foreground="#64748B"/>
            <TextBlock Grid.Column="1" Text="Windows 客户端 v0.0.268311" FontSize="13" Foreground="#1E293B"/>
        </Grid>
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="站点版本" FontSize="13" Foreground="#64748B"/>
            <TextBlock Grid.Column="1" Text="{Binding Site.Version}" FontSize="13" Foreground="#1E293B"/>
        </Grid>
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="目标框架" FontSize="13" Foreground="#64748B"/>
            <TextBlock Grid.Column="1" Text=".NET 8.0 (WPF)" FontSize="13" Foreground="#1E293B"/>
        </Grid>
        <Border Grid.Row="4" Background="White" CornerRadius="8" Padding="14" Margin="0,6,0,0">
            <StackPanel>
                <TextBlock Text="官网" FontSize="13" Foreground="#64748B" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Site.Website}" FontSize="13" Foreground="#1976D2"/>
            </StackPanel>
        </Border>
        <Button Grid.Row="5" Content="关闭" Width="100" Height="34" Margin="0,18,0,0" HorizontalAlignment="Center"
                Click="OnCloseClick" Background="#1976D2" Foreground="White" BorderThickness="0" Cursor="Hand"/>
    </Grid>
</Window>
